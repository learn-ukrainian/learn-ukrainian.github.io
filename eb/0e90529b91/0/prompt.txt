[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/fastlane-deps-mapping
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Task: fastlane slim-deps mapping gap — API tests need pydantic/starlette/filelock (one small PR)

PR #7201's `Pytest fastlane (changed tests)` failed at "Install slim fastlane test dependencies":
`fastlane requirements failed: selected tests import unmapped third-party module(s): context_profiles,
filelock, pydantic, starlette, task_identity, thread_handoff_canary, validate_checkpoint_routing`.
Four of those are first-party sibling-fallback imports — a scanner defect fixed separately in PR #7200;
IGNORE them. Your unit is the three REAL packages: `filelock`, `pydantic`, `starlette`.

## Outcome
`scripts/ci/requirements-fastlane.txt` (the reviewed slim profile, installed with `-c requirements-lock.txt`)
covers the direct third-party imports that the selected API/delegate tests reach, so a PR touching
`tests/test_delegate_api.py` / `tests/api/*` no longer hard-fails the install step. Pin each at the exact
version in `requirements-lock.txt` and keep the file's section/comment conventions.

## Evidence to produce (quote raw output)
1. Which selected test → which module imports each of the three names (the scanner's graph; reproduce
   with `scripts/ci/fastlane_requirements.py` helpers on `tests/test_delegate_api.py`
   `tests/test_delegate.py` `tests/api/test_delegate_active.py` `tests/orchestration/test_job_host_exec.py`).
2. After the change, run the scanner's selection for those tests and show the three names are satisfied
   (the remaining unmapped set must be exactly the four first-party names, which #7200 removes).
3. Existing scanner tests green: `.venv/bin/pytest -q tests/test_ci_fastlane_requirements.py`; ruff n/a.
4. One conventional commit (`ci(fastlane): map pydantic/starlette/filelock for API test paths (#7141)`,
   X-Agent trailer). NO push, NO auto-merge, NO amend after reporting. Report HEAD SHA + evidence +
   ready PR body (note: unblocks #7201 together with #7200).