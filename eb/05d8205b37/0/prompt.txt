[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/kimi/atlas-a11y-6814-6815
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Dispatch: rename colliding --lu-on-accent token on PR #6819

## ROUTING_CARD_V1
- tier: practical
- model_x_harness: kimi k3-256k (author of #6819)
- why_this_tier: author-fix of a named CF finding; mechanical token rename
- advisor_packet: n/a
- owned_paths:
  - site/src/styles/course.css
  - site/src/pages/words-of-the-day/practice.astro
  - site/src/pages/lexicon/index.astro
  - site/tests/unit/k3-chrome-locale.test.ts
- acceptance_cmd: grep -n -- '--lu-on-accent' site/src/styles/course.css must NOT introduce a new :root/[data-theme] override; custom.css definition stays; vitest k3-chrome-locale green
- alternatives_considered:
  1. claude — just wrote the CF; cannot implement the fix they reviewed
  2. cursor — hot / pace deficit
  3. codex — hot / deficit
- CAPACITY_CARD: pick=kimi status=cool avoid=codex,cursor source=routing-budget

## CF finding (binding)
Claude VERDICT CHANGES_REQUESTED posted on PR #6819. `course.css` redeclared `--lu-on-accent`, shadowing `site/src/css/custom.css` (`#1c1e21` on gold `--lu-accent`). Light-theme `.lu-badge` / filter / cefr badges become white-on-gold.

## Work
1. Stay on `kimi/atlas-a11y-6814-6815` (PR #6819), head `295c2ac0`.
2. Rename the **new** token introduced by this PR to `--lu-on-primary-fill` (or another unused name). Update every consumer this PR added:
   - `site/src/styles/course.css` declarations
   - `site/src/pages/words-of-the-day/practice.astro`
   - `site/src/pages/lexicon/index.astro`
   - `site/tests/unit/k3-chrome-locale.test.ts` pin
3. Do **not** change `site/src/css/custom.css` `--lu-on-accent`.
4. Confirm `rg 'lu-on-accent' site/src/styles/course.css` no longer assigns that name in :root or [data-theme].
5. `cd site && npx vitest run tests/unit/k3-chrome-locale.test.ts`
6. Commit + push same branch. Trailer: `X-Agent: kimi/atlas-6819-rename-on-accent`
7. No merge.

## Done means
Pushed rename; custom.css token intact; new head SHA printed.