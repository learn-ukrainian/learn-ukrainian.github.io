[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/phase3-6375-school-context-negative-recovery-cursor
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

ROUTING_CARD_V1
tier: practical
model_x_harness: Cursor Auto x Cursor
why_this_tier: bounded CodeQL-static security correction on Cursor-owned PR branch; Cursor has about 96.5% monthly capacity remaining
advisor_packet: Claude exact-head review at batch_state/tasks/review-6782-school-negative-recovery-claude-r2.result BLOCKED only because CodeQL alert #378 cannot prove a variable chmod mode is safe; reviewer verified the runtime hardening, receipt, real custody reproduction, and tests otherwise clean
owned_paths: scripts/projects/open_model_data/phase3_school_context_negative_recovery.py; tests/test_open_model_phase3_school_context_negative_recovery.py; data/projects/open_model_data/inventory/phase3_school_context_negative_recovery_receipt_v1.json
acceptance_cmd: Ruff + focused/siblings + complete tests/test_open_model_phase3_*.py; GitHub CodeQL alert/check must be green
alternatives_considered: Codex rejected because hot/deficit and this is bounded implementation; Grok rejected at 96% until reset; Claude reserved as independent reviewer
parallel_free_seats: Cursor chosen for branch continuity; other lanes remain available for unrelated work

Repair PR #6782 after exact-head BLOCK at 5a2cc4ec200f9dedd9897d737a547cc695745dc2. You are not alone; preserve unrelated work.

1. Confirm clean worktree and expected head. Fetch and rebase onto current origin/main (now includes merged #6781). Resolve only in scope.
2. Eliminate CodeQL alert #378 at current line 220. The runtime design is secure, but CodeQL flags `os.chmod(path, mode)` because `mode` is variable despite the guard. Make the permission policy statically obvious: remove caller-controlled/dynamic mode from `_atomic_write`, use the literal or direct constant owner-only mode 0o600/PRIVATE_FILE_MODE at every creation/fchmod/chmod path, and update callers/tests. Do not dismiss/suppress CodeQL and do not edit CodeQL config.
3. Preserve mkstemp O_EXCL/random temp behavior, cleanup-on-error, atomic replace, symlink safety, private text discipline, all denominators, and false gates.
4. Regenerate the committed receipt through the production path against pinned custody because implementation hash changes. Never print private text.
5. Add/adjust tests so they prove the API cannot request a permissive mode and the output is 0600. Avoid a signature that still exposes a mode value to CodeQL.
6. Ruff, focused, sibling, and full tests/test_open_model_phase3_*.py. Report exact counts.
7. Inspect diff/status. Commit with X-Agent: cursor/fix-6782-codeql-static-cursor and force-with-lease push. Keep PR draft; no merge/ready/comment.
8. Return new SHA, receipt SHA, implementation SHA, test counts, and residual.

Do not touch the schema or any file outside the three owned paths unless an actual rebase conflict requires it; report before expanding scope.