[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/kimi/infra-6812-bridge-module-identity
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Dispatch brief — infra-6812-bridge-module-identity

**Issue:** #6812 — read it fully first (`gh issue view 6812`). It records the Kimi-seat
review finding and the recommended direction.

## Evidence preamble (#M-4, binding)
Every claim in your report and PR must be tool-backed: state the claim, the deterministic
command that proves it, and quote the raw output.

## Shippable unit (one PR)
Standardize the bridge package on the single module identity recommended in #6812 across
the test suite, so monkeypatching and module-level state can no longer diverge between the
two `sys.modules` entries — and add a guard that prevents the second identity from being
reintroduced.

## Acceptance criteria (verify BEFORE opening the PR)
1. Grep proof of zero remaining non-canonical bridge imports in the test suite — quote the
   exact command and its (empty) output. Grep path segments, not substrings (#M-16).
2. The reintroduction guard is mutation-checked (#M-16): add a throwaway non-canonical
   import locally → guard must fail; quote that run, then remove the throwaway.
3. Full `.venv/bin/pytest tests/ai_agent_bridge/` green, plus every other test file you
   touched; `.venv/bin/ruff check` green on changed files.
4. No production-code behavior change (imports/identity only) — state this explicitly with
   the diff summary.

## Out of scope
- The ~340 inline `sys.path.insert` primer deletions mentioned in the issue — file that as
  a follow-up note on #6812 if your work confirms it is now possible; do not do it here.
- Any bridge feature/behavior change.

## Process (numbered, binding)
1. Work only inside your dispatch worktree (already created for you).
2. Implement + guard.
3. Run pytest + ruff; quote outputs.
4. Conventional commit(s) with trailer `X-Agent: kimi`.
5. Push; open PR titled `test(bridge): ... (#6812)` referencing #6812.
6. Do NOT enable auto-merge; do NOT merge.

---
ROUTING_CARD_V1
task_id: infra-6812-bridge-module-identity
tier: practical
model_x_harness: kimi/k3-256k (everyday coding seat)
why_this_tier: wide mechanical sweep across ~16 test files + a guard; needs care, not authority
advisor_packet: fable (this brief)
owned_paths: tests/, scripts/ai_agent_bridge/ (identity only)
acceptance_cmd: .venv/bin/pytest tests/ai_agent_bridge/ && .venv/bin/ruff check <changed files>
alternatives_considered:
  - cursor/auto — free — taken by #6803 (concurrency_limit 1)
  - agy/gemini-flash — free — taken by #6802 (disjoint work)
parallel_free_seats: cursor, agy, codex per /api/orient headroom