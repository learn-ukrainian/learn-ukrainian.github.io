[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/grok/infra-6976-no-idle-r2
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Task: implement #6976 — no-idle enforcement, report-only first (one PR)

You are grok, implementing GitHub issue #6976 on stream infra-harness (epic #6943). The design
is already settled by operator-directed cross-family discussion (codex+kimi,
conversation_f8aff2ab2c13, recorded in the issue body) — implement THAT spec, do not redesign.

**Shippable unit (ONE PR):** build-order items 1 and 2 from the issue body, report-only:
1. **Conditional settle-event reminder**: at every dispatch/review settle, show eligible ready
   items + WIP/resource caps, and require either a dispatch or a structured disposition code
   (`dependency_blocked | review_wip_cap | ci_capacity | disk_capacity | human_decision |
   no_ready_work`). No nagging when nothing is actionable.
2. **Eligibility-aware idle telemetry, report-only**: eligible idle opportunity-seconds +
   settle events lacking a dispatch or valid disposition, per the eligibility definition in
   the issue. Report-only — no enforcement, no thresholds, no gating.

**Out of scope (do NOT build):** issue items 3 (Monitor idle dashboard) and 4 (admission
control); any `--enforce` behavior change beyond what the issue text already grants
`driver_breadth_report`; any raw idle-time threshold.

## Coordination constraint (facts, not design)
PR #6981 (yours, in cross-family review at head d554d8da) touches `scripts/delegate.py`
task-id/receipt handling and is expected to merge soon. Your branch cuts from current
`origin/main`, which does NOT yet contain it. Keep your conflict surface with #6981 minimal;
if #6981 merges while you work, rebase onto main before opening the PR.

## Evidence rule (#M-4)
Every claim in your report is backed by quoted raw tool output. Mutation-check every guard
test: revert the behavior → the test must fail (quote it), else the test is imaginary.

## Steps
1. Work in your dispatch worktree (already created on a fresh branch from origin/main).
2. Read issue #6976 body in full (`gh issue view 6976`) — it is the spec of record.
3. Implement items 1+2. Design choices (module layout, data model, wiring point) are YOURS —
   propose them in the PR description with a one-paragraph rationale.
4. Tests for every new function/behavior; run targeted pytest via
   `.venv/bin/python -m pytest <files>` (NOTE: the `.venv/bin/pytest` launcher is currently
   broken — always use `python -m pytest`). Then `.venv/bin/ruff check` on changed files.
5. Conventional commit(s); reference `#6976`.
6. You cannot push (known lane limitation) — leave the worktree committed and clean, report
   the branch name + head SHA; the driver finalizes push + PR.
7. NO auto-merge, NO merge — driver owns the merge after cross-family review.
8. Report: what you built, where it wires in, quoted test/ruff evidence, mutation-check
   evidence, residual (anything from items 1+2 you could not finish and why).

## Acceptance criteria
- Settle-event reminder fires only when eligible ready items exist; requires dispatch-or-
  disposition; the six disposition codes accepted, anything else rejected.
- Telemetry is report-only and eligibility-aware per the issue definition.
- Every new behavior has a test that fails when the behavior is reverted (quoted proof).
- Targeted pytest green via `python -m pytest`; ruff clean; tree committed, no untracked junk.

## Routing card (ROUTING_CARD_V1)
tier=practical · model_x_harness=grok-4.6/native-grok · why_this_tier=free implement-capable
lane; just authored the adjacent delegate task-id guard (#6981) — highest context-fit; §2c.3
same-lane pipelining while its unit N is in review · owned_paths=scripts/fleet/**,
scripts/orchestration/**, scripts/delegate.py (minimal), tests/** ·
acceptance_cmd=`.venv/bin/python -m pytest <new test files>` ·
alternatives_considered=cursor (occupied: 2 live out-lane tasks, concurrency 1), agy (busy:
infra-6972-rebase), kimi (HOT for dispatch per lane facts), codex (dispatch deficit; holds
the review seat for #6981) · parallel_free_seats=none remaining implement-capable ·
advisor_packet=n/a (tier=practical; spec pre-reviewed cross-family: codex+kimi discussion on
issue) · research_classification=generic (no registry record matches; pointer-free,
deliberate)