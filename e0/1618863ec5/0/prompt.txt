[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/agy/claudex-7210-delta3
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

Delta 3 on PR #7233 (branch agy/claudex-repo-python-7210, head 4e29649a21 — checked out). CI fastlane FAILS on this exact head with two findings; local runs are green, so both are environment-latent — root-cause properly, do not band-aid. (1) tests/orchestration/test_claudex_supervisor.py::test_valid_rollover_relaunches_exact_route_once fails in CI: ValueError: Claudex rollover request failed: [Errno 2] No such file or directory: '/home/runner/work/.../.venv/.venv/bin/python' — note the DOUBLED .venv: somewhere your delta-1 change to the rollover test's repo_root parameter passes a VENV DIRECTORY where a repo root is expected (project_python().parent.parent is a venv dir, never a repo root). Work out why it is masked locally (canonical_state_root probably rescues it) and pass the actual canonical repo root; make the test fail the same way in both environments if the wrong thing is passed. (2) tests/test_subprocess_timeout_guard.py::test_no_timeout_less_subprocess_calls_under_tests (#5740 guard): your new E2E subprocess.run calls at tests/orchestration/test_claudex_supervisor.py:552 :561 :577 :616 lack timeout= — add explicit bounds (30 for git fixture calls, 60-120 for the supervisor E2E run). Acceptance (quote raw output; primary interpreter /Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/pytest, from YOUR venv-less worktree root): tests/orchestration/test_claudex_supervisor.py tests/orchestration/test_thread_handoff.py tests/test_subprocess_timeout_guard.py ALL green — the guard suite is part of acceptance from now on; ruff clean. ONE commit (X-Agent trailer). NO push, NO amend after reporting. Report HEAD SHA + why-CI-differed narrative + evidence; driver re-runs independently.