[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/kimi/infra-6957-cf-smoke
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Dispatch brief — #6957 Cursor CF smoke tests

CAPACITY_CARD: pick=kimi status=unknown-local (5h window usable; CodexBar binary missing on Monitor) avoid=codex source=orient.headroom
ROUTING_CARD_V1:
- tier: practical
- model_x_harness: kimi k3 native
- why_this_tier: test-only CF identity checks; Kimi is a strong reviewer/implementer; Cursor concurrency 1 already used for #7026
- owned_paths: tests for CF/review identity; model-assignment consumers
- alternatives_considered: cursor (on #7026); agy (on #7028); claude (CF #7053)
- parallel_free_seats: cursor #7026; agy #7028
- acceptance_cmd: `.venv/bin/python -m pytest -q tests -k cursor -k 'review or cf or quorum or attest'`

Initiator: grok-infra. AGENT_NO_MERGE=1. Stream: infra-harness #6943. Parent: #6952.

## Problem
`cursor:auto` is never an acceptable formal-review identity. A Cursor driver must not launder Auto into fake single-reviewer CF. Same-family review must refuse using the **attested** family (Cursor `grok-4.6` is not native Grok; Cursor `composer-2.5` is not Kimi).

## Scope (issue #6957 acceptance — all four boxes)
Automated tests, no product-policy rewrite:

- attested `cursor:composer-2.5` author + same-family reviewer → refuse
- attested Cursor author + eligible other family exact-head → allowed
- `cursor:auto` with `resolved_model=null` → not driver-of-record; not single-reviewer CF
- #6489 dual-family quorum path still exists for already-authored Auto **worker** PRs (do not delete it)

Do not mock away `resolved_model`. Paste real pytest node ids.

## Non-goals
Changing the everyday reviewer ladder. Making Auto a CF reviewer. Self-merge. Architecture/process changes.

PR: `Refs #6957`. Trailer `X-Agent: kimi/infra-6957-cf-smoke`. Conventional commit. Push + open PR. Do **not** merge.

## Verify
```bash
.venv/bin/python -m pytest -q tests -k cursor -k 'review or cf or quorum or attest'
.venv/bin/ruff check $(git diff --name-only origin/main)
```
Quote the node ids. All four acceptance boxes checked in the PR body.