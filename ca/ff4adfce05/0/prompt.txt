[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/agy/work-6850-fix-predicate
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Address review findings on PR #6854 (issue #6850 — actionable-only default)

You are the authoring lane for PR #6854. The cross-family review verdict is CHANGES
REQUESTED with exactly one finding. Read the review comment on PR #6854 in full (posted by
claude-infra, "CROSS-FAMILY REVIEW OF RECORD") and address it on the SAME branch.

```
ROUTING_CARD_V1
tier: practical
model_x_harness: gemini-3.7-flash-high × agy
why_this_tier: authoring lane addresses its own review findings (#0 lane model)
advisor_packet: n/a
owned_paths: dashboards/work.html, tests/**
acceptance_cmd: see Acceptance below
alternatives_considered: none applicable — review fixes return to the authoring lane
parallel_free_seats: n/a (single follow-up)
```

## Evidence discipline (#M-4)
Quote deterministic command output for every claim in your follow-up PR comment.

## Steps
1. Work only in the dispatch worktree on branch `agy/work-6850-actionable-default`.
2. Implement the required change from the review comment (deny-list polarity, exact
   predicate stated there).
3. Extend the browser tests per the review: include a healthy ON_TRACK/OPEN_GITHUB row and
   assert it is HIDDEN in the default view, still VISIBLE under `?view=all`.
4. Run the same test set as before plus ruff; quote counts.
5. Commit (conventional, reference #6850 and the PR), push to the same branch.
6. Post a short evidence comment on PR #6854 describing the delta. Do NOT merge, do NOT
   enable auto-merge.

## Acceptance
- With a healthy projection (mostly ON_TRACK/OPEN_GITHUB rows), the default view shows
  ONLY red/yellow + genuinely work-demanding rows; All view unchanged.
- All previously passing tests still pass; new fixture row covered both ways.