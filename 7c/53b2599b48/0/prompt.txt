[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/agy/infra-6959-gemini-default-model
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Delta on PR #6961: preserve AB_GEMINI_MODEL through the full ask path (CF finding)

## ROUTING_CARD_V1
- tier: practical
- model_x_harness: gemini-3.5-flash-high × agy ACP wrapper (workspace-write, existing dispatch worktree)
- why_this_tier: review-finding fix goes back to the authoring lane (agy authored PR #6961)
- advisor_packet: n/a
- owned_paths: scripts/ai_agent_bridge/_gemini.py, tests/ai_agent_bridge/test_process_routing.py, tests/test_gemini_bridge.py
- acceptance_cmd: /Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python -m pytest tests/ai_agent_bridge/test_process_routing.py tests/test_gemini_bridge.py -q
- alternatives_considered: (1) driver fixes inline — REJECTED: fixes to review findings belong to the authoring lane; (2) different lane — REJECTED: PR ownership
- parallel_free_seats: cursor, grok

## Evidence preamble (#M-4 — binding)
Every claim in your report must quote raw tool output from THIS run.

## Context
Your PR #6961 (branch agy/infra-6959-gemini-default-model, worktree = your cwd, head 490e2fa289)
received a cross-family FAIL with ONE finding (full text on the PR):

`AB_GEMINI_MODEL` is resolved TWICE on the synchronous ask path: `ask_gemini`
(`_gemini.py:121-161`) resolves the env override, then `process_and_respond` (`:279-282`)
resolves again — and because real Gemini model names begin with `gemini`,
`resolve_compat_model` replaces the resolved override with the registry pin. Reviewer's live
probe: `AB_GEMINI_MODEL=gemini-3.1-pro-high` reached `_run_gemini_sync` as
`gemini-3.7-flash-high`. Your tests mock `process_and_respond` BEFORE the second resolution
(`test_process_routing.py:402-426`, `test_gemini_bridge.py:422-446`), so they cannot catch it.

## Shippable unit
One commit on the PR branch fixing the double resolution — resolve the model exactly once (or
carry override provenance so the second resolution is a no-op for env-overridden values) — plus
an END-TO-END regression test that drives `ask_gemini` THROUGH the real `process_and_respond`
resolution boundary and asserts the env override survives to the runner invocation. The test
must fail on the current head (mutation check: verify it reds before your fix, quote that
output) and pass after. Out of scope: anything beyond this finding.

## Steps
1. Verify cwd worktree, `git rev-parse HEAD` = 490e2fa289aa11854fd234879adb0b17c3eac3f2, tree
   clean. Else STOP and report.
2. Write the failing e2e test FIRST; run it; quote the red output (proof the finding reproduces).
3. Fix the double resolution.
4. Acceptance: both test files green via
   `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python -m pytest
   tests/ai_agent_bridge/test_process_routing.py tests/test_gemini_bridge.py -q` + ruff on
   changed files. (`.venv/bin/pytest` launcher is broken — known; use `python -m pytest`.)
5. Conventional commit `fix(bridge): resolve gemini model once, preserve AB_GEMINI_MODEL override
   (#6959)`; push to the PR branch (you pushed the original — same remote flow). Do NOT touch
   auto-merge.
6. Report: new HEAD, red-then-green test outputs, ruff tail.