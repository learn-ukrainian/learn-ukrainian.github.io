[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/services-7112-delta
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Delta for PR #7112 (`fix(ops): services.sh fix + SSH LocalForward remote delegate`) — CF FAIL: remote shell injection

You are the cursor lane (author of PR #7112, branch `cursor/services-ssh-tunnel-fix-108c`, head 948b5ea6db). The
cross-family reviewer (kimi) FAILED the PR on one blocking finding, with three non-blocking residuals. Fix all four in
ONE commit on this branch. NO GitHub auth — never call `gh`; return text.
Primary interpreter: `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python`.

## Blocking — remote shell injection (reviewer-proven)
`_delegate_remote` (services.sh:327) interpolates `$*` unquoted into the remote shell string:
    command ssh -o BatchMode=yes "$host" "cd \"$root\" && ./services.sh ${remote_action}${*:+ $*}"
Reviewer proof with an ssh shim and `LU_SERVICES_SSH_HOST=fakehost`:
    ./services.sh start 'api;touch /tmp/ssh_inject_test/PWNED'   → PWNED created; runs even though `cd` failed (`;` binds looser than `&&`).
Pre-PR an unknown arg was an inert "Unknown service"; post-PR, whenever delegation triggers (the target environment),
any arg is a remote command on the shared writer host.
Required: validate every service argument against the known service set BEFORE delegation (unknown ⇒ refuse locally
with the existing "Unknown service" path, never forwarded), AND shell-quote what is forwarded (`printf '%q'`). Add a
regression test that feeds an injection payload through the delegation path and asserts the ssh shim never receives it.

## Non-blocking residuals from the review (fix in the same commit)
1. `tests/api/test_services_ops.py::test_pid_reconciliation` and `::test_pid_reconciliation_integration` share the REAL
   worktree `.pids/api.pid` and fail under `pytest -n 2` (reviewer reproduced on origin/main too — pre-existing, but
   this file is yours in this PR and the PR tier is about to run under xdist). Make them hermetic (tmp_path pids dir).
2. The spawn-refusal unit test self-stubs `_ssh_tunnel_port_pid`, so `_is_ssh_tunnel_pid` is only covered indirectly.
   Add a direct test of the real detection helper (comm==ssh / ssh-leading cmdline vs a non-ssh owner).
3. Document in the header: (a) `start` with no args delegates ALL services when any one port is tunneled;
   (b) delegated calls drop `--live`/`--force`; (c) behaviour when `lsof` is absent.

## Acceptance (quote raw output)
a. `bash -n services.sh`; `shellcheck services.sh` — no NEW findings vs origin/main (pre-existing SC2086×2/SC2034 may stay).
b. `.venv/bin/python -m pytest tests/api/test_services_ops.py -q` and with `-n 2` — all pass both ways (no skips introduced).
c. Injection regression: show the new test FAILING with the fix reverted (git stash the services.sh hunk), then passing.
d. `.venv/bin/ruff check tests/api/test_services_ops.py` clean.
Commit (`fix(ops): validate + quote delegated service args; hermetic pid tests (#7112)`), push this branch (you have no
`gh`; `git push` works), do NOT enable auto-merge. Final response: head SHA, evidence a–d, CLOBBER SELF-AUDIT line.