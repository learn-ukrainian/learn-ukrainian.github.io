{"metadata":{"actual_model":"gemini-3.7-flash-high","entrypoint":"delegate","harness":"agy","requested_model":"gemini-3.7-flash-high","runner_agent":"agy"},"repo_path":"/Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/agy/infra-7023-leftover-apis","session_id":"fleet-7be368c45f82405f9ccf73ff679e2e02","timestamp":"2026-08-18T19:22:26.497620Z","type":"session"}
{"text":"[delegate worktree]\nRun all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/agy/infra-7023-leftover-apis\nDo not switch branches in the main checkout.\nIf you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.\n\n[shared project interpreter]\nNever create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.\nSparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).\n\n[optional delivery signal]\nCommits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {\"outcome\":\"no_change\",\"reason\":\"why no changes are required\"}`. This line is optional — its absence never fails the dispatch.\n\n# Dispatch brief — #7023 leftover public-API surfaces\n\nCAPACITY_CARD: pick=agy status=cool will_last=true (gemini weekly ~65% remaining) free=cursor,kimi,claude avoid=codex(near_cap/deficit) source=codexbar+routing-budget\nROUTING_CARD_V1:\n- tier: practical\n- model_x_harness: agy / gemini-flash (Antigravity)\n- why_this_tier: public Monitor mount leftover; AGY fit for harness/infra scripts; cool budget\n- advisor_packet: n/a (filed leftover after teacher moved to private :8443/:8788)\n- owned_paths: scripts/api/main.py; scripts/api/hramatka_router.py (unmount only — do not delete the module if private/tests still import); scripts/api/rag_router.py; docs/MONITOR-API.md; tests that assert these mounts\n- acceptance_cmd: see Verify\n- alternatives_considered: cursor (used for sibling #7024 last-good); kimi (docs entrypoints); claude (rules/scripts index); codex (REFUSE deficit)\n- parallel_free_seats: cursor #7024; kimi #7012+#7014; claude #7011+#7013\n\nInitiator: grok-infra (epic:6943 / infra-harness). Stream lease already held — do not claim. AGENT_NO_MERGE=1.\n\n## User-visible outcome\nPublic Monitor (`127.0.0.1:8765`) is Monitor + Sources. It must not keep a forever-503 `/api/hramatka/readyz` because baker/schema live on the **private teacher**. `/api/rag/*` leftover name is either aliased to `/api/sources/*` or documented as a temporary alias with a removal date. Do **not** invent `/api/readyz`.\n\n## Live evidence (issue #7023 + comment)\nPublic process on `21382b1d`:\n1. `/api/hramatka/lessons/{id}/support`, `/api/hramatka/linguistics/verify`, `/api/hramatka/readyz` still mounted via `scripts/api/main.py` `include_router(hramatka_router)` + `initialize_hramatka_store`. `/api/hramatka/readyz` → 503 (`baker`, `schemas`).\n2. `/api/rag/stats` 200; `/api/sources/stats` 404. `/api/health` still reports `mcp_rag`. `services.sh` already calls `rag` a legacy alias \"remove after the next quarterly cleanup pass\".\n3. `/api/readyz` is already 404. Live probes are `/api/health` and `/api/work/v1/health`. **Do not add** a k8s-style alias.\n\nIssue expected (quote): \"Public Monitor either drops the Hramatka/RAG leftovers or documents them as intentional shared contracts.\" Teacher/Hramatka now runs from infra-private `./services.sh` on loopback :8443/:8788. **Drop the public Hramatka mount.** For RAG, prefer `/api/sources/*` as the live name and keep `/api/rag/*` only as a documented temporary alias if callers still exist (prove with rg, do not guess).\n\nOptional same-PR only if cheap and you have proof: static aliases `GET /work` → `/work.html`, `GET /orient` → `/orient.html` (comment on #7023). Do not boil the ocean of deprecated `/api/blue/live-status` etc. (already documented P3).\n\n## Work\n1. rg callers of `/api/hramatka` and `/api/rag` in this public repo + tests + frontend. Unmount public Hramatka only if nothing in **this** public process still requires it.\n2. Add `/api/sources/*` (or rename router prefix) so `/api/sources/stats` is not 404. Update `/api/health` `mcp_rag` label if that string is now a lie.\n3. Update `docs/MONITOR-API.md` OpenAPI-facing notes. Tests: public app no longer serves `/api/hramatka/readyz` 503; `/api/sources/stats` exists; `/api/readyz` stays 404.\n4. Conventional commit + push + PR linking **#7023**. Trailer `X-Agent: agy/infra-7023-leftover-apis`. Do **not** merge or arm auto-merge.\n\n## Non-goals\n- Do not edit `scripts/api/dashboard_router.py` (sibling #7024).\n- Do not change private teacher/Hramatka services (other repo).\n- Do not add `/api/readyz`.\n- Do not delete `hramatka_router.py` if tests or a documented import still need the module — unmount from the public app.\n- Do not edit the primary checkout.\n\n## Verify\n```bash\n.venv/bin/ruff check scripts/api/main.py scripts/api/rag_router.py\n.venv/bin/python -m pytest -q tests/test_api_endpoints.py tests/test_api_resilience.py\n# plus any new/targeted mount tests you add\n```\nQuote TestClient status for `/api/hramatka/readyz`, `/api/sources/stats`, `/api/rag/stats`, `/api/readyz`.\n\n## Layout\n`.worktrees/dispatch/agy/infra-7023-leftover-apis/`\nMode: workspace-write.\n","timestamp":"2026-08-18T19:22:26.497620Z","type":"user"}
