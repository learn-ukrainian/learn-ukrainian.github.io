[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/kimi/hramatka-hygiene-pr3
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Task: Hramatka hygiene PR-3 — safe settle reaper (post_task_reap)

## Authority
Consult: conversation_7b6241377cd44c7ea5265da5c85efb5c. Reap is **high blast radius** — guards are the merge bar.

## Outcome
```
.venv/bin/python -m scripts.fleet.post_task_reap --task-id <id> --dry-run
.venv/bin/python -m scripts.fleet.post_task_reap --task-id <id> --apply
```

### Hard guards (must mutation-test)
1. Never reap if `batch_state/tasks/<id>.json` status is spawning/running
2. Never reap if worktree has uncommitted changes (`git status --porcelain` non-empty)
3. Never reap by path-name prefix alone (`runtime-review-*`) without matching task binding + process gone
4. Unknown ownership → report, do **not** reap
5. Default mode dry-run; `--apply` required to delete

### Scope
- Dispatch worktrees under `.worktrees/dispatch/` bound to task_id
- Optional: finished ACP runtime-review paths only when process gone AND task terminal
- Document call site in drive-epic settle §5 (one line: run post_task_reap after terminal status)

### Tests
Fixtures for: running skip, dirty skip, clean terminal reap, ambiguous retain.
Mutation: disable liveness check → test fails.

### Non-goals
No stream fence (PR-2), no hygiene_check (PR-4), no name-only mass delete of /tmp.

### Delivery
Worktree, PR, no merge. X-Agent trailer.