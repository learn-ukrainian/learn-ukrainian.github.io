[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/agy/homonym-paronym-promote
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# REQUIRED DELIVERABLE — do not claim no_change

## Homonym is NOT done
- Issue #6138 OPEN
- data/lexicon/homonym_pairs.yaml does NOT exist on main
- No practice-homonym mode emit path is complete

## Required
1. Create data/lexicon/homonym_pairs.yaml from miyklas relation_candidates where relation=homonym AND review_status=approved (75 rows). Schema like paronym_pairs: slugA/slugB, distinction_gloss_uk, citations, frames (min 1–2 sense-disambiguation frames each). High UA quality.
2. Wire generate_practice_deck.py + publish.py + check_static_practice_assets + srs.ts PRACTICE_MODES for **homonym** (mirror antonym).
3. Promote remaining approved miyklas **paronym** not already in paronym_pairs.yaml (currently 55; miyklas has 64 approved).
4. Tests proving load + empty fail-closed.
5. Commit push PR linking #6138 #6132 #4387.

FORBIDDEN: outcome no_change; inventing pairs; skipping frames.

X-Agent: agy/homonym-paronym-promote