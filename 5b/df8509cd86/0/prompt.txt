[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/agy/ci-timings-7174
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Task: implement #7174 — `scripts/ci/ci_timings.py` (one shippable unit, one PR)

Read GitHub issue #7174 (`gh issue view 7174`) — it defines the outcome, inputs/outputs, constraints, and acceptance. Also read `.agent/ci-waveb-baseline-2026-08-23.md` if present in your checkout (it is gitignored; if absent, the baseline merge_group rows are: pytest shards 1-4 avg 7.1–7.5 min (max 8.3), Contracts 5.2 (5.3), Pytest plan 3.3 (3.4), Frontend build+vitest 2.2 (8.5), Frontend E2E 1.5 (5.3) over 10 runs on 2026-08-22/23).

Evidence preamble (#M-4): every claim in your final report quotes the deterministic tool output that backs it (the CLI's own table for the reproduction, `pytest -q` pass line, `ruff check` line).

Steps:
1. Work in this dispatch worktree (branch from origin/main).
2. Implement the CLI exactly as scoped in #7174: read-only, `gh api`/`GITHUB_TOKEN` read scopes only, bounded pagination, explicit `timeout=` on every subprocess call (`tests/test_subprocess_timeout_guard.py` enforces this repo-wide — run it), nearest-rank p95, stable-ordered JSON.
3. Tests on a recorded fixture (no network) per the issue; run `.venv/bin/pytest` on your test module + `tests/test_subprocess_timeout_guard.py`; `.venv/bin/ruff check` on changed files.
4. Reproduce the baseline merge_group rows with the real CLI (network allowed for this one manual run) and quote both tables in the PR body.
5. One row in `docs/SCRIPTS.md`.
6. Conventional commit with the `X-Agent` trailer; push; PR titled `feat(ci): ci_timings — per-event/per-job CI duration and queue-wait measurement (#7174)`; body: Summary · quoted acceptance evidence · `Refs #7141` · `Closes #7174`.
7. Do NOT merge, do NOT arm auto-merge, do NOT enqueue. Final report: PR number + head SHA + quoted evidence.