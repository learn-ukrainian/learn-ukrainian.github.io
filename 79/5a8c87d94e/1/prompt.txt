# Task: finish and validate #6183 typed Entire-context resolvers

## Identity and classification

- unique task name: `6183-entire-typed-resolvers-kimi-finish`
- functional role: implementation recovery, adversarial simplification, tests
- task family: `infra-harness`
- track: `infra`
- accountable root: Codex Desktop task `019fb7a7-5e56-7760-8f53-0980ec7f0d0b`
- worker: native Kimi Code K3

## Recovery input

Continue the existing isolated worktree and branch:

- worktree: `/Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/glm/6183-entire-typed-resolvers-glm`
- branch: `glm/6183-entire-typed-resolvers-glm`
- base: `8a8b502776e9965e62d7b30f884cff333db96465`
- predecessor task: `6183-entire-typed-resolvers-glm`
- predecessor status: `needs_finalize` after the GLM/OpenCode 600-second
  initial-output timeout; process is dead and no commit exists

The predecessor left compiling, Ruff-clean partial edits in
`scripts/entire_context/{resolvers.py,recall.py,cli.py}`. Preserve useful work,
but treat it as unreviewed. Inspect every line before adopting it. Do not start
a second branch/worktree and do not revert unrelated files.

## Outcome and approved architecture

Complete the remaining five strict local-only resolvers required by #6183:
`github_issue`, `github_pr`, `fleet_receipt`, `formal_review`, and
`monitor_run`. They must use existing local canonical caches/stores only,
read-only, offline, body-free, deterministic, optional, and non-load-bearing.

- GitHub issue: fresh `issue_stream_audit.json`, exact open issue plus unique
  stream/epic membership; no body/title.
- GitHub PR: completed formal-review job + durable GitHub publication + exact
  local head commit.
- Formal review: completed job + hash-verified, strictly parsed sealed verdict;
  expose only body-free receipt fields, never artifact IDs/payload/findings.
- Fleet: exact terminal `requests` row, never invocation JSON/message bodies.
- Monitor: exact terminal Agent Process Monitor lease, never PID/process time.
- SQLite URI `mode=ro` only; no migrations, WAL mutation, pruning, service call,
  network, Entire CLI, GitHub mutation, or public Entire refs.

One design correction to scrutinize: a refreshed issue cache must not make an
otherwise unchanged issue locator permanently unusable. Cache freshness must
be verified at admission and recall, but volatile cache generation time should
not enter the canonical digest unless you can prove deterministic rebootstrap
is the intended contract. Prefer a stable digest over canonical membership,
with verification time carried as evidence rather than identity.

Simplify the predecessor's ~900-line resolver expansion if equivalent strict
behavior can be expressed more clearly. Do not weaken fail-closed validation.

## Official Entire product workflow mapping

The user supplied Entire's official Skills, Search Past Agent Work,
Investigate Why Code Exists, and Cross-Agent Review/Recap guides. Finish the
canonical skill and public-pilot documentation mapping:

- agents use local body-free `search`, `explain-change`, and `handoff` for the
  product-style prompts;
- native `entire blame --json` is allowed for local attribution;
- prompt-bearing `entire why`, cloud `entire search`, generated recap,
  investigate findings, and Entire review are optional/manual and cannot enter
  the canonical capsule automatically;
- Entire review is supplemental and never replaces Fleet formal review;
- use `.venv/bin/python` in project commands.

## Owned paths and authority

Write only:

- `scripts/entire_context/resolvers.py`
- `scripts/entire_context/cli.py`
- `scripts/entire_context/recall.py`
- `scripts/entire_context/model.py` only for an exact typed identity exemption;
  never weaken generic guards
- `tests/test_entire_context_recall.py`
- `agents_extensions/shared/skills/entire-context/SKILL.md`
- `docs/entire/PUBLIC-SOURCE-PILOT.md`

Do not edit generated agent mirrors. No GitHub mutation, PR, formal review,
merge, deployment, or cleanup authority. Do not modify/remove the untracked
`.venv` worktree convenience link.

## Required tests and adversarial evidence

- Exact typed bootstrap CLI and root overrides for all five sources.
- Existing git/ACP/rollover output remains compatible.
- Stable/idempotent locators and parity across consumer labels.
- Fail closed for stale/malformed/missing cache, absent publication/Git object,
  nonterminal/unknown Fleet and Monitor rows, mismatched sealed blob hash/job
  binding, SQLite outage, digest drift, and caller-asserted false joins.
- Source/cache/database snapshots remain byte-identical after read paths.
- Stub `entire`, sockets/network, and mutating store helpers to prove no calls.
- Output scan rejects body, prompt, response, transcript, summary, raw capture,
  artifact IDs, PID/process identifiers, secrets, and `refs/entire`.

Run at minimum:

```bash
.venv/bin/pytest -q tests/test_entire_context.py tests/test_entire_context_recall.py tests/test_entire_context_live.py tests/test_entire_native_onboarding.py
.venv/bin/ruff check scripts/entire_context tests/test_entire_context_recall.py
.venv/bin/python scripts/lint/lint_agent_skills.py
git diff --check
.venv/bin/python scripts/audit/lint_opsec_leaks.py
```

Commit all accepted changes with trailer:

`X-Agent: kimi/6183-entire-typed-resolvers-kimi-finish`

## Terminal condition and return contract

Stop only with a committed, tested bounded implementation or a precise blocker.
Return branch/commit, changed files, tests/counts, design corrections made,
remaining risks, and final `git status --short --branch`. Do not claim formal
review or issue closure.