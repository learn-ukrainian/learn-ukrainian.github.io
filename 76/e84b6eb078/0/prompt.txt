[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/6733-pointer-auto-deploy
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Task: allowlist practice-deck pointer for Pages auto-deploy (#6733)

**Driver:** grok-atlas · mechanical CI
**X-Agent:** `cursor/6733-pointer-auto-deploy`

## CAPACITY_CARD
pick=cursor Auto (not Gemini pin)
avoid=agy-for-CI
source=operator-cursor-auto + #6733

## Context
#6731 bumped `site/src/data/lexicon-practice-deck.pointer.json` plus factory scripts. Auto-deploy skipped (`unknown_path` + `content_drift` on `site/src/data/`). Last successful Pages deploy stayed at #6730, wedging later pushes. Driver already `workflow_dispatch`'d `deploy-pages.yml` on `main` (run 31720792891) to unstick **now**.

## Frozen outcome
- `decide_auto_deploy` treats **only** `site/src/data/lexicon-practice-deck.pointer.json` (and the atlas catalog pointer if the same pattern exists) as site-code, **not** `content_drift`. All other `site/src/data/` stays drift. `curriculum/` and `site/src/content/` stay drift. Any `scripts/` path still `unknown_path`.
- Tests in `tests/test_deploy_pages_workflow.py` cover: pointer-only → deploy; pointer+scripts → unknown_path; lexicon-manifest.json still content_drift.
- Do **not** auto-deploy all of `site/src/data/`.
- PR, no merge. Leaves #6733 #4387 open (or close #6733 only if you also confirm the force-deploy run succeeded — quote the run URL).

## #M-4
Quote ruff + pytest of the eligibility tests. Quote the force-deploy run conclusion if you check it.