[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/grok/work-control-plane-public-p1
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

You are the Grok implementation owner for public PR #6842 in the existing dispatch worktree.

Exact head: `58b091487c76f47ebcc378f770f2de13880a156a`.

The exact-head review found one remaining correctness issue:

- `fetch_delegate_tasks()` discards the trusted production scoped loader’s authoritative public `total` and recomputes it from the capped 500-row page. With 501 public tasks it emits `total: 500`, `truncated: false`, silently hiding one public task.

Fix the count/truncation contract:
- on the fixed trusted production path (`loader is None`, repository-scoped upstream), preserve the upstream authoritative scoped public total and set `truncated = total > admitted_page_count`;
- on injected/untrusted loaders, continue deriving total/count/truncation only from rows that pass exact public claim admission, never trusting a supplied total;
- apply the equivalent rule to active delegate tasks if that path has a bounded page or total contract; do not invent truncation where the upstream active path is complete;
- keep repository fields internal/redacted and keep the prior trust fix intact.

Add regressions for 501 public production tasks → 500 rows, total 501, truncated true; <=500 → exact total, false; injected loader with dishonest total/private rows → recomputed admitted total, no private influence. Verify schema/API response semantics and docs if needed.

Run delegate API, complete Work/API/privacy/dashboard, fleet observer/dashboard, Monitor contracts, Ruff, and `git diff --check`. Schema digest remains `89fb9c1eec41baaa00a328d456340111163c1e3ab899cd7baa15e284fff65bde`.

Commit with trailer `X-Agent: grok/work-control-plane-public-p1-fix12`, push if permitted, do not merge or change PR state. Return exact verification and residual risk.