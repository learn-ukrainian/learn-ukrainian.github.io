[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/agy/claudex-repo-python-7210
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Task: #7210 — Claudex relaunch gate must survive venv-less linked worktrees (one PR)

Read `gh issue view 7210 -R learn-ukrainian/learn-ukrainian.github.io` first (if `gh` is unavailable in your
worktree, the essentials are below).

## Problem (from the issue)
`scripts/orchestration/claudex_supervisor.py` resolves its relaunch interpreter as
`PROJECT_ROOT / '.venv/bin/python'`. In a linked worktree without a private `.venv` (every sparse/full dispatch
worktree and any detached verify checkout) the relaunch gate fails with `FileNotFoundError`, failing
`tests/orchestration/test_claudex_supervisor.py` ×3 and the supervised `test_thread_handoff.py` ×3. Reproducible on
plain origin/main:

```
git worktree add --detach /tmp/x origin/main && cd /tmp/x && <primary>/.venv/bin/pytest -q tests/orchestration/test_claudex_supervisor.py
```

## Outcome
The relaunch gate works from venv-less linked worktrees and detached checkouts AND preserves the launcher contract —
when the supervisor relaunches from a worktree, it must still end up on the interpreter the launcher intends (the
primary repo's venv when that is the right one), never silently on some other Python. The issue names a candidate
(`sys.executable`); the peer session that filed it endorses "sys.executable with a fallback preference for the primary
venv when it exists". Adopt, improve, or argue for better — you own the design; state your reasoning in the PR body.

## Hard boundaries
- Change `scripts/orchestration/claudex_supervisor.py` (+ its tests) ONLY. Do NOT edit `session_streams`,
  `thread_handoff.py`, SessionStart hooks, or any session-handling surface — those are another lane's files. Running
  their tests is fine; editing them is not.
- Add a test that runs the supervisor from a venv-less worktree (the repro above, as a test). Mutation-check it:
  revert your fix locally → the new test must fail; say so with output in the PR body.

## Acceptance (quote raw output; PRIMARY interpreter — worktrees have no .venv)
1. `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/pytest -q tests/orchestration/test_claudex_supervisor.py tests/orchestration/test_thread_handoff.py` green from YOUR worktree (which has no .venv — that IS the repro).
2. Same suites green from the primary checkout; ruff clean on changed files.
3. One conventional commit (`fix(orchestration): resolve Claudex relaunch interpreter in venv-less worktrees (#7210)`,
   X-Agent trailer). NO push, NO auto-merge, NO amend after reporting. Report HEAD SHA + evidence + ready PR body in
   your final response (you have no gh auth — RETURN the text; the driver posts and will flag the monitor-epic peer
   for awareness).