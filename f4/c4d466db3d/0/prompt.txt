[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/agy/claudex-7210-delta
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

Delta on YOUR OWN PR #7233 (branch agy/claudex-repo-python-7210, head f10dbebd35 — you are checked out on it) fixing the cross-family review findings. Apply ALL THREE, nothing else: (1) MEDIUM scripts/orchestration/claudex_supervisor.py:53-56 — tier 3 must return Path(sys.executable) UNRESOLVED: .resolve() follows the venv symlink to the base pyenv interpreter and silently loses pyvenv.cfg/site-packages; ALSO replace the silent 'except Exception: pass' around the tier-2 canonical_state_root call (:50-51) with a loud path: keep falling through, but emit one warning line to stderr naming the exception class so a wrong-interpreter fallback is never silent. (2) tests/orchestration/test_claudex_supervisor.py:544 — the tier-2 assertion only discriminates because .venv/bin/python happens to be a symlink; restructure the priorities test so tier 2 vs tier 3 are distinguishable regardless (e.g. monkeypatch sys.executable to a distinct sentinel path inside that test) — it must FAIL if tier 2 regresses even when venv python is a real file. (3) tests/orchestration/test_claudex_supervisor.py:552-582 — the E2E venv-less-worktree test must assert the CHILD landed on the primary venv python: record sys.executable in the child log record (the child record helper at :172-180) and assert equality with the primary venv interpreter path. Acceptance (quote raw output; primary interpreter /Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/pytest, run from YOUR worktree root which is venv-less): tests/orchestration/test_claudex_supervisor.py all green; mutation-check BOTH new assertions (regress tier 2 by making canonical_state_root raise -> priorities test AND E2E test must fail; restore); ruff clean. ONE commit on the branch (fix(orchestration): un-resolve tier-3 interpreter, loud tier-2 fallback, assert child interpreter (#7210), X-Agent trailer). NO push, NO amend after reporting. Report HEAD SHA + evidence; driver pushes.