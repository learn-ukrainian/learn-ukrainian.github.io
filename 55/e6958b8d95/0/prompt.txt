[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/kimi/reservation-supersede-fix
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

Fix the review-reservation stale-envelope deadlock (issue #6415, hit 4+ times today). Evidence: scripts/fleet_comms/routing_reservations.py:354 — reserve_selection raises authority_key_semantic_conflict whenever the LATEST reservation row for an authority_key has a different semantic_sha256, EVEN IF that row is in a terminal status (failed/expired/cancelled). Consequence: after ANY failed review attempt, no different reviewer envelope can ever reserve the same head; orchestrators must force-push head refreshes as a treadmill workaround. Design intent to preserve: an ACTIVE reservation with a different envelope must still conflict (no double-selection, no quota herd); idempotent replay of the SAME envelope must still work. THE FIX: when the latest row is TERMINAL and the new request's semantic_sha256 differs, allow a fresh reservation (new attempt number), recording a 'superseded_terminal_attempt' decision event referencing the prior reservation_id — never silently discard history. Steps: 1) Read reserve_selection + the settle/expiry lifecycle fully first. 2) Reproduce in a failing unit test: reserve envelope A → settle failed → reserve envelope B on same authority_key → currently raises; after fix succeeds with attempt+1 and the audit event. 3) Keep tests proving: active-row different-envelope still conflicts; same-envelope idempotent replay unchanged; substitution path unchanged. Mutation-check: reverting the fix must fail your new test and only it. 4) pytest on the touched test files + ruff. 5) X-Agent trailer, push, PR referencing #6415. No auto-merge.