[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/phase3-6375-school-parent-context-codeql-r2
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

You own a minimal exact-head follow-up on draft PR #6788. You are not alone: do not touch unrelated files, do not merge, do not weaken security. Verify starting head 1633b763116bc763644aaf6c169425456ac1aa6f. GitHub CodeQL check 94662153127 reports four HIGH alerts, all Overly permissive file permissions: script line 244 because caller-controlled mode reaches chmod and tests lines 271, 301, 331 because explicit 0644 chmod. Fix the root cause without suppressions, linter/config changes, or dismissing alerts. Preserve private outputs at fixed literal 0600 and dirs 0700. The public receipt must safely accept normal Git tracked mode 0644 on an existing checkout, but code need not make new files world-readable: prefer a fixed private atomic writer for creation and accept only explicit safe existing public modes, with tests that do not call chmod 0644. Keep changed-output refusal and symlink guards. Also address the earlier review concern before new cross-family review: validate_receipt compares pinned values but may not re-open current public bindings; determine and implement the smallest fail-closed current-input rebind so stale/tampered current NEGREC, eval-context, or source-universe public receipts cannot pass standalone validation. Keep private source/partition/tarball verification in verify/production where paths exist. Add mutation tests, no private text output. Because implementation hash changes, deterministically regenerate the text-free public receipt and a new immutable custody successor strategy without overwriting either existing custody receipt or the 3526-row private JSONL; if the existing successor filename is occupied, create a versioned second successor and update tests. Preserve exact private hash 8bb7bbcfe6c1eb5082200543274d37a9edfa602a66efe6dfd5187a92ad1925ab and all denominators/gates. Use apply_patch. Run the new adapter Drive production reproduction, focused/compat tests excluding only the separately assigned pre-existing negative-recovery macOS production failure, Ruff/diff-check, then commit with X-Agent: cursor/6375 and push. Report exact new head/hashes/custody locator/tests. Do not claim Phase 3 complete; Phase4 remains blocked.