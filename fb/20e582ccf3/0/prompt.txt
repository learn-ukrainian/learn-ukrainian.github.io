[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/monitor-7188-review-fixes
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Task: PR #7255 review fixes — per-host project state (#7188, parent #7177). Same authoring lane (cursor).

Evidence rule (#M-4): back every claim with a command you ran (test output, commit SHA). You are attached to the PR branch
`cursor/monitor-7188-project-state` (head 2d6b7e0a8d). Do not rebase or force-push; commit on top.

## Review of record (Gemini seat) — VERDICT: REQUEST_CHANGES. Findings verbatim:
## Findings

- [scripts/api/project_state_collect.py:255](scripts/api/project_state_collect.py#L255) — **blocker** — `_default_service_state(name: str, definition: ServiceDefinition)` ([project_state_collect.py:219](scripts/api/project_state_collect.py#L219)) requires two positional arguments, but `collect_service_row` calls `state_fn(definition.name)` with only one argument when `service_state` is default (`None`). This causes every live in-process evaluation on the API host to fail with `TypeError: _default_service_state() missing 1 required positional argument: 'definition'`, yielding HTTP 500 when `GET /api/fleet/projects/v1` evaluates the self host. Unit tests missed this because `test_project_state.py` monkeypatched `_live_local_document` with a mock lambda and `test_project_state_collect.py` never invoked `collect_local_document()` or `collect_service_row()` with defaults.
- [scripts/orchestration/run_project_state_reporter.sh:5](scripts/orchestration/run_project_state_reporter.sh#L5) — **major** — `REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../../.." && pwd)"` traverses 3 directory levels instead of 2 (`../..`). From `scripts/orchestration/`, this resolves to the parent directory of the repository root. Running the script (or executing it via the systemd unit or LaunchAgent) fails with `python: can't open file '<parent_dir>/scripts/api/project_state_local.py': [Errno 2] No such file or directory`.
- [scripts/api/project_state_local.py:15](scripts/api/project_state_local.py#L15) — **minor** — Executing `.venv/bin/python scripts/api/project_state_local.py report` (as documented in `docs/MONITOR-API.md:259` and `docs/runbooks/project-state-reporter.md:13`) fails with `ModuleNotFoundError: No module named 'scripts.api'` because `sys.path[0]` is initialized to `scripts/api/` instead of the project root. Adding `sys.path.insert(0, str(Path(__file__).resolve().parents[2]))` or running via `-m scripts.api.project_state_local` is necessary for direct CLI execution.
- [scripts/api/project_state_local.py:44](scripts/api/project_state_local.py#L44) / [docs/runbooks/project-state-reporter.md:19](docs/runbooks/project-state-reporter.md#L19) — **minor** — `--dry-run` is registered only on the root parser before `subparsers`. Running the exact command from the runbook (`.venv/bin/python scripts/api/project_state_local.py collect --dry-run`) fails with `error: unrecognized arguments: --dry-run` because the argument is placed after the `collect` sub-command.

---

## Commands Run & Result Lines

## Outcome
`GET /api/fleet/projects/v1` returns 200 with a real self-report on the API host with NO monkeypatching (real collector path against
a fixture repo/services); the timer runner resolves the repo root; the documented CLI invocations in `docs/MONITOR-API.md` and
`docs/runbooks/project-state-reporter.md` work exactly as written (`--dry-run` included). Nothing else in the PR changes meaning.

## Required tests (mutation-checked)
- An UNMOCKED end-to-end test: `TestClient` GET on the self host with the real `collect_local_document` (fixture git repo with an
  `origin/main` ref, no network), asserting 200 and the document shape — reverting the `_default_service_state` fix must fail it.
- A test that executes `scripts/orchestration/run_project_state_reporter.sh` (or asserts its resolved `REPO_ROOT`) from a temp copy of
  the tree layout — the `../../..` bug must fail it.
- A test that runs the documented CLI commands via `subprocess` (`collect --dry-run`, and direct-file execution if you keep documenting
  it) and asserts exit 0.

## Steps
1. `git log -1` must show 2d6b7e0a8d. Reproduce the blocker:
   `LU_MONITOR_HOST_ID=mac-operator .venv/bin/python -c "from fastapi.testclient import TestClient; from scripts.api.main import app; c=TestClient(app, raise_server_exceptions=False); r=c.get('/api/fleet/projects/v1?host_id=mac-operator'); print(r.status_code)"` → 500 today.
2. Fix all four findings; add the tests above.
3. Run `.venv/bin/pytest -q tests/api` (full) + `.venv/bin/ruff check` on changed files + `bash -n scripts/orchestration/run_project_state_reporter.sh`.
4. Conventional commit (`fix(fleet): …`) with the `X-Agent` trailer; PUSH to `cursor/monitor-7188-project-state` (the previous run
   finished without pushing — confirm with `git status -sb` that the branch is not ahead of origin before you finish).
5. Report: commit SHA(s), the passing pytest line, and one sentence per finding on how it was fixed.