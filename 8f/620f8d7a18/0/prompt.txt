[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/agy/sources-7027-shadow-agy
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Dispatch brief — #7027 check_russian_shadow calques + fake lemmas

CAPACITY_CARD: pick=gemini status=cool rem=99% avoid=codex,cursor source=routing-budget
ROUTING_CARD_V1:
- tier: practical
- model_x_harness: gemini
- why_this_tier: leakage gate; Gemini idle; not UK content writing
- owned_paths: scripts/verification/check_ru_morph.py; tests that cover is_russian_pattern
- alternatives_considered: agy (#7028); claude (infra)
- parallel_free_seats: gemini #7026; agy #7028

Initiator: grok-infra. AGENT_NO_MERGE=1.

## Outcome
Documented Russianisms/calques are flagged. A VESUM hit does not disable the heuristic for known shadows. Failed morph analysis does not invent `russian_lemma`.

## Live fail table (issue)
| word | required |
|---|---|
| слідуючий | flag; no fake RU lemma |
| учбовий | flag; no garbage pymorphy3 lemma |
| получити, здача | heuristic runs (do not VESUM-short-circuit to false/0.0) |
| врач | stay true |
| книга | stay clean negative |

`ukrainian_alternative` may stay null this PR if no source-backed alternative exists — do not invent.

Root: `scripts/verification/check_ru_morph.py` (`is_russian_pattern`). MCP handler only calls that. Do not edit `.mcp/servers/sources/server.py` (#7026 owns it).

Tests with the table above. PR Refs #7027. Trailer `X-Agent: gemini/sources-7027-shadow`. Do not merge.

## Verify
```bash
.venv/bin/ruff check scripts/verification/check_ru_morph.py
.venv/bin/python -m pytest -q -k 'russian_shadow or ru_morph or is_russian_pattern'
```
Quote node ids for the five words.