[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/agy/6575-trufflehog-lob
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Task: Close #6575 — TruffleHog Lob false-positives

Closes #6575. Stream #4707.

## Evidence already proven
- Issue root cause: Lob detector "verifies" well-shaped `test_<string>` including long pytest names.
- Reproduced again 2026-08-12 on capacity PR: `test_check_budget_hard_sub_on_hot_status` → verified Lob; rename + squash → 0 findings (#6655).

## Required permanent fix (issue option 1 + 2)
1. `.github/workflows/ci.yml` TruffleHog step: add `--exclude-detectors=Lob` to `extra_args` (repo has no Lob credentials). Verify flag works with pinned action v3.96.0 / trufflehog 3.x.
2. Rename the contracts/CI check step label from "gitleaks" to "TruffleHog" if the display name still says gitleaks (accuracy).
3. One short note in an existing runbook (ci-gate.md or worktree-cleanup-adjacent CI doc) with local repro recipe from the issue body.
4. Optional: short comment in `.trufflehogignore` header pointing at #6575.

## Out of scope
- Disabling all secret scanning
- History rewrite of old commits

## Validation
- Local: `trufflehog --help` shows exclude-detectors; document in PR
- actionlint if workflow touched
- Open PR with Closes #6575, X-Agent trailer

CAPACITY_CARD: pick=agy avoid=codex(deficit) cursor=busy