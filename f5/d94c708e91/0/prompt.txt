[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/infra-7020-merge-group-2
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# infra-7020-merge-group-2

Work only on existing branch `codex/agent-gh-identity-aj`. Do not merge. Do not open a second PR.

## ROUTING_CARD_V1
- tier: practical
- model_x_harness: cursor / Auto
- why_this_tier: mechanical merge-group pytest; Cursor now cool (79%, will_last=true)
- advisor_packet: n/a
- owned_paths: tests/test_agent_runtime.py (and fakes only)
- acceptance_cmd: `.venv/bin/python -m pytest tests/test_agent_runtime.py -q --tb=short`
- alternatives_considered: claude (language leftover 673), agy (sources), codex (near_cap refuse)

## CAPACITY_CARD
pick=cursor status=cool rem=79% will_last=true free=cursor,claude,agy,kimi,gemini avoid=codex(near_cap) source=routing-budget

## Bug
PR #7020 head `bae0130d43cf` was **kicked from merge queue again**.
Merge-group CI **FAILED**: https://github.com/learn-ukrainian/learn-ukrainian.github.io/actions/runs/32203835283
(2026-08-19T01:08:10Z, after #7037 merged). Branch CI is green. Prior FakeProc CM + `_agent_only_popen` was not enough for this merge-group SHA.

## Work
1. Pull the failed job log. Quote the first traceback and the FAILED test names.
2. Fix root cause in test fakes/adapters. Do **not** weaken GH_TOKEN strip, identity, or merge-shim policy.
3. Full `tests/test_agent_runtime.py` green. No skips.
4. Commit + push to `codex/agent-gh-identity-aj`.
   - `fix(harness): merge-group agent_runtime fakes after #7037 (#7020)`
   - `X-Agent: cursor/infra-7020-merge-group-2`
   - Refs #7020 — do not close.

## Non-goals
- New PR, merge, workflow policy, coverage-floor changes