[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/6419-main-telemetry-test-fail
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# #6419 — Three tests fail on main (telemetry footer + linear-pipeline)

## ROUTING_CARD_V1
- tier: practical
- model_x_harness: cursor (heap/practical code fix) — free seat while codex runs #6522/#6479
- why_this_tier: reproduce-fix-test for pre-existing main failures; no architecture
- advisor_packet: n/a
- owned_paths:
  - tests/test_api_telemetry_footer.py
  - tests/test_linear_pipeline_telemetry.py
  - scripts/ paths those tests exercise (minimal production fix only if tests are correct and code is wrong)
- acceptance_cmd: `.venv/bin/python -m pytest tests/test_api_telemetry_footer.py tests/test_linear_pipeline_telemetry.py -q`
- alternatives_considered:
  1. codex/luna — busy with #6522/#6479
  2. agy — also fine for bounded test/code fix
- parallel_free_seats: agy, claude (avoid if paths collide)

## Operator goal
Found while verifying PR #6418: these fail on clean main (not introduced by #6418):

1. `tests/test_api_telemetry_footer.py::test_current_context_telemetry_skips_newest_transcript_without_usage`
2. `tests/test_linear_pipeline_telemetry.py::test_writer_rule_fired_event_emitted_on_known_failure_class`
3. `tests/test_linear_pipeline_telemetry.py::test_python_qg_surface_policy_scans_yaml_sidecars`

Related observation: Monitor API `/api/state/manifest` `_telemetry.ctx: null` (#5769 may be sibling).

## Required
1. Reproduce on current `origin/main` in the worktree; quote full failure output.
2. Root-cause each failure (fixture drift vs product regression vs env assumption).
3. Fix the **root cause** — do not skip tests, weaken assertions, or gut coverage.
4. If a test is wrong relative to intentional product behavior, fix the test with evidence of the intended contract in source.
5. Leave main green for these three + no new failures in the two files.

## Non-goals
- Full CI redesign
- Fleet-comms plane changes
- Broad telemetry redesign beyond what these three need

## Process
- Worktree only; `X-Agent: cursor/6419-main-telemetry-test-fail`
- ruff + the acceptance pytest + `git diff --check`
- Push + PR `Fixes #6419`. No auto-merge. No self-seal CF.
- Return PR URL, head SHA, root-cause per test, evidence.