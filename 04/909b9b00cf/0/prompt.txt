[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/delegate-sweep-7203-delta4
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Delta 4: cursor/delegate-sweep-7203 (PR #7206) — CodeQL high: overly permissive marker file mode (same branch, on top)

CodeQL on this PR: `py/overly-permissive-file` [high] at `scripts/delegate.py:745` — the no-clobber marker
create from delta 2 uses `os.open(marker, os.O_WRONLY | os.O_CREAT | os.O_EXCL, 0o644)`, i.e. world-readable.
The `.delegate-task-id` marker is per-user runtime state; nothing else needs to read it.

## Do
- Use `0o600` for the marker (and for any other file this PR creates with an explicit mode — grep the diff for
  `0o6`/`0o7` and `os.open(`). Do not weaken the `O_EXCL` no-clobber semantics.
- If a test asserts the mode or reads the marker via another user, adjust it; add an assertion that the created
  marker's mode is `0o600` (`stat.S_IMODE`).

## Acceptance (quote raw output; PRIMARY interpreter)
- `env -u LU_ALLOW_NOTEBOOK_DISPATCH /Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/pytest -q
  tests/test_delegate_runtime_tmp_sweep.py tests/test_delegate_readonly_guard.py` green; ruff clean.
- Grep of the PR diff shows no remaining `0o644`/`0o664`/`0o755` for files created by this PR.
- One conventional commit on top (`fix(delegate): create lease markers owner-only (#7203)`, X-Agent trailer).
  NO push, NO amend, NO rebase, NO auto-merge. Report new HEAD SHA + evidence.