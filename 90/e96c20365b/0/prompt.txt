[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/agy/atlas-6529-fix
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Fix CHANGES_REQUESTED on YOUR PR #6529 (shard 03) — row-TEXT identifier scrub

Codex found the identifier in EMITTED ROW CONTENT: one `lemma` and one `approved_gloss` value
in batch 03 match the identifier patterns. The generator copies non-key fields byte-identically,
so this is a GENERATOR POLICY GAP — issue #5995's AC always required "no scrubbed personal
identifier remains in … row text".

Required (never print the identifier anywhere while doing this):
1. Extend the generator's policy layer to scrub identifier matches in row-text fields (lemma,
   glosses, notes — all text fields), sourcing patterns from the opsec linter's definitions.
   PREFER value-level scrub to `[REDACTED]` placeholders over row removal (preserves row count,
   decision structure, and the 11,724 invariant); if a scrubbed lemma breaks validation
   semantics, STOP and report the conflict instead of removing rows silently.
2. Regenerate batch 03; update the stale test count 53→54 (codex item 2).
3. **Re-scan MERGED batches 01–02 with the new policy**: if either contains row-text matches,
   say so with counts — the driver will queue their re-emission as a follow-up PR; do NOT
   amend merged data in this PR.
4. Rerun: opsec lint, validator (54 files / 64,975 rows), concatenation invariant, pytest, ruff.
Push SAME branch. NO auto-merge. Report: commit SHA + scrubbed-field count + 01/02 scan result.