[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/phase3-6375-school-negrec-production-fix
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

You own a separate bounded Phase 3 follow-up for the already-merged school negative-recovery adapter. You are not alone in the codebase: do not revert others, do not touch PR #6788 files, do not merge. Start from current origin/main and read AGENTS.md/current rules. Independently reproduce this local failure on macOS: tests/test_open_model_phase3_school_context_negative_recovery.py::test_production_receipt_reproducible_against_drive_custody fails because tempfile.TemporaryDirectory returns a lexical /var path and the strict symlink guard rejects /var to /private/var. Preserve the guard; use a canonical symlink-free realpath temp root with secure 0700 cleanup. Adversarially audit the adjacent patterns that PR #6788 exposed: tracked public receipt mode must be safely idempotent at 0644, current/pinned implementation and schema bindings must fail closed, and changed existing receipts must never be silently overwritten. Fix root causes only in the negative-recovery adapter, its focused tests, and its text-free public receipt. Do not mutate private corpus inputs, the v2 denominators, schema, linters, generated status/audit/review artifacts, or Phase 3/Phase 4 gates. Regenerate the receipt deterministically against the exact existing custody tarball, never print private rows, and confirm the source tarball/hash is unchanged and already in Google Drive custody; no duplicate private data is needed. Use apply_patch for code/test changes. Use /Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python. Run the focused Drive production reproduction, mutation/immutability tests, related evaluation-context/v2 compatibility tests, Ruff, and diff-check. Inspect the exact diff, commit with X-Agent: cursor/6375, push, and open a DRAFT PR linked to #6375. Report exact head, receipt hashes, tests, CI state, and residuals. Phase 3 remains incomplete and Phase 4 blocked.