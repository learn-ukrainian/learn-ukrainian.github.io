[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/agy/timeout-7213-s7-delta
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

Delta on YOUR PR #7245 (branch agy/timeout-burndown-7213-s7, head 58b749a366 — checked out). CF verdict CHANGES REQUESTED, four findings. (1) MATERIAL — the slice-6 clobber class RECURRED: your edit at scripts/projects/open_model_data/gemma_hardware_probe.py:1426-1438 DELETED the adjacent 'write_atomic(ATTEMPT_LEDGER_PATH, ready_claim)' while restructuring the subprocess.run call — the local-ledger status upgrade in the crash window is silently lost. RESTORE the line exactly where it was; add a test that pins BOTH ledger writes in the launch path (the existing tests monkeypatch write_atomic without asserting the ATTEMPT_LEDGER_PATH call — make the pin real: assert the call list includes it). Then SELF-AUDIT every other hunk in your slice-7 commit for deleted adjacent statements (diff each touched function against main and list in the PR body: 'clobber self-audit: N hunks reviewed, 0 deletions beyond the intended sites'). (2) REQUIRED — the three 300s bridge caps (phase3_rule_author_runner.py:36, phase3_heldout_label_transport.py:45, phase3_source_production_transport.py:42) bound calls the module itself designs as unbounded (:906-908 passes --no-timeout to ai_agent_bridge): per the slice brief's own rule these are SKIP-with-reason sites — REVERT the bounds on exactly those sites, restore their allowlist entries, and record them as skipped ('unbounded-by-design supervised bridge call; module passes --no-timeout') — OR, if you find a real in-module budget justifying a cap, cite it and raise the value to it. Do not keep an invented 300. (3) MINOR — phase3_rule_author_runner.py:531 records TimeoutExpired as 'bridge_oserror:TimeoutExpired'; give the timeout its own accurate label without changing the item-level failure semantics. (4) OBSERVATION — add a one-line comment at phase3_school_parent_section_context.py:660 explaining the deliberately hardcoded receipt sha (verified frozen-receipt match). Acceptance (quote raw output; primary interpreter; from YOUR worktree root): tests/test_subprocess_timeout_guard.py tests/test_open_model_data_timeouts.py tests/test_gemma_hardware_probe.py green; the new ledger-pin test FAILS if the write_atomic line is removed again (mutation-check, quote); entry-point smoke of gemma_hardware_probe and the three phase3 bridge modules; ruff clean. ONE commit (X-Agent trailer). NO push, NO amend after reporting. Report HEAD SHA + the clobber self-audit line + evidence; driver re-runs independently.