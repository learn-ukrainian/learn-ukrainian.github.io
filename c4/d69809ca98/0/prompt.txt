[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/agy/infra-5214-grow-lexicon-checkpoint
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Dispatch brief — infra-5214-grow-lexicon-checkpoint

**Issue:** #5214 — read it fully. 11+ hour enrichment runs currently lose EVERYTHING on a
crash at hour 11 (single terminal write), and fetch caches are reverted in `finally`, so
restarts re-fetch from zero.

## Evidence preamble (#M-4, binding)
Every claim tool-backed: claim + deterministic command + quoted raw output.

## Shippable unit (one PR)
Make long `grow_lexicon_from_content` runs crash-safe and resumable: periodic durable
checkpointing of accumulated results, resume-from-checkpoint on restart (no re-fetching
already-fetched lemmas), and persistent (not reverted) fetch caching across runs. You own
the mechanism (checkpoint format/interval, cache persistence layer); defend it in the PR
with one rejected alternative. Add progress visibility so "silent for hours" is gone
(periodic stderr/log heartbeat is enough).

## Acceptance criteria (verify BEFORE opening the PR)
1. Kill-resume test: start a run on a small fixture, kill it mid-way, restart — completed
   work is preserved and already-fetched items are NOT re-fetched; quote both run logs.
2. Checkpoint write is atomic (a crash during checkpoint cannot corrupt the previous one) — test.
3. Mutation-check the key guard test (#M-16).
4. `.venv/bin/pytest` green on affected files; `.venv/bin/ruff check` green on changed files.
5. Default behavior for existing CLI invocations unchanged except the new durability.

## Out of scope
- Changing enrichment sources/rate limits; parallelizing the fetch loop.

## Process (numbered, binding)
1. Work only inside your dispatch worktree.
2. Implement + tests; run pytest + ruff; quote outputs.
3. Conventional commit(s) with trailer `X-Agent: <your-lane>`.
4. Push; open PR titled `feat(lexicon): ... (#5214)`. Do NOT enable auto-merge; do NOT merge.

---
ROUTING_CARD_V1
task_id: infra-5214-grow-lexicon-checkpoint
tier: practical
model_x_harness: (set at fire time from capacity_pick)
why_this_tier: durability redesign of a long-running tool — needs judgment on atomicity/resume
advisor_packet: fable (this brief)
owned_paths: scripts/lexicon/grow_lexicon_from_content.py + tests
acceptance_cmd: .venv/bin/pytest <affected test files> && .venv/bin/ruff check <changed files>
alternatives_considered: (filled at fire time)
parallel_free_seats: (filled at fire time)