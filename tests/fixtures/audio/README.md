`pronunciation.wav` is the 0.441-second Piper-generated clip for `або` from the
#4696 slice, with oracle input `або́`. It is mono PCM16 at 22,050 Hz (19,500 bytes).
See `docs/runbooks/pronunciation-audio.md` for engine, model and license provenance.
This fixture tests audio decoding/validation; it is not a pronunciation gold label.
