[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/agy/timeout-7213-s7-delta2
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

Delta 2 on PR #7245 (branch agy/timeout-burndown-7213-s7, head 3a68d28f21 — checked out). The merge-group python shard FAILED on this head: tests/test_open_model_phase3_v2_compatibility.py::test_tracked_matrix_is_complete_hash_bound_and_blocks_phase4 — 'CompatibilityError: v2 engine artifact hash drift' (+ test_matrix_rejects_v2_drift_semantic_reuse_and_missing_claim regex mismatch). Diagnosis to verify: the v2 compatibility matrix pins sha256 hashes of the phase3 engine scripts; your delta-1 commit modified gemma_hardware_probe.py + the three bridge modules + phase3_rule_author_runner.py without re-pinning the matrix, so the tracked hashes drifted. FIX PROPERLY: find the matrix's own documented re-pin procedure (the module/docs define how engine_bindings hashes are legitimately updated — follow it exactly; do NOT hand-edit hashes if a generator/receipt tool exists; if the procedure requires a receipt or justification field, write it: timeout-bounding slice #7213 + CF-driven ledger restore). If any file the matrix pins was changed in delta-1 unnecessarily, prefer minimizing drift. Acceptance (quote raw output; primary interpreter; from YOUR worktree root): /Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/pytest -q tests/test_open_model_phase3_v2_compatibility.py tests/test_subprocess_timeout_guard.py tests/test_open_model_data_timeouts.py tests/test_gemma_hardware_probe.py ALL green — and ALSO run the full 'grep -rl' sweep of test files referencing every module your two delta commits touched, and quote that list + results (this suite was missed by all three of us because targeted runs skipped it). Ruff clean. ONE commit (X-Agent trailer). NO push, NO amend after reporting. Report HEAD SHA + which re-pin procedure you followed + evidence; driver re-runs independently.