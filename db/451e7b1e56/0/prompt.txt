[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/infra-6890-next-hardening
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Task: resolve issue #6890 — /next hardening residuals

ROUTING_CARD_V1
- tier: practical
- model_x_harness: cursor:auto (native Cursor CLI)
- why_this_tier: six bounded, pre-reviewed residuals from a CF review — mechanical-with-judgment; served rules name Cursor Auto first pick for this class this month
- advisor_packet: n/a (tier=practical)
- owned_paths: scripts/work/**, frontend work-board assets, tests for both
- acceptance_cmd: .venv/bin/pytest (affected work-API tests) + ruff clean + CI green on PR
- alternatives_considered: kimi k3-256k (reserved for the harder #6888 debug), grok-4.6 (taken for #6852; breadth), codex (capped until ~2026-08-20)
- parallel_free_seats: agy (stall-prone, #6888 open), deepseek (rough, toolless-ask)

## Evidence rules (#M-4)
Every claim in your PR/report must be tool-backed: name the deterministic command and quote its raw output. No asserted numbers/paths/SHAs without tool output. Cite evidence per fix item.

## Steps
1. Read GitHub issue #6890 in full (it lists the six residuals with their context; refs #6880 #6889). Read the referenced CF review context on PR #6889 if needed.
2. Work ONLY in your dispatch worktree (created for you). Never touch the primary checkout.
3. Implement all six residual items at the root-cause layer. Where the issue offers a design choice (e.g. fail-open vs 503 for an unreadable registry), pick the fail-closed/honest-signal option and record the rationale in the PR body.
4. Add/extend tests for each behavior change; run the affected pytest files and ruff on every edited file.
5. One conventional commit (or a small logical series), push, open a PR titled against #6890 with a body listing each residual → its fix → its evidence.
6. Do NOT arm auto-merge and do NOT merge. The driver handles review + merge.