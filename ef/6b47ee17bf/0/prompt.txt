[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/kimi/infra-5375-5374-surface-normalization
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Dispatch brief — infra-5374-joiner-fix (review fix on PR #6832)

Your PR #6832 review verdict is REQUEST_CHANGES with ONE blocker (full text on the PR —
read via REST: `gh api repos/learn-ukrainian/learn-ukrainian.github.io/issues/6832/comments`):
U+02BC (ʼ) is missing from `_LEXICAL_TOKEN_PATTERN`'s apostrophe joiner class, so
U+02BC-carrying words split BEFORE `_APOSTROPHE_FOLD` applies — the #5374 false-MISSING
persists on tokenizer paths.

## Steps (in your existing worktree / branch kimi/infra-5375-5374-surface-normalization)
1. Add ʼ (U+02BC) to the tokenizer joiner class.
2. Regression test: a surface like `Мʼяки́й знак` (U+02BC + stress mark) through the
   TOKENIZER path resolves against ledger lemma `м'який знак`; mutation-check it (#M-16):
   revert the joiner change → test fails; quote the run.
3. `.venv/bin/python -m pytest tests/audit/test_post_build_review.py -q` green (absolute
   interpreter path); ruff green on changed files; quote outputs.
4. Commit (`fix(review): admit U+02BC in the lexical token joiner (#5374)`, trailer
   `X-Agent: kimi`), push same branch, reply on the PR via REST with evidence.
5. No auto-merge; no merge.

ROUTING_CARD_V1
task_id: infra-5374-joiner-fix
tier: heap
model_x_harness: kimi/k3-256k (authoring lane, review fix on own PR)
advisor_packet: fable (this brief + PR review)
owned_paths: scripts/audit/post_build_review.py, tests/audit/test_post_build_review.py
acceptance_cmd: .venv/bin/python -m pytest tests/audit/test_post_build_review.py -q
alternatives_considered:
  - none — authoring-lane rule binds
parallel_free_seats: n/a (single bounded fix)