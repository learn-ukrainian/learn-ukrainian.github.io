# Retired V6 review reference

This historical path is retained for documentation links, not execution.
Use the registered lifecycle in
`agents_extensions/shared/prompt-contracts/registry.v1.yaml`; build-owned
phase templates live under `scripts/build/phases/`.
