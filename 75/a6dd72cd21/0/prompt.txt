[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/delegate-sweep-7203-delta3
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Delta 3: cursor/delegate-sweep-7203 (PR #7206) — one env-dependent test (same branch, on top)

Driver re-probe of head a5d2f36993 WITHOUT the dispatch env inherited from the worker:
`tests/test_delegate_runtime_tmp_sweep.py::test_dispatch_survives_runtime_tmp_backfill_write_failure` FAILS
(`cmd_dispatch` returns 2 — the VPS-forward refusal path) and PASSES only when `LU_ALLOW_NOTEBOOK_DISPATCH=1`
is in the environment. Your worker inherited that variable from my dispatch; CI will not have it.

## Do
Neutralize placement inside the test the way the sibling dispatch tests already do —
`tests/test_delegate.py:147`: `monkeypatch.setenv(job_host_exec.ENV_ALLOW_NOTEBOOK, "1")` — and audit the other
new tests in that file for the same dependency (any test that reaches `cmd_dispatch`). No code change.

## Acceptance (quote raw output; PRIMARY interpreter)
- `env -u LU_ALLOW_NOTEBOOK_DISPATCH /Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/pytest -q
  tests/test_delegate_runtime_tmp_sweep.py tests/test_delegate_readonly_guard.py` — all green WITHOUT the variable.
- ruff clean. One conventional commit on top (`test(delegate): pin notebook placement in backfill dispatch test (#7203)`,
  X-Agent trailer). NO push, NO amend, NO rebase, NO auto-merge. Report new HEAD SHA + the green run.