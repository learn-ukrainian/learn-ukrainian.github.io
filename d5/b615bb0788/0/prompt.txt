[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/infra-6945-delta-supervisor
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Task: fix the CI failure on your PR #6945 (branch cursor/infra-registry-6943-swap)

ROUTING_CARD_V1
- tier: heap/mechanical
- model_x_harness: cursor:auto (authoring lane — CI failure on its own PR)
- why_this_tier: one-test delta + a bounded sweep
- advisor_packet: CI run 32028120494; driver reproduced locally at head 3fb3516b10
- owned_paths: tests/test_session_supervisor.py (+ any test with hardcoded epic:4707 stream ids)
- acceptance_cmd: .venv/bin/pytest tests/test_session_supervisor.py + the swept files + ruff clean
- alternatives_considered: n/a — author-lane delta
- parallel_free_seats: n/a

## The failure (reproduced)
`tests/test_session_supervisor.py::test_driver_open_heartbeat_close_and_capsule_are_fenced` — the test hardcodes `stream_id="epic:4707"`; after your registry swap the supervisor/stream stack no longer accepts it, its fenced-event list comes back empty, `assert []` fails.

## Steps
1. Work on the EXISTING branch cursor/infra-registry-6943-swap in your dispatch worktree.
2. Fix the test: prefer deriving the epic id from the live registry (`issue_streams.yaml` → infra-harness anchor) so the NEXT epic succession cannot break it; hardcoding epic:6943 is acceptable only with a comment saying why derivation is impossible.
3. Sweep: grep tests for other hardcoded `epic:4707` (or `epic:5331`) stream ids and fix the same way.
4. LIVE-IMPACT CHECK (report in the PR comment, do not fix beyond tests without saying so): what production paths validate `stream_id` against the registry, and can a CURRENTLY-OPEN session stream carrying an epic:4707-era id break at deploy (cold-start refusal, lease validation)? If yes, name the path — the driver decides whether a dual-accept window is needed before merge.
5. Run acceptance + ruff. Push to the same branch, comment on PR #6945 with evidence.
6. Do NOT arm auto-merge (it is already armed) and do NOT merge.