[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/kimi/review-rail-hardening
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

CI is RED on your PR #6417 — pytest shard 4/4: tests/test_verify_review.py fails 3 tests (test_inflated_range_fails_line_mismatch, test_short_range_fails_line_mismatch, test_cli_duplicate_later_and_inflated_range). Root cause: your endpoint normalization in scripts/review/evidence.py accepts ANY wrong end_line when the verbatim matches at start_line — including INFLATED ranges (claimed end BEYOND start+quote_lines-1). An inflated claim asserts evidence covering lines the reviewer never quoted — an overclaim that must keep failing. Fix: 1) Restrict normalization to UNDER-claims only: normalize when claimed end_line <= start_line + quote_lines - 1 (and content matches exactly at start); an end_line GREATER than canonical remains OUTCOME_LINE_MISMATCH with the range_span_mismatch detail. 2) Update test_short_range_fails_line_mismatch to the new contract (short range + exact content -> verified, normalized end) — that behavior change is the intended fix; keep/verify test_inflated_range and the CLI duplicate test passing UNCHANGED. 3) Extend your normalization unit test with an inflated-range case asserting rejection (mutation-guard for this exact regression). 4) Run the FULL tests/test_verify_review.py + tests/ai_agent_bridge/test_review_worktree.py + ruff. Quote outputs in a PR comment. 5) Commit with X-Agent trailer, push same branch. No auto-merge.