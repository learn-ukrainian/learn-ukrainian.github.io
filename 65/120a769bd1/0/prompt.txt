[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/kimi/infra-6915-bridge-routing
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Task: resolve issues #6915 AND #6894 together — bridge legacy paths must derive routing/models from the live participant registry

ROUTING_CARD_V1
- tier: practical (upper)
- model_x_harness: kimi:k3-256k (native Kimi Code)
- why_this_tier: transport-layer root-cause work across the bridge's legacy paths; strongest free builder
- advisor_packet: both issues carry live reproductions + root-cause asks (filed by the driver from this weekend's failures)
- owned_paths: scripts/ai_agent_bridge/**, related tests
- acceptance_cmd: .venv/bin/pytest (bridge tests) + ruff clean + CI green on PR
- alternatives_considered: cursor (taking #6920), grok (taking #6916), codex (capped)
- parallel_free_seats: deepseek (taking test sweep), glm

## Evidence rules (#M-4)
Reproduce each defect at unit level before fixing; quote before/after. Mutation-check new guards.

## Steps
1. Read issues #6915 and #6894 in full — same root-cause family: bridge legacy paths carry their own hardcoded routing/model state that drifts from the live ACP participant registry. #6915: generic `process <id>` ignores the recipient seat, shells to the retired gemini CLI, and ACKS THE MESSAGE ON FAILURE. #6894: `ask-gemini` default slug is stale vs the agy pin.
2. Fix at ONE layer: routing and model defaults derive from the recipient seat / participant registry (the same source the working ask-* ACP routes use), not per-command hardcoded values. Ack/consumption becomes strictly conditional on a successful routed reply. Sibling sweep both issues demand: audit every process-* variant and every ask-* default slug for the same drift/ack-on-failure shapes.
3. Preserve the honest typed-error reply behavior (a failed processing may still notify the sender) — but the original message must remain unconsumed/retryable.
4. Work ONLY in your dispatch worktree. Tests for: recipient-derived routing, ack-only-on-success, stale-slug impossibility (defaults read the registry). Mutation-check the ack guard.
5. Run affected pytest + ruff. Conventional commit, push, ONE PR fixing both issues (Fixes #6915, Fixes #6894).
6. Do NOT arm auto-merge and do NOT merge.