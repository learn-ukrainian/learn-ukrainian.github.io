[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/phase3-6375-vspu-2025-source-admission-r1
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

You own one narrow blocker fix on draft PR #6795, branch
`cursor/phase3-6375-vspu-2025-source-admission-r1`, starting HEAD
`0fe913336e2f639a236b6c3767b85eaccd8fd049`.

You are not alone in the repository. Do not revert or alter work outside the
four PR paths. Do not touch main, generated status/audit/review artifacts,
linter configs, or `.python-version`. Do not merge or arm auto-merge.

Cross-family review found that all seven fields under
`custody.google_drive_provider_identity_sha256` are presented as custody/
readback proof but remain pattern-only and are not anchored during
`validate_receipt()`. An attacker can mutate them, recompute the receipt
self-hash, and pass `--check`. Root adjudicated this as material.

Implement a complete fail-closed seal using the seven privacy-safe hashes
already present in the tracked public receipt (never expose raw Drive provider
IDs or personal paths):

- add one authoritative module mapping constant containing the exact seven
  current digest values;
- schema-const every one of the seven fields to its authoritative value;
- in `build_custody_block`, hash the live provider IDs as today and require the
  complete produced mapping equals the authoritative mapping before emitting;
- in `validate_receipt`, independently require exact complete-map equality;
- extend resealed mutation tests so each of the seven paths is changed and the
  outer self-hash recomputed, and every forgery is rejected;
- add/retain positive assertions that the committed receipt carries the exact
  mapping;
- regenerate the tracked public receipt canonically so implementation/schema/
  body hashes are current.

Preserve text-free, private-custody-only `NARROW_ONLY`, no topic closure or
narrowing, no semantic gold, Phase-3-incomplete, Phase-4-blocked semantics.
Keep raw provider IDs out of tests, logs, receipt, and Git.

Verify with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python`:
focused VSPU + university admission/freeze/reconciliation tests; Ruff on the
changed Python/test files; module `--check`; explicit resealed mutation proof
for all seven fields; exact four-path diff/status.

Commit with
`X-Agent: cursor/phase3-6375-pr6795-drive-provider-seal-fix-r3` and push the
existing branch. Report exact new HEAD, commands/results, receipt body SHA, and
residuals. Fresh exact-head Claude+Grok review and CI remain the orchestrator's
responsibility.