[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/agy/timeout-burndown-7213-s6
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Task: #7213 slice 6 — burn down the subprocess-timeout allowlist for `scripts/audit/` (one PR)

Follow the merged pattern of slices 1–5 (#7214 `scripts/ci/`, #7215 `scripts/api/`, #7217 `scripts/ai_agent_bridge/`,
#7231 dispatch layer, #7239 `scripts/orchestration/`): a named module-level constant per bound class,
`TimeoutExpired` mapped onto each site's EXISTING failure semantics, one test per touched site exercising the real
code path with a monkeypatched `TimeoutExpired`.

This slice covers ONLY the 53 allowlist entries under `scripts/audit/` — the largest remaining block. Audit runs are
long unattended batch jobs; a hung child there stalls a whole audit pass silently.

## Outcome
Every `scripts/audit/` allowlist entry is either fixed (bounded, removed from the allowlist) or explicitly skipped
with a one-line reason in the PR body. No other directories.

## Rules for values
- Grep each module for existing `timeout=` / budget conventions FIRST and reuse them. Audit code may legitimately run
  long child processes (model calls, big greps, validators) — derive bounds from the module's own batch budgets where
  they exist; a site with a genuinely unbounded-by-design child (e.g. a supervised long model call with its own
  watchdog) is a SKIP with reason, not an invented number.
- Local git / file tools: tens of seconds. Network/API helpers: minutes, from the module's own budget.
- Map `TimeoutExpired` onto the site's existing failure shape (typed audit errors, per-item failure records in batch
  loops — a timeout on ONE item must fail THAT item exactly like any other per-item error, never abort the whole
  audit pass unless that is the current posture). State the shape per site in the PR table.

## Coordination
- 53 entries is large: if the honest bound/SKIP analysis makes one PR unwieldy, split INTO TWO commits in the same PR
  (by submodule), never two PRs (slices stay serial on the allowlist file).
- Do not touch quality-gate THRESHOLDS or audit pass/fail criteria (`scripts/audit/config.py` numbers are
  load-bearing) — bounds and mapping only.

## Acceptance (quote raw output; PRIMARY interpreter; run from YOUR worktree root)
1. `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/pytest -q tests/test_subprocess_timeout_guard.py` green
   (allowlist shrank; no stale entries; any test you add carries its own `timeout=` on subprocess calls).
2. Every existing test file for touched modules (`grep -rl "<module>" tests/`) green; ruff clean.
   KNOWN pre-existing red to EXCLUDE if a sweep pulls it in: `tests/orchestration/test_curriculum_lifecycle_pilot.py`
   (#7238 — 12 setup errors on plain main; not yours).
3. Table in the PR body: entry → fixed (constant + value + derivation) / skipped (why). Counts: fixed N, skipped M;
   remaining `scripts/audit/` entries must be exactly M.
4. One conventional commit (`fix(audit): bound subprocess calls — allowlist burn-down slice 6 (#7213)`,
   X-Agent trailer; two commits by submodule allowed if the table is split). NO push, NO auto-merge, NO amend after
   reporting. Report HEAD SHA + evidence + ready PR body (no gh auth — RETURN text; driver posts).