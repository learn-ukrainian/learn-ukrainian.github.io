[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/capacity-routing-permanent
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Task: Permanent capacity-first dispatch routing (operator 2026-08-12)

Closes nothing alone — link stream #4707. **No Codex** if budget still deficit; you are Cursor/Auto.

## Why
Drivers (esp. Grok) habit-route to Codex while CodexBar shows **deficit** and free seats (Cursor, AGY, GLM, Kimi) sit idle. Tools already exist (`/api/state/routing-budget`, `codexbar`, `--check-budget`) but are optional / easy to skip. Operator: make routing permanent.

## Deliverables

### 1) CLI: `python -m scripts.fleet.capacity_pick`
New module under `scripts/fleet/capacity_pick.py` (+ `__main__` path via package).

Behavior:
- Call `compute_routing_budget(fresh_codexbar=False)` by default; `--fresh` sets True (may be slow).
- Also read `/api/delegate/active` if Monitor up (fail-open).
- Print human table: lane | status | remaining% | will_last | pace | in_flight | notes
- Print `recommendation.primary_agent_for_code` + rationale
- Print **pick order** for code implement: cool/idle first; mark `AVOID` for hot/near_cap/deficit (`will_last_to_reset is False`)
- Exit 0 always unless --strict and no cool lane
- `--json` for machine output
- No network secrets; no long multi-provider codexbar loops (use budget API which already embeds codexbar)

### 2) `scripts/delegate.py` budget guard hardening
In `_resolve_agent_with_budget_guard` (with `--check-budget`):
- Keep existing near_cap hard-sub.
- **ADD:** if requested lane status is `hot` OR codexbar `will_last_to_reset` is False (deficit), treat like near_cap for hard auto-sub **when** a fallback exists in `agent_fallback_substitutions.yaml` `dispatch_fallbacks`.
- If no yaml fallback: **refuse dispatch** (exit non-zero) unless `--force-agent`, printing clear message listing cooler lanes from the budget snapshot.
- Update `scripts/config/agent_fallback_substitutions.yaml` `dispatch_fallbacks`:
  - `codex: cursor` (mechanical shed when codex hot/deficit)
  - keep existing `claude: codex` only if still intended — do not invent other mappings without comment
- Default: prefer keeping `--check-budget` **opt-in** still OR document that seat rules always pass it. **Do not** flip default on if it breaks hermetic tests without updating `tests/test_delegate_check_budget.py`.
- Prefer: add env `LU_DISPATCH_CHECK_BUDGET=1` that forces check-budget as if flag set (document in SCRIPTS.md / fleet-driver-routing) so launchers can enable without flag churn.

### 3) Docs (binding)
- `agents_extensions/shared/rules/fleet-driver-routing.md` § free-lane: add mandatory pre-dispatch `capacity_pick` + refuse deficit when free cooler seats exist.
- `agents_extensions/shared/skills/drive-epic/SKILL.md` §4 Dispatch: require `capacity_pick` + `--check-budget` before every implement dispatch.
- One short note in `docs/SCRIPTS.md` under delegate.

### 4) Tests
- Extend `tests/test_delegate_check_budget.py` for hot/deficit hard-sub and refuse-without-fallback.
- Unit tests for `capacity_pick` pure formatting with fixture payload (no live codexbar).

## Out of scope
- Redesigning model catalog tiers
- Forcing GLM into CI
- Changing language-lane rules

## Validation
- pytest tests/test_delegate_check_budget.py tests/fleet/ or tests/test_capacity_pick.py
- ruff on touched py
- PR with X-Agent trailer; no auto-merge by worker

## CAPACITY_CARD (for you)
You were picked as cursor because Codex is in deficit — prove capacity-first works.