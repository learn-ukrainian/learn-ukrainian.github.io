[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/agy/infra-6987-flaky-race
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Task: root-cause and fix issue #6987 — concurrent-substitution race (one PR)

You are agy, fixing GitHub issue #6987 (read it first — it carries the CI evidence). One
bounded unit; do not start other work.

## Summary
`tests/test_fleet_comms_authority_cutover.py::test_concurrent_substitution_requests_do_not_fork_authority_or_reservation`
fails intermittently in CI only: `RoutingReservationError: substitution_idempotency_conflict`.
Local: 9 clean runs across single/serial/xdist. The test drives concurrent substitution
requests against `scripts/fleet_comms/routing_reservations`; the idempotency guard fired where
the test expects both requests to converge without forking authority.

## Required approach (root cause, not symptom)
1. Read the test and `routing_reservations.py` substitution/idempotency path end-to-end.
   Determine which is wrong under true concurrency:
   - the PRODUCTION path (a real race window where two concurrent identical substitution
     requests can conflict instead of converging idempotently) → fix the code; or
   - the TEST (unsynchronized setup/threads making a legitimately-conflicting interleaving
     possible) → fix the test's synchronization.
   State which, with the exact interleaving that produces `substitution_idempotency_conflict`.
2. FORBIDDEN outcomes: retry loops around the assertion, `flaky`/rerun markers, skipping,
   widening the assertion to accept the conflict, sleeps as synchronization.
3. Reproduce deterministically if possible (inject the interleaving via monkeypatched
   sync points / barrier); the regression test must fail on the unfixed code — quote that
   (mutation-proof), then pass on the fixed code.
4. Run: the full test file serial + `-n auto --dist=loadfile`, plus any routing_reservations
   unit files; ruff on touched files. Quote output.
5. One PR vs main: conventional commit, `Fixes #6987`, X-Agent trailer, push, NO auto-merge.

## Evidence rule (#M-4): every claim backed by quoted raw tool output.

## Routing card (ROUTING_CARD_V1)
tier=practical · model_x_harness=agy/gemini-native (Antigravity 59% reserve) · why=free lane;
concurrency debugging within Flash capability with a deterministic-repro requirement ·
owned_paths=scripts/fleet_comms/routing_reservations.py, tests/test_fleet_comms_authority_cutover.py ·
acceptance_cmd=deterministic repro test red-on-unfixed, green-on-fixed ·
alternatives=cursor (reserved for PR-B on #6986 merge), codex (embargoed), grok (free — second
choice) · research_classification=generic (deliberate)