[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/infra-6953-c0-transport
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

Delta 2 on your PR #6973 (worktree .worktrees/dispatch/cursor/infra-6953-c0-transport): CI shard 2 red on ONE test — tests/test_agent_runtime_acp_transport_cutover.py::test_adapter_transport_metadata_and_auth_selector_are_non_secret asserts the adapter's env overrides equal exactly {'ACPX_AUTH_LOGIN': '1'}, but your child-PATH prepend now injects PATH into that surface (assertion diff shows ACPX_AUTH_LOGIN + a PATH value). Decide the RIGHT layer: if PATH belongs in the child env but NOT in the non-secret metadata/env_overrides surface that test guards, scope the prepend to the spawn env only (not the recorded overrides); if PATH legitimately belongs in env_overrides, update the test to assert the auth selector subset + a PATH entry that starts with the validated node dir (never weaken the non-secret guarantee — the test's point is no secrets in that surface). Quote your reasoning + the failing assertion before, green after. Acceptance: that file + acpx adapter suite green via /Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python -m pytest; ruff clean. Conventional commit fix(transport): scope node PATH prepend vs transport metadata surface (#6953); push. NO auto-merge. Evidence preamble binds.