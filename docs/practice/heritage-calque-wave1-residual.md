# Heritage calque wave-1 residual census (#6623)

Generated by `scripts/lexicon/heritage_calque_wave.py`. Every count below is tool-backed against `data/sources.db` (`ua_gec_errors` where `error_type='F/Calque'`) and `data/atlas.db` at the run that produced `data/lexicon/heritage_pairs.wave1-calque.yaml`.

## Pipeline counts

- UA-GEC `F/Calque` raw rows: 1807
- Unique (error, correct) pairs after normalize/dedupe/quote-artifact drop: 1807
- Routed to extend an existing curated pair (correct matches an existing pair's corrections/nativeLemma): 24
- Routed to a new pair (correct exact-matches a public practice-eligible Atlas lemma, non-proper-noun): 276
- Structurally unresolved (correct has no practice-eligible Atlas lemma and no existing pair) — cannot be emitted regardless of carrier availability: 1461
- Excluded: correct resolves to the same Atlas lemma as an existing `sense_restricted` pair (e.g. `лікар` vs the curated `доктор` -> `лікар`) — folding an undisambiguated mechanical frame into that pair would fail its `disambiguated: true` requirement, so these are dropped rather than misfiled: 46

## Emission

- Frames emitted extending existing pairs: 17
- Frames emitted on new pairs: 205
- New pairs emitted (>=1 frame found): 170
- **Total new frames emitted: 222**

## Residual — no carrier sentence found (fail-closed, never invented)

- 6 routable (error, correct) rows found zero clean corpus sentence containing `correct` as a whole token within the 40-220 char / Ukrainian-script / single-occurrence / no-OCR-artifact gates. Full list: `data/lexicon/heritage_pairs.wave1-calque.residual.json`.

- 0 additional routable rows were left unprocessed once the run's volume cap was reached (see `--max-total-frames`); they are not linguistic residual, just deferred to a future wave.

- 25 further routable rows were skipped purely by the per-pair frame cap (`--max-frames-per-existing-pair` / `--max-frames-per-new-pair`) keeping any single pair from dominating the wave; also deferred, not linguistic residual.

## Excluded — cross-POS homonym risk

- 47 routable rows were excluded because `correct` VESUM-resolves to more than one distinct lemma (e.g. `збіг` the noun "coincidence" vs `збіг` the masculine past tense of `збігти` "ran off"). A carrier sentence is matched by literal string only, so an ambiguous token risks landing the fill-slot on the wrong sense; these are dropped rather than guessed at.
