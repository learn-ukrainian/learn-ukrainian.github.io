# Bug Autopsy Index

One-liner per bug. Grep for symptoms or categories to find relevant detail files.

<!-- INDEX-START -->
| Date | Issue | Category | Summary |
|------|-------|----------|---------|
| 2026-04-05 | #1150 | score-parsing | Wiki review loop stuck at 8/10 — regex `(\d+)` can't match decimal scores like `8.8/10` |
| 2026-04-08 | — | mdx-parse | `re.sub` unescapes `\n` in JSX replacement strings → acorn parse failure on 39 A2 MDX files |
| 2026-04-08 | — | mdx-parse | Missing blank lines before HTML blocks → MDX parses `<div>` as inline, breaks `<TabItem>` nesting |
| 2026-04-08 | — | mdx-parse | LLM writer artifacts (stray ` ``` `, `<!-- -->`, bare `<br>`) break MDX parser |
| 2026-04-23 | #1431 | prompt-sync | Writer vs reviewer calibration drift on immersion/engagement/dialogue/plan — fixed via shared contract `scripts/build/contracts/module-contract.md` referenced by both sides |
| 2026-04-23 | EPIC | alignment-contracts | Sidecar cache reuse without hash check (`v6_build.py:3207`) — stale contract.yaml/wiki-excerpts.yaml silently consumed after plan/template/tokenizer change |
| 2026-04-23 | EPIC | alignment-contracts | `module_memory` sources_hash updated silently — corpus/rule changes land but old learned constraints persist (`module_memory.py:293-316`) |
| 2026-04-23 | EPIC | alignment-contracts | Rule-after-incident governance pattern — rules added post-incident are advisory, not CI-enforced; live contradiction between "no-rewrite" decision and `convergence_loop.py` rewrite strategies |
| 2026-05-05 | #1683 | agent-hallucination | Gemini fabricates verbatim Антоненко-Давидович quote flagging feminine «собака» as Russianism — reproducible across two threads same day, false per VESUM + АД corpus check + СУМ-11 «ч. і рідше ж.» + Мирний 1949 attestation |
| 2026-05-08 | #1808 | agent-comms | `ab discuss` rounds 2+ silently dropped peer round-1 replies — `build_agent_prompt` pulled channel-wide tail instead of thread-scoped, budget truncator evicted in-thread messages on noisy channels. Fix: `thread_id` parameter + drop monitor/context first when in thread mode (commit 83d08a9604). Detail in `agent-comms.md`. |
| 2026-05-10 | — | secret-leakage | Claude grepped `~/.bash_secrets` for `GEMINI_API_KEY` and printed the matched line in cleartext — value `AIza...` leaked to transcript. User had to rotate. Root: `grep` output included the full line including the value; the same command had `sed 's/=.*/=<REDACTED>/'` applied to `env` output but not file-grep output (inconsistent sanitization). Detail in `secret-leakage.md`. |
| 2026-05-21 | #2177 | ocr-hallucination | Gemini vision-LLM hallucinated etymologically-plausible content for wrong pages on dense print — caught by shadow uniqueness and substring repetition checks. Detail in `gemini-ocr-hallucinations.md`. |
| 2026-05-21 | #2001 | ocr-pivot | ESUM re-OCR project killed after 97% wrong-page rate; replaced with Internet Archive text layers (ABBYY XML vols 1/2/3/6, text.pdf vols 4/5); 29,576 entries reloaded into sources.db. Detail in `esum-ocr-pivot.md`. |
| 2026-05-29 | #2407 | codex-tool-capture | 3rd codex capture artifact: codex CLI 0.135.0 dropped the trailing `__` from `function_call` namespace (`mcp__sources` vs 0.132.0 `mcp__sources__`) → `_tool_name` join made `mcp__sourcesget_chunk_context` → false `mcp_tools_never_invoked` HARD-fail on every codex build despite 15 real sources calls. Gate conflates measurement-failure with behavior-failure. Detail in `codex-tool-capture.md`. |
| 2026-06-04 | #2682 | stanza-ci-flake | Stanza UK model md5 mismatch in CI prewarm wedged required `Test (pytest)` across main + the A1 stack #2613–#2668; root cause = required gate coupled to a live model download. Patched 3× same day; root-fixed by #2691 (model tests marked `slow`, excluded from required CI → hermetic). Detail in `stanza-model-md5-flake.md`. |
| 2026-06-12 | #2985 | codex-dispatch-stall | Codex dispatches complete the work + pass gates, then hang in a `write_stdin` empty-poll loop at the pre-submit/commit step and never commit → delegate shows `running` (zombie). 3× in one session (atlas-finalize-all, atlas-heritage-fix, kaikki-ipa-etym-wiring). Caught by non-zero `--silence-timeout` (the first run had it =0 → ran ~75 min unbounded). Orchestrator must finalize-the-zombie: verify gates, strip codex's out-of-scope pre-submit cleanup, commit/push/PR. Detail in `codex-dispatch-stall.md`. |
| 2026-06-12 | #3039 | deploy-orphan-guard-silent-abort | `npm run agents:deploy` silently no-ops because `deploy_prompts.sh`'s orphan guard aborts the WHOLE deploy on the first undeclared orphan — 6 stray `a2-*` scratch files in the `.agent/` deploy-target root blocked every deploy, so `.claude/`/`.codex/`/`.agent/` agent definitions went stale (the #3039 reviewer-seat policy never propagated; resync touched 197 files). `npm run --silent` hides the abort line. Fix: never write scratch to a deploy-target root (use `.agent/tmp/`); run `bash scripts/deploy_prompts.sh` directly and check exit code. Detail in `deploy-orphan-guard-silent-abort.md`. |
| 2026-06-12 | #2836 | node-modules-eloop | Self-referential `node_modules -> node_modules` symlink at repo root makes every `npm run build`/`npm ci` die instantly (exit 194, no output) with `spawn ELOOP` — npm walks ancestor `node_modules/.bin` into PATH and the loop ELOOPs the spawn. Looks like "Astro is broken"; astro runs fine directly (2353 pages/15s). True cause: the self-symlink was **committed** (#3041, mode 120000, self-target) because `.gitignore` had dir-only `node_modules/` — so every checkout/`reset --hard` re-materialised it. Cure: `git rm --cached node_modules` + `.gitignore` `node_modules/`→`node_modules`; plus `check_self_symlinks.py` canary (SessionStart + API-orient auto-heal) + delegate self-link guard. Detail in `node-modules-eloop-symlink.md`. |
| 2026-05-21 | #2001 (closed 2026-05-17) — autopsy added 2026-05-21 | esum-ocr-pivot | ESUM re-OCR pivot — vision-LLM failure → Internet Archive text layers |
| 2026-05-21 | #2177 (Follow-up to killed #2001 OCR handoff) | gemini-ocr-hallucinations | Gemini Vision-LLM OCR Hallucinations |
| — | — | node-modules-eloop-symlink | self-referential `node_modules` symlink breaks every Astro/npm build with `spawn ELOOP` |
| 2026-06-04 | #2613 | stanza-model-md5-flake | Stanza model md5 flake wedged required CI |
| 2026-06-14 | #3124 | fixture-only-feature-latent-gate-break | Atlas re-enrich turned `test_atlas_conformance` RED on main: #2971 appends `(etymology of base form X)` to the kaikki etymology source, the gate required exact `KAIKKI_SOURCE` match. #2971's dispatch brief forbade a real re-enrich → fixture-only tests never hit the regenerated artifact through the gate; latent until the orchestrator's first real regen (on main). Fix #3124 tolerates the suffix. Lesson: fixture-only feature + strict gate on a generated artifact = latent break on regen; wire the conformance suite into `verify_manifest.py`. Detail in `fixture-only-feature-latent-gate-break.md`. |
| 2026-06-15 | #3197→#3210 | atlas-conformance-vesum-false-positives | First real `make atlas` regen (+120K lines) turned §8 `lemma_in_vesum` RED on 4 lemmas — ALL false positives: lookup casefolded queries before exact-match but VESUM stores proper nouns/abbreviations capitalized (`Афіни`/`Чернівці`/`УЗД`; SQLite NOCASE folds only ASCII), proper-noun exemption was suffix-blind (`proper noun:pl`), and `хвастливий` is authentic (Грінченко/ЕСУМ/СУМ-20) but VESUM-absent. Fix: probe case-preserved form + base-pos match + tiny cited heritage allowlist; 4→0 violations. Class fix still open: sources.db heritage-fallback vs manual allowlist. Same family as #3124, one layer down (the conformance logic itself). Detail in `atlas-conformance-vesum-false-positives.md`. |
| 2026-06-17 | #3150 / #2985 | atlas-misdiagnosis | **CORRECTION of a false autopsy.** The `atlas-3150-autoexpand` dispatch actually **SUCCEEDED** (implemented #3150's vocab-coverage gate, expanded the manifest 2447→2783 with FULL enrichment wiki 657→718 / heritage 100%, tests 21/21, conformance 29/29) and merely hung at push (#2985 finalize-zombie). The reviewing orchestrator wrongly condemned it via two forensics errors: (1) measured the worktree manifest **while the process was still writing it** (transient `wiki=0`) instead of the committed blob (`git show HEAD:` = 718); (2) read a `origin/main..HEAD` **base-divergence** diff as a "deleted module" when `git show HEAD --stat` proves the commit touched zero curriculum files. Lesson: attribute changes from `git show <sha>` not `base..HEAD`; only measure COMMITTED artifacts; a `status=failed` dispatch ≠ failed deliverable — recover the zombie. Detail in `atlas-3150-misdiagnosis.md`. |
| 2026-06-22 | #3725 | deploy-tooling | `deploy_headroom.sh` left the Headroom proxy DOWN mid-upgrade via two independent bugs: (1) `pipx install --force` recreates the venv but uv refuses to clobber a venv it didn't create once `PIPX_DEFAULT_PYTHON` drifts 3.13→3.14 (Homebrew update) → `--force` aborts ("Not removing existing venv ... Use --clear"); (2) `headroom install stop` boots the launchd job out of the GUI domain but `install start` only `kickstart`s an already-loaded job → `Could not find service ... in domain 501` (exit 113). Fix: uninstall+install pinned to the venv's current interpreter (override `HEADROOM_PYTHON=`); on start-failure re-bootstrap the EXISTING plist — NOT `apply`, which regenerates `manifest.json` and drops proxy tuning. Detail in `2026-06-22-headroom-deploy.md`. |
| 2026-06-26 | #3852 | atlas-build | `npm run build` renders STALE Atlas data after a local manifest regen: `hydrate-manifest.mjs` downloads the OLD Release asset whenever local sha ≠ committed pointer sha, silently clobbering the regen. Caught only by render-verify (#M-4a). Fix: write the pointer LOCALLY first (`publish_manifest.gzip_manifest`/`build_pointer_payload`/`write_pointer`, no upload) → hydrate becomes a no-op → build → upload asset only at merge. Deeper coupling: any `scripts/lexicon/*.py` edit bumps the code fingerprint → `assertPointerFresh` throws → forces an asset republish (intentional; bundle output-neutral edits into a data-regen PR). Detail in `2026-06-26-atlas-hydrate-clobber.md`. |
<!-- INDEX-END -->
