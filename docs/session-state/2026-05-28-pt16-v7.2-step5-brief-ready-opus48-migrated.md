---
date: 2026-05-28
session: "Pt 16 — Opus 4.8 restart session. Resolved Q2/Q3 design gate, migrated live Claude dispatch defaults 4.7→4.8 (selective), drafted the Step 5 /goal brief (NOT fired), and cleaned up post-4.8 guidance. 2 PRs merged (#2395 #2397), 1 follow-up issue filed (#2396). Zero autopsies, zero regressions. Session paused at user request to FIRE STEP 5 IN A FRESH SESSION."
status: v7.2-step5-brief-ready-awaiting-fire-in-new-session
main_sha: 31e3806d37
main_green: yes
working_tree_dirty: this handoff + current.md update (committed direct to main, docs-only)
session_close: 2026-05-28 night
---

# 2026-05-28 — Part 16: Opus 4.8 restart — Q2/Q3 resolved, 4.8 migration shipped, Step 5 brief READY (not fired)

**THE ONE NEXT ACTION: fire Step 5. The brief is written, all prep is merged, firing is the only thing left and it is YOUR GATE.**

## TL;DR

First session on **Opus 4.8** (CLI 2.1.154). Cleared the Pt 15 design pause and did the Opus 4.8 migration the prior session set up. Everything is merged; the queue is parked at a single user gate: **fire the Step 5 `/goal`**.

- **Q2 = keep schema as-is** (no `severity`/`gate_name`/`reviewer_phrasing`). **Q3 = promote** the 3 A1 `writer.body` rules to `shared.contract`. Both user-approved. Q3 shipped in #2395.
- **Opus 4.7 → 4.8 live-default migration** shipped in #2395 — SELECTIVE (live defaults only; eval baselines + dead v5/v6 deliberately left at 4-7).
- **Step 5 `/goal` brief DRAFTED** (#2397) at `docs/dispatch-briefs/2026-05-29-v7.2-step5-prompt-generator-claude-goal.md`. **NOT fired** — explicit user gate.
- **Post-4.8 guidance cleanup** shipped in #2397 (stale `/effort` note + sonnet fallback rung).

## ▶ NEXT SESSION: fire Step 5 (the only queued action)

```bash
# run in a worktree; confirm Claude quota headroom first (see quota note below)
claude -p "/goal $(cat docs/dispatch-briefs/2026-05-29-v7.2-step5-prompt-generator-claude-goal.md)" \
  --model claude-opus-4-8 --effort xhigh
```

- The brief is `/goal`-structured (status-line protocol per `claude_extensions/rules/goal-driven-runs.md`), grounded in the real loader API + the actual `linear_pipeline.py` call sites (`writer_context` L2893, `review_context` L3699, `writer_prompt_path` L1778). M-cap = 30.
- **Quota gate (#M0):** a dispatched Opus 4.8 `/goal` competes with the user's interactive seat. Confirm headroom before firing. If tight → **codex fallback** (documented in the brief; Step 5 is more mechanical than Step 4).
- **Monitor** via the Claude session rollout JSONL (NOT batch_state logs — per #M-8); the `/goal` status-line tokens (`GOAL_STATUS`/`GOAL_DONE`/`GOAL_ABORT`/`GOAL_WAIT`) appear every turn.
- Step 5 halt predicate (when `GOAL_DONE` is legitimate): `prompt_generator.py` wired into `writer_context`/`review_context` behind `--use-generator`; flag OFF = byte-identical to legacy; flag ON = parity-equivalent for m20; new + existing tests green; PR opened. **No auto-merge** — orchestrator reviews.

## What this session shipped

| PR | Title | Merged SHA |
|---|---|---|
| **#2395** | Q3 slot promotion + Opus 4.8 live-default migration | squashed into `742f35f7a0` |
| **#2397** | Step 5 dispatch brief (draft, NOT fired) + post-4.8 guidance cleanup | squashed into `31e3806d37` |

Plus **issue #2396** filed — re-calibrate VESUM/russianism/code-review judges with `claude-opus-4-8` (the audit baselines this session deliberately left at 4-7).

### #2395 — Opus 4.8 migration classification (REFERENCE — don't re-litigate)
- **Migrated to 4-8 (live defaults only):** `agent_runtime/registry.py` (claude + claude-desktop), `adapters/claude.py`, `pipeline/dispatch.py`, `ai_llm/claude_call.py` (ladder primary + preferred), `_dispatch_wrappers.py` (review-deep `--model`), `build/linear_pipeline.py` (claude-tools writer+reviewer), `build/build_module_direct.py` (l2-uk-direct), `batch/batch_gemini_config.py` (`CLAUDE_OPUS`), `openai_proxy.py` (ADDED 4-8 route alongside 4-7), + rule/memory example strings.
- **LEFT at 4-7 (do NOT bump without re-calibration):** all `scripts/audit/*` — recorded F1 baselines + judge recommendations; `tests/audit/*` path assertions are coupled to the `claude-opus-4-7/` artifact dirs and BREAK if the strings move. Tracked in #2396.
- **LEFT (dead):** v5/v6 pipelines.
- **Tests updated:** `test_agent_runtime.py` (adapter default), `test_ab_dispatch_wrappers.py` (review-deep wrapper).

### #2397 — guidance cleanup
- CLAUDE.md `/effort` rows: the obsolete *"Opus 4.7 at high is weaker → use xhigh"* override replaced with 4.8 guidance (high = 4.8 default; xhigh for coding/agentic; sweep per route).
- `claude_call.py` `CLAUDE_MODEL_LADDER` fallback rung `sonnet-4-5 → sonnet-4-6`.

## V7.2 leg — execution status

| # | Step | Issue | Status |
|---|---|---|---|
| 1 | Purge m20 literals + leakage sentinel | #2383 | ✅ merged (Pt 15, #2393) |
| 2 | review_context wiki/contract pass-through | #2384 | ✅ merged (Pt 15, #2392) |
| 3 | wiki_coverage_gate inline tags | #2385 | ✅ merged (Pt 15, #2391) |
| 4 | universal_rules registry scaffold | #2386 | ✅ merged (Pt 15, #2394) |
| 4.5 | Q3 slot promotion (3 A1 rules → shared.contract) | — | ✅ merged (Pt 16, #2395) |
| 5 | `prompt_generator.py` | #2387 | 📝 **brief READY, NOT fired** — fire next session |
| 6 | Reviewer single-source emission | #2388 | ⏸ blocked by #5 |
| 7 | m20 rebuild + ship as anchor | #2389 | ⏸ blocked by #6 |
| 8 | m21+m22 pilot | #2390 | ⏸ blocked by #7 |

## Cold-start sequence for next session

1. Fetch + read this handoff.
2. Confirm main at `31e3806d37` or later (`git log --oneline origin/main -1`).
3. Orient via Monitor API (`/api/orient`, inbox).
4. **Confirm Claude quota headroom** (the fire competes with the interactive seat).
5. **Fire Step 5** with the command above (or codex fallback if quota tight). Run in worktree. Monitor via Claude rollout JSONL.
6. On `GOAL_DONE`: review the opened PR, merge if all blocking CI green (the `review / review` Gemini 7-min timeout is advisory per #M-0.5, NOT blocking — confirmed twice this session via `UNSTABLE`+`MERGEABLE`).

## What NOT to do next session

- ❌ **Do not re-litigate Q2/Q3 or the 4.8 migration classification** — both settled + merged. The audit baselines at 4-7 are DELIBERATE (#2396 tracks re-calibration).
- ❌ **Do not bump `scripts/audit/*` model strings** — `tests/audit/*` path assertions break, and it would falsely attribute 4.7's F1 numbers to 4.8.
- ❌ **Do not auto-fire Step 5 without confirming quota** (#M0) — and firing is the user's gate regardless.
- ❌ **Do not edit the registry slots again** — Q3 is done; the 3 A1 rules are `shared.contract`.
- ❌ **Do not ship the old m20 artifact** at `.worktrees/builds/a1-my-morning-20260528-122552` — Step 7 rebuilds fresh post-Step-6.

## Remaining flags / follow-ups (non-blocking)

- **#2396** — 4-8 judge re-calibration (low priority; 4-7 baselines remain valid).
- **`openai_proxy._ROUTABLE_MODELS`** has a phantom `claude-sonnet-4-7` route (not in catalog) and lacks `sonnet-4-6` — left as-is this session (no evidence of a caller; removing risks breaking one). Address if a Sonnet-via-proxy need surfaces.
- **#2378** — trim `linear-write.md` below 130KB ceiling: subsumed by Step 5 (template shrinks as content moves to registry/generator). CLOSE when Step 5 lands.
- **#2380 R5** — wiki_coverage_gate vs V7.1 inline tags: closed by Step 3 (#2391); verify auto-closed.
- **Q1** (R-NO-CHILDREN-PRIMARY-QUOTES level scope) — deferred; file if Step 5 surfaces it.

## Build worktrees on disk (per #M-10 forensic-keep)

```
.worktrees/builds/a1-my-morning-20260527-163310    Pt 9 forensic
.worktrees/builds/a1-my-morning-20260527-185032    ⭐ Pt 10 baseline (V7-hardened + codex 9.5/10) — KEEP
.worktrees/builds/a1-my-morning-20260528-122552    ⭐⭐ V7.1+codex+R1+R2 (Step 7 rebuild target — fresh re-fire post-Step-6, NOT --resume)
```

## One-paragraph context dump for fresh-session resume

Pt 16 was the first Opus 4.8 session. It closed the Pt 15 design pause: Q2 settled as schema-as-is, Q3 settled as PROMOTE (the 3 A1 `writer.body` rules → `shared.contract`, which turned out to be required for Step-5 generator/legacy parity since the legacy reviewer prompt already hardcodes those 3 `#R-*` anchors). It then did the Opus 4.7→4.8 migration the prior session set up — but SELECTIVELY: only live dispatch/adapter/pipeline defaults moved to `claude-opus-4-8`; the `scripts/audit/*` calibration baselines + recorded F1 tables were deliberately left at 4-7 (bumping them would lie about which model produced the numbers, and `tests/audit/*` path assertions are coupled to them — re-calibration tracked in #2396). Both changes shipped in PR #2395 (merged `742f35f7a0`), all blocking CI green, only the advisory `review / review` Gemini 7-min timeout failing (non-required). Then the Step 5 `/goal` dispatch brief was drafted (#2397, merged `31e3806d37`) at `docs/dispatch-briefs/2026-05-29-v7.2-step5-prompt-generator-claude-goal.md` — `/goal`-structured, grounded in the real loader API + `linear_pipeline.py` call sites, with the opt-in `--use-generator` migration path (flag OFF = byte-identical to legacy), the single-Obligation-Checklist contract (writer+reviewer+`wiki_coverage_gate`), 4.8 prompting tunings, and M=30 cap. Same PR cleaned up the now-obsolete CLAUDE.md `/effort` guidance and bumped the `claude_call` sonnet fallback rung to `sonnet-4-6`. **The session paused at user request to fire Step 5 in a fresh session.** The single next action is: confirm Claude quota headroom, then `claude -p "/goal $(cat docs/dispatch-briefs/2026-05-29-v7.2-step5-prompt-generator-claude-goal.md)" --model claude-opus-4-8 --effort xhigh` in a worktree (codex fallback if quota tight). Steps 6-8 (#2388/#2389/#2390) remain blocked behind Step 5.

End of 2026-05-28 Pt 16. V7.2 Steps 1-4.5 anchored + Opus 4.8 migrated. Hand off to fire Step 5.
