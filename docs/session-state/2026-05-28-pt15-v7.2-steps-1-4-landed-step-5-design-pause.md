---
date: 2026-05-28
session: "Pt 15 — V7.2 Steps 1-4 ALL LANDED. Wall-clock: ~50 min from Steps-1/2/3 fire to Step-4 merge. Registry shape verified ready for Step 5. Pause at user request to evaluate registry design before firing Step 5 generator + restart with Opus 4.8 (just released). 4 PRs merged this session (#2391 #2392 #2393 #2394). Zero autopsies. Zero regressions reported. Step 5 brief NOT yet drafted; needs writing in next session before fire. Steps 6-8 still pending sequential."
status: v7.2-steps-1-4-landed-step-5-design-pause-awaiting-user-OK
main_sha: 475e19acc5
main_green: yes
working_tree_dirty: see "Untracked artifacts" below
session_close: 2026-05-28 evening
---

# 2026-05-28 — Part 15: V7.2 Steps 1-4 ALL LANDED, Step 5 design-pause for Opus 4.8 restart

**Read TL;DR. Read the 3 open design questions. Then decide whether to fire Step 5 as-planned or refine schema/slots first.**

## TL;DR

V7.2 leg is **half-shipped**: Steps 1-4 (bleed-stop trio + registry scaffold) landed in a single session via 3 parallel codex dispatches + 1 claude-headless dispatch. All 4 PRs merged with all blocking CI green (only Gemini-Dispatch advisory `review / review` cancelled — same 7m11s timeout pattern on all 4, advisory only).

The universal-rules registry exists. 15 fragments extracted from `linear-write.md`, frontmatter schema fixed, loader + topological sort + 34 unit tests. Step 5 (prompt generator) is unblocked.

**Session paused at user request** to evaluate the shipped registry design before firing Step 5, and to restart with Opus 4.8 (released today).

Steps 5-8 still need ~1 week to deliver:
- Step 5 — `prompt_generator.py` (2-3 days, codex or claude — see recommendation)
- Step 6 — reviewer single-source emission (~1 day, codex)
- Step 7 — m20 rebuild + ship as anchor (~30 min, orchestrator inline)
- Step 8 — m21+m22 pilot (~1-2h each, orchestrator + codex)

## What's at `475e19acc5` (main right now)

This session's commits:
- `869a58ad9c` — fix(v7.2): wiki_coverage_gate parses inline implementation maps (**PR #2391**)
- `31b01d0080` — fix(v7.2): pass wiki_manifest + implementation_map_contract into review_context (**PR #2392**)
- `5a1c21b2fd` — fix(v7.2): purge m20 literals from linear-write.md + add cross-lesson leakage sentinel (**PR #2393**)
- `475e19acc5` — feat(v7.2): universal rules registry scaffold (no generator wiring yet) (**PR #2394**)

Local main and `origin/main` both at `475e19acc5`.

## V7.2 leg — execution status

| # | Step | Title | PR | Status |
|---|---|---|---|---|
| 1 | Purge m20 literals + leakage sentinel | codex | #2393 → `5a1c21b2fd` | ✅ merged |
| 2 | review_context wiki/contract pass-through | codex | #2392 → `31b01d0080` | ✅ merged |
| 3 | wiki_coverage_gate inline tags | codex | #2391 → `869a58ad9c` | ✅ merged |
| 4 | universal_rules registry scaffold | claude headless | #2394 → `475e19acc5` | ✅ merged |
| 5 | prompt_generator.py | (codex or claude) | — | ⏸ awaiting user OK + brief drafting |
| 6 | Reviewer obligation emission from same registry | codex | — | ⏸ blocked by #5 |
| 7 | m20 rebuild + ship as anchor | orchestrator inline | — | ⏸ blocked by #6 |
| 8 | m21+m22 pilot | orchestrator + codex | — | ⏸ blocked by #7 |

Issue numbers: #2383 (1) #2384 (2) #2385 (3) #2386 (4) #2387 (5) #2388 (6) #2389 (7) #2390 (8).

## What Step 4 actually shipped (registry shape for Step 5)

Claude headless wall-clock: ~10 minutes active dispatch time (vs the brief's ~1-2 day estimate — design judgment task converged fast).

### Files

```
scripts/build/universal_rules/
  R-ACTIVITY-COMPOSITION.md     shared.contract  [all/all/all]   deps: [R-RENDERER-CHARTER]
  R-AUDIENCE-LANGUAGE-A1.md     writer.body      [a1,a2/core/all] deps: []
  R-BAD-FORM-MARKER.md          shared.contract  [all/all/all]   deps: [R-VESUM-ALL-WORDS]
  R-CITE-HONEST.md              shared.contract  [all/all/all]   deps: []
  R-CLEAN-TABLES.md             shared.contract  [all/all/all]   deps: []
  R-GRAMMAR-TERMS-A1.md         writer.body      [a1,a2/core/all] deps: []
  R-IMPL-MAP-COMPLETE.md        shared.contract  [all/all/all]   deps: []
  R-NO-CHILDREN-PRIMARY-QUOTES.md shared.contract [a1,a2/core/all] deps: [R-CITE-HONEST, R-TEXTBOOK-30W]
  R-NO-SCAFFOLDING-LEAKS.md     shared.contract  [all/all/all]   deps: []
  R-PROSE-FLOOR-A1.md           shared.contract  [a1,a2/core/all] deps: []
  R-RENDERER-CHARTER.md         writer.preamble  [all/all/all]   deps: []
  R-SINGLE-VOICE-A1.md          writer.body      [a1,a2/core/all] deps: []
  R-TEXTBOOK-30W.md             shared.contract  [all/all/all]   deps: [R-CITE-HONEST]
  R-VESUM-ALL-WORDS.md          shared.contract  [all/all/all]   deps: []
  R-VOICE-META.md               shared.contract  [all/all/all]   deps: []
  README.md
scripts/build/universal_rules_registry.py    # loader: 289 LOC
tests/build/test_universal_rules_registry.py # 34 tests, 0.25s
docs/best-practices/universal-rules-registry.md  # 149 LOC design doc
```

### Loader API (what Step 5 consumes)

```python
from scripts.build.universal_rules_registry import load_applicable_rules, Rule

rules: list[Rule] = load_applicable_rules(
    level="a1",
    track="core",
    activity_profile="default",
    slot="shared.contract",  # or "writer.body" / "writer.preamble" / "reviewer.rubric" / None
)
# returns topologically sorted: every `depends_on` precedes its dependents
# tie-break: filename-alphabetical
# `Rule.telemetry_id` reconstructs `#R-X` aligned to linear_pipeline.RULE_* constants
```

Validated invariants (enforced by `_parse_fragment` + tests):
- Filename stem MUST equal frontmatter `id` (so `R-VESUM-ALL-WORDS.md` must have `id: R-VESUM-ALL-WORDS`).
- `id` must start with `R-` (NO `#` prefix in frontmatter; the `#` is reconstructed via `Rule.telemetry_id`).
- `slot` ∈ {`writer.preamble`, `writer.body`, `reviewer.rubric`, `shared.contract`}.
- `applies_to.levels` ⊆ {a1, a2, b1, b2, c1, c2, all}; `tracks` ⊆ {core, seminar, all}; `activity_profiles` is free-form.
- `depends_on` entries validated against the FULL registry, NOT the filtered subset → a filtered-out parent doesn't block the dependent, but a missing-from-registry parent always raises `MissingDependencyError`.

### Step 5 emission contract (what the generator will do)

- Writer prompt = `writer.preamble` + `writer.body` + `shared.contract` (topo-sorted across all three)
- Reviewer prompt = `reviewer.rubric` + `shared.contract` (topo-sorted)
- Currently `reviewer.rubric` is empty (0 fragments) — reserved for future direct reviewer-only rules; all 11 `shared.contract` rules carry the reviewer load via single-source emission

### Slot distribution in shipped registry

- `writer.preamble`: 1 (R-RENDERER-CHARTER)
- `writer.body`: 3 (R-AUDIENCE-LANGUAGE-A1, R-SINGLE-VOICE-A1, R-GRAMMAR-TERMS-A1 — all A1-only styling)
- `shared.contract`: 11 (the heart of the single-source-of-truth fix)
- `reviewer.rubric`: 0

## THREE OPEN DESIGN QUESTIONS for next session before Step 5 fires

Per Pt 14's handoff: *"After Step 4 lands, evaluate registry design with user before firing Step 5."* These are concrete decisions next session needs:

### Q1 — R-NO-CHILDREN-PRIMARY-QUOTES level scope

Claude tagged `[a1, a2]` only. Rule body says "no Grade 1-3 textbook blockquotes in the published module body" — arguably applies B1+ too. PR #2394 reviewer comment (orchestrator): "*Not blocking, refine later when Step 5 surfaces concrete generator behavior.*"

**Default decision next session:** leave at `[a1, a2]`, file as a follow-up issue. Step 5 can fire without resolving this.

### Q2 — Schema additions before Step 5 commits to it

PR #2394 explicitly invited reviewers to add fields now (cheap to change before generator references the schema):
- `severity` (e.g. WARN vs HARD)
- `gate_name` (mapping to deterministic gate)
- `reviewer_phrasing` (separate body text the reviewer sees, vs the writer body)

**Orchestrator recommendation (rejected all three in merge comment):**
- `severity` belongs to the gate, NOT the rule. Rules describe what to do; gates score how badly missing it is.
- `gate_name` would create a SECOND mapping that drifts with existing `linear_pipeline.RULE_*` constants (already wires `#R-*` → gate). Single source of truth survives.
- `reviewer_phrasing` is better handled by Step 5's template wrapper than by adding a parallel body to every fragment.

**Default decision next session:** schema stays as-is (id, description, applies_to, slot, depends_on). Step 5 commits.

If user disagrees, raise BEFORE Step 5 fires — much cheaper than a follow-up PR after generator code references the schema.

### Q3 — 3-way slot cut for A1 writer.body rules — keep or promote to shared.contract?

The 3 A1-only `writer.body` rules (R-AUDIENCE-LANGUAGE-A1, R-SINGLE-VOICE-A1, R-GRAMMAR-TERMS-A1) are NOT emitted to the reviewer prompt under the current scheme. They're "writer instructions about output style" — the writer is told to "stay in English at A1", "use single teacher voice", "use real grammar terms".

**Cursor's single-source-emission principle** (from the multi-agent ADR discussion): *"three hand-maintained descriptions of the same contract drift apart."* The reviewer arguably should know the writer was told these — otherwise it can't catch when the writer fails to follow them.

**Options:**
- (a) Keep 3-way cut: writer.body stays writer-only. Reviewer doesn't see these rules. (Current shipped state.)
- (b) Promote all 3 to `shared.contract`: reviewer sees the writer's instructions and can score against them.
- (c) Add `reviewer.rubric` rules that mirror them with reviewer-specific phrasing (would have wanted Q2's `reviewer_phrasing` field).

**Orchestrator recommendation:** (b) — promote to `shared.contract`. Reviewer rubric is dimensional; the current `tone` reviewer at A1 was over-penalizing UK connectors in EN prose because it didn't know the charter said "EN explanation, UK as target only." Giving the reviewer the same rule the writer saw closes that gap.

**Default decision next session:** promote the 3 to `shared.contract` if user agrees. Touches 3 fragment files + maybe a comment in the generator doc. Trivial PR before Step 5 fires.

## Recommendation for Step 5 firing strategy

**REVISED 2026-05-28 post-commit** after reading Anthropic's Opus 4.8 release notes + the official prompting guide at `https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/claude-prompting-best-practices#prompting-claude-opus-4-8`:

### Headline 4.8 changes (release-note tweets)
- 69.2% on SWE-bench Pro (up from 4.7's 64.3%)
- 4× less likely to allow code flaws to pass unremarked
- API default effort = `high` (changed from prior); rate limits raised
- Explicitly named "especially strong at long-horizon work"; Anthropic recommends pairing with `/goal`
- Built-in `/claude-api migrate` skill updates model strings + prompt phrasing

### Behavior changes from official prompting guide (DO NOT skip these)
The release-note tweets are summary-level; the prompting guide has the load-bearing details. Read these BEFORE drafting Step 5 brief — they change concrete prompt phrasing:

1. **Effort for coding work: `xhigh`, NOT the API default `high`.** Anthropic guide: *"Start with the `xhigh` effort level for coding and agentic use cases, and use a minimum of `high` effort for most intelligence-sensitive use cases."* The tweet "defaults to high" referred to the API default; the GUIDE says override to xhigh for coding. Effort matters MORE on 4.8 than prior versions.
2. **Thinking is OFF by default.** Must explicitly set `thinking: {type: "adaptive"}` to get extended thinking. Prior 4.7 patterns that relied on implicit thinking will silently regress on 4.8.
3. **More literal instruction following.** 4.8 does NOT silently generalize. If a rule should apply broadly, state scope explicitly ("apply this to every section, not just the first"). Helps API/pipeline determinism; hurts prompts that relied on inference.
4. **Tool-use bias toward reasoning.** 4.8 reasons before calling tools — under-calls in agentic search/coding unless you raise effort to `high`/`xhigh` OR explicitly prompt for tool use. **Concern for our V7 writer**: Pt 14 saw 25 tool calls / 12 distinct MCP tools on codex-tools; 4.8 in a future claude-tools writer slot might call fewer without explicit "use these MCP tools eagerly" prompting.
5. **Code-review harness gotcha — high-impact for us.** Anthropic guide: *"When a review prompt says things like 'only report high-severity issues,' 'be conservative,' or 'don't nitpick,' Claude Opus 4.8 may follow that instruction more faithfully than earlier models did."* Our `local-code-review` skill, `content-review` skill, `plan-review` skill, and Codex-as-reviewer pattern may show LOWER RECALL on 4.8 without prompt updates. Fix: tell the reviewer "report every issue including low-confidence ones; a separate verification step will filter."
6. **Max output tokens — start at 64k for xhigh/max.** Smaller budgets choke long-horizon multi-tool work.
7. **Subagent spawning down by default.** Add explicit "spawn when fanning out across items / reading multiple files" guidance to the Agent-tool-using prompts (our orchestration patterns).
8. **Response length self-calibrates.** Verbosity is no longer fixed; tune prompts that depend on a specific style.
9. **Interactive coding uses MORE tokens than autonomous.** Push Step 5+ toward `/goal`-style autonomous runs over many-turn interactive sessions.
10. **Tone defaults shift** — direct, opinionated, sparing emoji. Reviewer prompts that depended on a warmer voice may need tuning.

### After Q2/Q3 settled, fire Step 5 as Opus 4.8 via `/goal`

- Step 5 is the kind of work 4.8 is explicitly named for: generator design (judgment) + composition logic (coding) + tests + wiring across multiple call sites. Long-horizon coding work.
- Codex baseline already collected (Pt 15 Steps 1-3: ~30-37 min wall-clock each, mechanical PRs, clean output). Firing Step 5 on codex would give zero new signal.
- Opus 4.7 baseline already collected (Step 4: ~10 min active, 15 fragments + 289-LOC loader + 384-LOC tests + 149-LOC design doc, exceptionally good). Firing on 4.8 lets us measure the advertised 4.7→4.8 gain on a real comparable workload.
- `/goal` is the right harness for Step 5 because it has a measurable predicate (generator wired + tests pass + backward-compat flag works + Pt 15 acceptance criteria met) and multiple sub-steps. Goal-driven-runs rule at `claude_extensions/rules/goal-driven-runs.md` documents the structured-status-line protocol.
- Anthropic's official "use `/goal`" recommendation for long-horizon work matches our existing infrastructure.

### Step 5 brief MUST include (4.8-specific prompting)

- `--effort xhigh` (NOT high — Anthropic explicit override for coding)
- `--max-output-tokens 65536` or similar (start at 64k per Anthropic)
- Explicit `thinking: {type: "adaptive"}` setting if the dispatch adapter supports it (otherwise note as a limitation)
- Literal scope declarations: "this composition rule applies to ALL slots, not just shared.contract"; "load_applicable_rules() returns rules — emit ALL of them into the prompt slot, do not filter further"
- Explicit tool-use prompting if the dispatch wants MCP calls: "use mcp__sources__verify_words for every Ukrainian form before emitting"
- For the implicit code-review pass (Gemini-Dispatch / `/code-review` skill / Codex adversarial), add the "report every issue including low-confidence; separate verification will filter" line if the reviewer is 4.8

Step 5 dispatch brief **does not yet exist on disk** — Pt 14 only shipped briefs for Steps 1-4. Drafting the brief is the first thing to do next session (~10-15 min — slightly bigger now because of `/goal` structure + predicate definition + 4.8 prompting tunings); fire `/goal` once user OKs Q2/Q3.

**Before drafting Step 5 brief, run `/claude-api migrate`** — Anthropic's built-in skill that updates model strings + suggests prompt improvements tuned for 4.8. Mechanical migration first, then targeted manual fixes for items 1-10 above.

## Cold-start sequence for next session (with Opus 4.8)

1. **Fetch + read this handoff (incl. the Step-5-strategy revision + the prompting-guide-derived items — see "Recommendation for Step 5 firing strategy" section).**
2. **Confirm main SHA** via `git log --oneline origin/main -1` → expect `1d4cb15322` or later (Pt 15 handoff commit; later if amendment commit was pushed).
3. **Verify Opus 4.8 active** — `claude --version` or check Anthropic's official model string. Per Anthropic's prompting guide (link below): API default effort is `high`, **but explicit override to `xhigh` is recommended for coding/agentic work**. Rate limits raised on 4.8.
4. **Run `/claude-api migrate`** — Anthropic's built-in mechanical migration of model strings + prompt phrasing across the codebase to 4.8-tuned forms. ~5-10 min. Land migration commit before Step 5 brief draft so the brief uses correct strings.

### 4.8 migration checklist (kubedojo-derived 2026-05-28, project-specific)

| # | Step | Status at Pt 15 close | Action for next session |
|---|---|---|---|
| 4a | `/claude-api migrate` — model strings + prompt tuning | TODO | Run first thing after main-SHA confirm |
| 4b | Verify `.claude/settings.json` `effortLevel: xhigh` | ✅ already set | Confirm not regressed by `/claude-api migrate` |
| 4c | Audit dispatch brief templates for "coverage > filtering" language (4.8 follows "be conservative / only high-severity / don't nitpick" too faithfully → lower recall on existing review harnesses) | ✅ no first-pass hits in `claude_extensions/`, `.claude/`, `scripts/` — but worth a deeper sweep over `~/.claude/skills/{content-review,plan-review,batch-review,prompt-review,code-review,local-code-review}/SKILL.md` and any `dispatch-briefs/*-review-*.md` templates | Sweep + add the Anthropic-recommended "report every issue including low-confidence; a separate verification step will filter" preamble where missing |
| 4d | Audit curriculum-orchestrator skill + V7 phase prompts for "after every N tool calls, summarize" scaffolding (4.8 provides good interim updates natively; scaffolding is redundant) | ✅ no hits in `claude_extensions/`, `.claude/`, `scripts/build/phases/` | Clear — leave as-is |
| 4e | Add explicit "fan out parallel" reinforcement in briefs that have ≥2 independent items (4.8 spawns fewer subagents by default) | TODO — Pt 14's Steps-1/2/3 worked because the handoff explicitly said "fire in parallel"; future briefs with multi-item fan-out should embed this in the brief text | Add to brief template when drafting Step 5 (if it has independent sub-tasks) and any future multi-task briefs |
| 4f | Don't preemptively rewrite curriculum-orchestrator agent definition — observe 4.8 on Step 5 first, tune after empirical signal | TODO | Leave agent definition alone through Step 5 |

### Additional 4.8-specific items from Anthropic prompting guide (not in kubedojo's list)

| # | Step | Action |
|---|---|---|
| 4g | **Update MEMORY.md #M0** — the "Opus 4.7 at high is weaker than prior versions" override is OBSOLETE | Replace with: "4.8 default effort is `high`; coding work explicit override to `xhigh` per Anthropic guide. Rate limits raised on 4.8 — re-check `scripts/config/agent_fallback_substitutions.yaml` for routing-budget impact." |
| 4h | **Verify `thinking: {type: "adaptive"}` is set on Claude dispatches** — 4.8 has thinking OFF by default; prior 4.7 patterns silently regress | Check `scripts/agent_runtime/adapters/claude.py` for thinking parameter; add adaptive default if missing |
| 4i | **Re-check V7 writer claude-tools path** — 4.8 reasons before tool-calling, may under-call MCP tools | If/when claude-tools writer is reactivated, add explicit "use mcp__sources__verify_words for every Ukrainian form before emitting" tool-use prompting |
| 4j | **Max output token budget** — Anthropic recommends 64k+ for xhigh/max | Verify dispatch adapter sets this; raise if currently lower |

### Resume the V7.2 leg sequence

5. **Resolve Q2 + Q3 with user** (Q1 can defer). Two short multiple-choice prompts; defaults given above. Q2: schema-as-is vs add fields. Q3: keep 3-way slot cut vs promote A1 writer.body rules to shared.contract.
6. **If Q3 = promote**: draft a tiny PR (or inline edit on main if user OKs) that flips the 3 fragments' `slot:` field from `writer.body` to `shared.contract`. ~5 LOC. Land before Step 5 fires so the generator sees the corrected slot distribution.
7. **Draft Step 5 dispatch brief** at `docs/dispatch-briefs/2026-05-29-v7.2-step5-prompt-generator-claude-goal.md` (~150-200 lines, model the shape on Pt 14's Step 4 brief but structured for `/goal` not single-dispatch, and incorporating items 4g-4j above). Required surface:
   - Load registry via `load_applicable_rules()`
   - Compose `writer.preamble` + `writer.body` + `shared.contract` for writer prompt, topo-sorted
   - Compose `reviewer.rubric` + `shared.contract` for reviewer prompt, topo-sorted
   - Single Obligation Checklist block generated once, consumed identically by writer prompt + reviewer prompt + `wiki_coverage_gate` parser
   - Wire into `scripts/build/linear_pipeline.py:writer_context()` and `review_context()` (call sites already exist)
   - Backward-compatible: opt-in flag (`--use-generator` or similar); legacy `linear-write.md` path still functional during migration
   - Comprehensive tests
   - **Predicate for `/goal` halt**: "generator wired + opt-in flag works + writer/reviewer prompts render identically modulo formatting from registry-via-generator vs legacy-template + new tests green + existing tests green + PR opened"
   - **Status-line tokens per `claude_extensions/rules/goal-driven-runs.md`**: `GOAL_STATUS turn=N/M blocked=N/M no_progress=N/M queue_head=<token>`
8. **Fire as `claude -p "/goal ..."`** (NOT a single dispatch): `claude -p "/goal $(cat docs/dispatch-briefs/2026-05-29-v7.2-step5-prompt-generator-claude-goal.md)" --model claude-opus-4-8 --effort xhigh` (Anthropic explicit override for coding work — see "Step 5 firing strategy" section item 1). Run in worktree. Goal-driver-stop hook will scrub state on terminal status per `goal-driven-runs.md`.
9. **Monitor via Claude session rollout JSONL** under `~/.claude/projects/<worktree-path-hash>/<session-uuid>.jsonl` — NOT batch_state logs (per Pt 14 lesson #4 + #M-8). For `/goal` runs the status-line tokens (`GOAL_STATUS`, `GOAL_DONE`, `GOAL_ABORT`, `GOAL_WAIT`) appear in every turn; Monitor filter can match those structurally.

## What NOT to do next session

- ❌ **Do not fire Step 5 without resolving Q2 + Q3.** Schema changes are 100× cheaper now than after generator code references the schema.
- ❌ **Do not skip the Q3 slot-promotion check.** This is the cursor-named single-source-emission principle. If reviewer can't see the writer's instructions, drift class is not eliminated.
- ❌ **Do not bundle Q1 into Step 5.** R-NO-CHILDREN-PRIMARY-QUOTES level scope refinement is a separate follow-up issue. Don't expand Step 5 scope.
- ❌ **Do not ship today's m20 artifact** at `.worktrees/builds/a1-my-morning-20260528-122552`. It used the contaminated template (now purged in Step 1). Step 7 will rebuild m20 under the clean pipeline post-Step-6.
- ❌ **Do not auto-fire Step 5 even if user is idle.** Per Pt 14 handoff and this Pt 15 handoff: design-check is explicit user gate, not orchestrator inference.

## Active dispatches at session close

**Zero.** All 4 dispatches completed and merged:
- v7.2-step1-purge-m20-literals-2026-05-28 → `done` (codex; PR #2393 merged)
- v7.2-step2-review-context-passthrough-2026-05-28 → `done` (codex; PR #2392 merged)
- v7.2-step3-wiki-coverage-gate-inline-tags-2026-05-28 → `done` (codex; PR #2391 merged)
- v7.2-step4-rule-registry-scaffold-2026-05-28 → `done` (claude headless; PR #2394 merged)

## PRs landed this session

4 PRs merged, all blocking CI green, only Gemini-Dispatch `review / review` cancelled (advisory, same 7m11s timeout pattern on all 4):

- **#2391** — fix(v7.2): wiki_coverage_gate parses inline implementation maps — merged
- **#2392** — fix(v7.2): pass wiki_manifest + implementation_map_contract into review_context — merged
- **#2393** — fix(v7.2): purge m20 literals from linear-write.md + add cross-lesson leakage sentinel — merged
- **#2394** — feat(v7.2): universal rules registry scaffold (no generator wiring yet) — merged

Wall-clock per dispatch:
- Step 1 (codex xhigh, 19 LOC writer prompt + 155 LOC sentinel + 1 LOC pin-test + 21 LOC ADR): ~37 min from fire to PR open
- Step 2 (codex high, 47+19+13+33 LOC across 4 files): ~32 min from fire to PR open
- Step 3 (codex high, 22 LOC parser + 18 LOC fixture + 68 LOC tests): ~32 min from fire to PR open
- Step 4 (claude headless xhigh, 15 fragments + 289 LOC loader + 384 LOC tests + 149 LOC design doc + 11 LOC README): ~10 min from fire to PR open ← fastest

CI on every PR: ~6-7 min from PR open to all blocking checks green. Merge-to-merge cadence ~10-15 min apart.

## Build worktrees on disk (per #M-10 forensic-keep)

```
.worktrees/builds/a1-my-morning-20260527-163310    Pt 9 forensic
.worktrees/builds/a1-my-morning-20260527-185032    ⭐ Pt 10 baseline (V7-hardened + codex 9.5/10) — KEEP
.worktrees/builds/a1-my-morning-20260528-122552    ⭐⭐ V7.1+codex+R1+R2-fixed (Step 7's rebuild target — clean writer output, 1505w, 25 tool calls / 12 distinct tools, all python_qg PASS, wiki_completeness PASS, halted at wiki_coverage_gate before Step 3 landed — re-fire as fresh build post-Step-6, not --resume)
```

Build branches in local git database for forensic continuity (most relevant):
```
build/a1/my-morning-20260527-185032    Pt 10 baseline
build/a1/my-morning-20260528-014807    V7.1+claude pilot (Pt 12, failed tone REJECT)
build/a1/my-morning-20260528-060820    V7.1+codex pilot (Pt 13, pipeline-incomplete)
build/a1/my-morning-20260528-122552    ⭐⭐ V7.1+codex+R1+R2 (Pt 14, ready for Step 7 fresh re-fire post-generator)
```

(Many older `build/a1/my-morning-2026052[0-7]-*` branches retained per #M-10; not load-bearing for next session.)

## Issues open at session close

V7.2 leg follow-ups:
- **#2387** — Step 5 prompt_generator.py (next session)
- **#2388** — Step 6 reviewer single-source emit (blocked by #5)
- **#2389** — Step 7 m20 rebuild + ship (blocked by #6)
- **#2390** — Step 8 m21+m22 pilot (blocked by #7)

V7.1 follow-ups not blocked by V7.2:
- **#2380** — V7.1 build pipeline regressions surfaced by m20 pilot. R1+R2 fixed Pt 14 via PR #2382. R3+R4 addressed by Pt 14's V7.1+codex re-fire empirical data. R5 (`wiki_coverage_gate` vs V7.1 inline tags) → CLOSED by Step 3 PR #2391 (verify next session — issue may auto-close on PR merge).
- **#2378** — V7.1 follow-up: aggressive trim of linear-write.md to drop ceiling below 130KB. Subsumed by V7.2 Step 5 (template shrinks dramatically as content moves to registry). CLOSE when Step 5 lands.
- **#2368** — `/api/delegate/active` route handler regression — low priority workaround via `/api/orient` (still active workaround, no fix planned).
- **#2369** — PR #2367 scope-creep evaluation (`_contract_yaml` vocabulary fields + dialogue-gloss sentence) — read-only investigation comment posted; no action planned.

Newly orchestrator-suggested follow-ups (file if relevant when Step 5 lands):
- R-NO-CHILDREN-PRIMARY-QUOTES level scope refinement (Q1 above)

## Untracked artifacts at session close

This file:
```
docs/session-state/2026-05-28-pt15-v7.2-steps-1-4-landed-step-5-design-pause.md  (this handoff)
```

Pt 14's ADR is still at `docs/decisions/pending/2026-05-28-wiki-driven-prompt-generator.md` (PENDING — should move to accepted state once Step 5 lands; not blocking).

`docs/session-state/current.md` needs top-row update to point to this Pt 15 brief.

## Behavioral lessons from this session

1. **Parallel codex dispatches are GO when paths don't overlap.** Steps 1+2+3 were designed in Pt 14 to be independent — fired all 3 in parallel, no cross-contention, all 3 PRs opened within 6 minutes of each other. The `--silence-timeout 5400` from Pt 14's lesson worked perfectly: zero kills, all 3 finished cleanly. Codex post-PR-open idle pattern (#M-8) reproduced: dispatch shows `running` for ~45 min after task_complete event because codex CLI hangs around. **`gh pr list` is the source of truth, not `/api/delegate/active`.**

2. **Monitor filter discipline matters.** First Monitor filter (broad regex matching `git push`, `gh pr create`, etc. anywhere in the JSONL line) generated event spam from tool_call payloads containing those literal strings. **Tightened to `"payload":\{"type":"task_complete"|"shutdown"|"error"`** — exactly 4 events fired across the whole session (one per dispatch terminal state). Per the Monitor docstring: *"the filter should be selective — but selective means 'the lines you'd act on,' not 'only good news.'"* Right pattern is structural JSON-shape match, not keyword.

3. **`review / review` cancellation is the Gemini auto-review timeout pattern.** All 4 PRs hit the exact same 7m11s `cancelled` conclusion. Per #M-0.5 / Gemini-Dispatch advisory layer: cancelled = no findings delivered = not blocking. Documented elsewhere but worth re-noting: do NOT treat `review / review = cancel` as a substantive failure.

4. **Claude headless completes design tasks faster than the brief estimates.** Step 4 brief estimated 1-2 days; actual active dispatch time was ~10 minutes for 15 fragments + 289-LOC loader + 384-LOC tests + 149-LOC design doc + answers to 4 design questions. Either the brief over-estimated, or Opus 4.7 was operating at full velocity. Useful calibration for sizing future claude-headless dispatches.

5. **Worktree-removal-after-merge requires explicit cleanup.** `gh pr merge --squash --delete-branch` deletes the REMOTE branch but fails to delete the LOCAL branch when a worktree still references it. Pattern: after merge, `git worktree remove --force <path> && git branch -D <branch>`. This recurred 4 times this session — solid reason to standardize a post-merge cleanup helper.

6. **Stale `.git/index.lock` files from background `git fetch` calls.** Observed twice this session — background CI watchers triggered concurrent fetches that left zero-byte lock files. Clean rm + retry resolves. If this pattern persists, consider centralizing fetch through a serialized helper or removing the lock-file pattern from concurrent watchers.

## One-paragraph context dump for fresh-session resume

Pt 15 closed with V7.2 leg HALF-SHIPPED: Steps 1-4 all landed (PRs #2391/#2392/#2393/#2394, 4 squash merges into main at `475e19acc5`), zero autopsies, zero regressions, ~50-minute wall-clock from Steps-1/2/3 parallel fire to Step-4 merge. The universal-rules registry (`scripts/build/universal_rules/` + `scripts/build/universal_rules_registry.py`) exists with 15 fragments, 34 unit tests, design doc at `docs/best-practices/universal-rules-registry.md`. The PR #2394 design judgment is sound; orchestrator merged with one minor scope-refinement note (R-NO-CHILDREN-PRIMARY-QUOTES level scope, deferred) and a rejection of all 3 invited schema additions (severity / gate_name / reviewer_phrasing — all redundant with existing telemetry or better handled in Step 5's template wrapper). Session paused at user request to evaluate registry design + restart with Opus 4.8 (just released). Next session resolves Q2 (schema as-is vs add fields) + Q3 (keep 3-way slot cut vs promote 3 A1 writer.body rules to shared.contract) — defaults are "as-is" and "promote" respectively. After Q2/Q3, draft Step 5 brief at `docs/dispatch-briefs/2026-05-29-v7.2-step5-prompt-generator-codex.md` and fire codex (recommended over claude headless — Step 5 is mechanical implementation, not design judgment; reserve claude for Step 6 reviewer single-source emit which IS judgment-heavy). Step 5 generator must compose writer prompt from (preamble + body + shared.contract) and reviewer prompt from (rubric + shared.contract), single Obligation Checklist consumed by both prompts + `wiki_coverage_gate` parser, backward-compatible with `--use-generator` flag during migration. Don't ship today's m20 build artifact at `.worktrees/builds/a1-my-morning-20260528-122552` — Step 7 rebuilds under the clean pipeline post-Step-6 as a fresh build (not `--resume`).

End of 2026-05-28 Pt 15. V7.2 Steps 1-4 anchored. Hand off to Opus 4.8.
