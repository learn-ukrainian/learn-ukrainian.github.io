# Session Handoff: 2026-04-07 (A2 Pipeline Overhaul)

## What was accomplished (26 commits)

### Pipeline architecture — wiki replaces RAG
- Research step uses wiki articles for ALL tracks (core + seminar). `_build_core_packet` (RAG) removed.
- Knowledge packet budget raised 8K→30K chars in ALL code paths (single-call, chunked, skeleton).
- Pre-verify dropped textbook search — keeps VESUM, pravopys, style guide, CEFR only.
- Write template reframed: "Wiki Teaching Brief — Your Authoritative Source" with anti-verbatim constraint.

### Review loop fixes
- Section rewrite fallback REMOVED — deterministic find/replace fixes only. LLM rewrites were degrading scores (8.2→7.8).
- R2 only when R1 < 9.0. Above 9, apply fixes and accept.
- Hard fail at score < 7.0 — pipeline halts, doesn't publish broken modules.
- Gemini probe + 5-retry loop REMOVED — single dispatch, 15 min timeout.
- Word count moved OUTSIDE content block in review prompt (reviewer was flagging it as "AI artifact" and misestimating word count).

### Write quality improvements
- Write retries: "Fix ONLY the listed errors" not "FROM SCRATCH".
- Plan-point checklist injected at END of write prompt (recency effect).
- Teacher voice: "Let us look at..." is fine. Only penalize: self-congratulatory, gamified, empty filler.
- INJECT_ACTIVITY markers normalized to kebab-case before activities step (was breaking inline injection).
- Chunked write path fixed: was missing persona, had 8K truncation, stale "NO meta-commentary" rule.

### Persona system
- Single source of truth: `config_tables.py:TRACK_PERSONAS` (22 tracks matching curriculum.yaml).
- Core levels: "Lead Ukrainian Instructor" with evolving flavor (Patient Guide → Demanding Colleague).
- Seminar tracks: distinct specialist voices (Professor of History, Folklore, etc.).
- `_resolve_persona()` + `_get_persona_description()` imported by v6_build.py — no duplicates.

### Textbook references (#1172)
- `scripts/build/textbook_refs.py` — resolves wiki Source N → chunk_id → pidruchnyk.com.ua PDF#page=N.
- 25/36 textbooks on pidruchnyk (direct PDF deep links), 11 on shkola.in.ua (fallback).
- Plan references in Джерела section auto-linkified via `_resolve_textbook_url()` in enrich.py.

### Other
- GEMINI.md rewritten with current architecture (wiki, SQLite, no RAG).
- A2 stress annotations skipped in prose (словník only). A1 keeps all-word stress. B1+ vocab only.
- A2 old artifacts cleaned (39 research packets, 35 vocab, 35 activities, build-stats).
- Multi-line fix paragraph destruction bug fixed (Gemini code review finding).
- textbook_refs.py NoneType crash fixed.
- #1164 closed (AI noise in wiki — was false alarm, rewrite loop disabled).
- #1173 closed (match-up item count — false alarm, parser handles pairs correctly).

### Bug fixes from Gemini adversarial code review
- Multi-line fix was saving whitespace-normalized content → destroyed all paragraph breaks.
- Marker normalization: case-insensitive "Items" strip, added re.IGNORECASE.
- textbook_refs.py: `page < None` TypeError when first chunk has no page number.
- Low-score modules (< 7.0) now halt pipeline instead of silently publishing.

## Actively in progress (user running)
- **A2 module builds** — module 1 (a2-bridge) completed and passes review at 9.7/10.
- User is building remaining A2 modules (2-69) with:
  ```bash
  .venv/bin/python scripts/build/v6_build.py a2 {N} --writer=gemini-tools --reviewer=gemini-tools
  ```

## DO NOT TOUCH
- Do not modify any A2 orchestration, review, or content files while user is building.
- Do not rebuild or re-review a2-bridge — it's done (9.7/10 on R3).

## A2 build status
- 69 plans ✅ | 69 discovery ✅ | 69 wiki articles ✅ | Pipeline ready ✅
- Module 1 (a2-bridge): DONE — 9.7/10
- Modules 2-69: user building

## Key architecture to know
1. **No Qdrant/RAG** — everything in `data/sources.db` (SQLite FTS5). MCP at port 8766.
2. **Wiki = knowledge source** — `wiki/grammar/a2/*.md` (69 articles, 134K words).
3. **Personas** in `scripts/pipeline/config_tables.py:TRACK_PERSONAS` — single source of truth.
4. **Review**: deterministic fixes only, R2 if R1 < 9.0, hard fail < 7.0.
5. **A2 stress**: none in prose, словník only.
6. **Textbook links**: auto-generated in Ресурси tab via `textbook_refs.py`.

## Open issues
| # | Title | Priority |
|---|-------|----------|
| #1122 | Fix A2 build pipeline + rebuild first 3 modules | Active — pipeline done, building |
| #1172 | Textbook URLs in references | Core done, URL validation remaining |
| #1142 | v6 refactor (BuildContext) | Later |
| #1148 | Activity profiles | Later |
| #1149 | Site design | Later |
| #1150 | Wiki all levels | C1/C2 partial, folk partial |

## What to do next session
1. **Continue A2 batch** if user hasn't finished all 69.
2. **A1 re-publish** — run publish step on all 55 A1 modules to add textbook links:
   ```bash
   for i in $(seq 1 55); do .venv/bin/python scripts/build/v6_build.py a1 $i --step publish --resume; done
   ```
3. **Clean dead code** — `_rewrite_weak_sections()` and `_get_knowledge_packet_for_rewrite()` in v6_build.py are unused.
4. **A1 rebuild decision** — compare A2 quality against A1, decide if A1 needs wiki-based rebuild.
