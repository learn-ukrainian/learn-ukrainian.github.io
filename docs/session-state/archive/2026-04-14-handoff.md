# Session Handoff — 2026-04-14 (closing state)

## Status at close

**Issues #1228 and #1229 both CLOSED.** All in-scope work shipped. A1 live-status badge delivered; infra debt cleared; seminar research-only tracks archived.

---

## Commits this session (11 total)

### P0 — Landing real-time module status (user-explicit critical priority)
| SHA | Description |
|---|---|
| `465d80a52` | LiveStatus component — fetches `/api/state/module/{track}/{num}` |
| `6f16d53b0` | Gemini review fixes: prod-mode skip, aria-label, derivation order |
| `1f38cc891` | Gemini re-review fix: publish-phase shadow bug |

**Browser-verified on `/a1/`:** 31 ✅ / 23 🔄 / 1 ❌ matches `/api/state/track-health/a1` exactly. The 1 ❌ is `many-things` (user is currently rebuilding).

### P1 — Infrastructure cleanup
| SHA | Description |
|---|---|
| `b4da2d38c` | **P1.1** — remove retired v4 API branches (Codex direct) |
| `ac1c00548` | **P1.2** — wiki quality_gate, cmd_review_existing, dashboard `read_status()`, review_validation v5/v6 doc |
| `01880787a` | **P1.3** — dispatch timestamp collisions, sqlite leak, dead subprocess branch |
| `2d78acd81` | Regression tests for all P1 fixes (171 pass) |

### Cleanup
| SHA | Description |
|---|---|
| `50efc0928` | Handoff correction — original "0 of 1,093 use legacy pipelines" claim was wrong |
| `78a2c2a8a` | Archive 3 orphan dirs: `lit-crimea`, `lit-doc`, `a1-backup-2026-04-08` |
| `e68068f98` | Migration script (v5/v3 → v6 state schema, dry-run) — shelved, kept for future use |
| `c45d7a3c1` | **Archive 12 seminar research-only tracks** — bio, istorio, lit, ruth, oes, lit-essay, lit-youth, lit-war, lit-hist-fic, lit-fantastika, lit-humor, lit-drama |

---

## Key decisions made

1. **Do NOT migrate v5/v3 state schemas.** Codex's manifest audit showed 936 v5 + 564 v3 state files alive, but discovery revealed **every seminar track has 0 shipped lesson `.md` files** — they're research-phase artifacts only. Migrating would be busywork; the wiki-to-module pipeline (#1136) will build seminars fresh from wiki articles.
2. **Archived 12 seminar tracks under `_archive/`.** Plans stay live in top-level `plans/{track}/*.yaml`. `folk` stayed live (first wiki-build test, has real content).
3. **A2 reviewer = `codex-tools`** going forward (switched back from Gemini self-review; capacity is back).

---

## Open items (for next session)

### A1 — `many-things` hotfix
**Status: user-started.** Build queued at `queued-after-write`. Should land on `/a1/` live-status as ✅ when publish completes. Nothing for next session to do unless it fails.

### A2 rebuild
**Needs batching decision before starting.** 69 modules, 0/58 audited passing.

Command template (with the new reviewer):
```bash
.venv/bin/python scripts/build/v6_build.py a2 <N> --range <M> --resume \
  --writer gemini-tools --reviewer codex-tools
```

Open questions for user:
- Full `--range 1-69` or chunks of 10?
- Start outside 14–20 CET peak window?

### Seminar builds (future, #1136)
Wiki-to-module pipeline not yet shipped. When it is:
- Seminar plans live in `plans/{bio,istorio,lit,ruth,oes,…}/*.yaml` — consume those.
- Fresh `curriculum/l2-uk-en/{track}/` dirs will be produced by v6; archived research artifacts are available as priors if useful.

### Follow-up opportunity (not urgent)
Now that only v6 state files exist outside `_archive/`, the v5/v3 parsing branches in `scripts/api/state_*.py` and `scripts/generate_mdx/core.py` can be removed. Was blocked before; unblocked now. File as a new issue when convenient.

---

## What's on disk uncommitted (pre-existing, not mine)

Lingering modified files in the working tree from prior session:
- `.coverage`
- `curriculum/l2-uk-en/a1/audit/sentence_hashes.json`
- `curriculum/l2-uk-en/a1/orchestration/my-day/v6-review-prompt.md`
- `curriculum/l2-uk-en/a1/orchestration/weather/index.md` + `v6-review-prompt.md`
- `curriculum/l2-uk-en/a1/review/weather-review.md`
- `curriculum/l2-uk-en/a1/weather.md`
- `starlight/src/content/docs/a1/weather.mdx`
- several untracked dispatch-meta JSONs and review-structured YAMLs under `a1/orchestration/weather/`

These are from the user's previous build session (weather rebuild), not this session's work. Decide whether to commit, stash, or revert.

---

## Tools & patterns that earned their keep

- **Cross-agent review via channels** (`ab post reviews "…" --to gemini|codex --mode workspace-write`). Codex caught Claude mistakes (v5/v3 premise); Gemini caught Codex mistakes (two real shipping bugs in LiveStatus).
- **Claude-in-Chrome browser verification**. Inspected live DOM on `/a1/` to confirm badge counts match `/api/state/track-health/a1` after each fix.
- **Hard-data manifest audit before code removal**. Writing a 30-line Python scanner to count mode=v5 vs v6 saved us from deleting 936 live state files.

## Patterns that failed

- **900s hard_timeout on workspace-write deliveries**. Big Codex tasks time out mid-commit, leaving work stranded on disk. Fix: split into sub-tasks ≤10 min each, or raise the timeout. Both P1 batched and first P1 rescoped tasks were affected.
- **Old handoff premise** ("0 of 1,093 use legacy") was fabricated from a bad scanner. Codex's pushback with real numbers saved the day. Lesson: always ground strategic claims in live manifest queries, not memory.
