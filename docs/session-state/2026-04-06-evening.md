# Session State: 2026-04-06 Evening

## What was accomplished

### 1. Wiki compiler fixes (#1164)
- Dedup overlapping H2 sections in rewrite responses
- Removed 15K char truncation (root cause of review oscillation)
- Timeouts bumped to 900s
- Skip-review for already-passed articles (review-final.md marker)
- Markdown fence stripping from compilation output

### 2. External source auto-matching (#1165) — CLOSED
- Keyword-based FTS5 search for external articles (no more manual YAML mapping)
- Word boundary matching via regex
- Dedup against YAML-mapped URLs
- Orphaned YAML keys fixed (slug normalization)

### 3. SQLite FTS5 database (#1167) — CLOSED
- Unified database replaces JSONL-into-memory approach
- Textbooks (24K) + external articles (1.2K) in FTS5
- v6 research step migrated from RAG to FTS5 (85x faster: 0.1s vs 9.9s)
- 8 new tests + 23 existing tests pass

### 4. Full RAG replacement (#1168) — IN PROGRESS
- Architecture reviewed by Gemini: separate tables per collection
- Standard indexed tables for dictionaries, FTS5 for prose
- build_sources_db.py rewritten to ingest ALL collections
- DB build running: textbooks + external + literary + 9 dictionaries
- **BUILDING NOW** — check `data/sources.db`

### 5. Review loop degradation (#1169) — FILED
- Evidence: 0/5 rewrites improved scores, all degraded
- Fix: disable rewrite loop, recompile from scratch instead
- Not implemented yet — blocked on wiki rebuild

### 6. Effort reduction
- Removed `effort: high` from 5 review skills (saves tokens)

## What is IN PROGRESS

1. **DB build running** — `data/sources.db` being built with all collections (~670K entries)
2. **A2 wiki articles deleted** — need rebuild after DB is ready
3. External sources download just finished — new content available

## What needs doing next

### Immediate (after DB finishes building):
1. Update `sources_db.py` with query functions for all new tables
2. Wire MCP server to use SQLite instead of Qdrant
3. Remove Qdrant from startup, Claude config, Gemini config
4. Rebuild A2 wiki: `.venv/bin/python scripts/wiki/compile.py --track a2 --all`
5. Fix review loop (#1169) before running `--review`

### Open issues:
- #1142 — v6 refactor
- #1148 — activity profiles
- #1149 — site design
- #1150 — wiki all levels (A2 wiki articles needed)
- #1151 — external ingestion (GDrive backup remaining)
- #1161 — prompt redesign (3/5 validation passed)
- #1164 — AI noise cleanup (post-batch)
- #1166 — Pyright type warnings
- #1168 — full RAG replacement (Phase 1 running, Phases 2-4 pending)
- #1169 — review loop fix

## Key files changed this session
- `scripts/wiki/compile.py` — dedup, truncation, timeout, skip-review, fence strip
- `scripts/wiki/enrichment.py` — FTS5 search replaces JSONL loading
- `scripts/wiki/build_sources_db.py` — unified DB builder (all collections)
- `scripts/wiki/sources_db.py` — FTS5 query functions
- `scripts/wiki/compiler.py` — fence stripping
- `scripts/research/build_knowledge_packet.py` — FTS5 replaces RAG
- `claude_extensions/skills/*/SKILL.md` — removed effort: high
- `docs/resources/external_resources.yaml` — fixed orphaned keys
- `tests/test_sources_db.py` — 8 new tests
- `tests/test_wiki_compiler.py` — 3 new tests (74 total)
