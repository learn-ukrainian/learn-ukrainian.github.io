# Task: #1340 — re-validate #1330 diagnostic against new T1-T2 pipeline

**Prerequisites verified**: #1348 stages (a)-(d) landed (`428be007b`, `59a419228`, `9e86a33a0`, `83dc10279`). #1337 schema shipped (`45432e7db`). Cold encode of ~160K units runs separately on user's Mac — when that completes, `data/embeddings/manifest.db` + `.npy` shards exist and `search_sources(strategy="unified_dense")` returns real results.

Full spec: `gh issue view 1340`. ACs are AC1–AC5 in the issue body.

**Do NOT run this dispatch until cold encode completes.** Check `data/embeddings/manifest.db` exists with non-empty `embedding_shards` table before firing. Otherwise the modern strategy returns degenerate zero-scored results.

## Scope — exactly this

1. **Patch `scripts/wiki/diagnostics/retrieval_playback.py`** — add a `--strategy` flag
   - Values: `legacy_chunk` (default, existing behavior using `search_textbooks`) and `modern_dense` (new, using `search_sources(strategy="unified_dense")`)
   - Note: #1340 issue body calls the modern value `modern_dense_section`. Stage-c shipped `strategy="unified_dense"` as the actual API. Use `--strategy modern_dense` in CLI, map internally to `search_sources(strategy="unified_dense", track=track, limit=40)`.
   - The `legacy_chunk` path stays identical to today's behavior — no regression to the existing tests in `tests/wiki/test_retrieval_playback.py`
   - The `modern_dense` path calls `search_sources` and adapts its return format to the shape `match_returned_concepts` expects (list of dicts with `text`, `chunk_id`, `grade`, `source_file`, `author`, `title`, `score`/`rank`)

2. **Output file separation**
   - Default output: `wiki/.reviews/diagnostics/a1-sounds-letters-playback.{json,md}` (existing)
   - When `--strategy modern_dense`: write to `wiki/.reviews/diagnostics/a1-sounds-letters-playback.modern.{json,md}` (suffixed)
   - When `--strategy legacy_chunk`: unchanged paths (existing)

3. **Comparison report** — new file `wiki/.reviews/diagnostics/a1-sounds-letters-comparison.md`
   - Generated by a new CLI mode: `.venv/bin/python scripts/wiki/diagnostics/retrieval_playback.py --track a1 --slug sounds-letters-and-hello --compare`
   - Reads both `legacy.json` and `modern.json`, produces a 10-concept × 2-strategy table:
     ```
     | Concept | Legacy (present in returned 40?) | Modern (present in returned 40?) |
     |---|---|---|
     | syllable_count_rule | yes | yes |
     | larynx_touch_exercise | no | yes |
     | ... |
     ```
   - Headline metrics: `legacy_concepts_present: N/10`, `modern_concepts_present: M/10`
   - **Verdict**: `PASS if modern ≥ 8/10, FAIL if modern < 8/10`
   - If `--compare` is passed without both output files present, error with helpful message asking user to run both strategies first.

4. **Run the real diagnostic against real DB** (this is the validation itself)
   - Run `legacy_chunk` → saves to `a1-sounds-letters-playback.json` (overwrites existing; baseline was 6/10)
   - Run `modern_dense` → saves to `a1-sounds-letters-playback.modern.json`
   - Run `--compare` → writes comparison report
   - Commit all three outputs

5. **AC3 / AC4 branching based on result**
   - **If modern ≥ 8/10 (PASS)**: update `docs/architecture/adr/adr-006-compile-layer-retrieval.md` Revision block with "validated against #1330 diagnostic, modern retrieval hits N/10 concepts." Comment on #1335 (EPIC) checklist. Close #1340.
   - **If modern < 8/10 (FAIL)**: do NOT close any issue. Add a comment on #1340 with the failing concepts, file a follow-up issue titled "Embedder bakeoff: BGE-M3 vs Qwen3-Embedding-4B vs intfloat/multilingual-e5-large-instruct on T1-T2 corpus" that references the same 10 concepts as ground truth. Do NOT proceed to stage (e)/batch builds.

## Ground rules

- Branch: `main`. No worktrees.
- `.venv/bin/python`, `.venv/bin/ruff`, `.venv/bin/pytest` — never bare.
- Commit: `feat(diagnostics): add modern_dense strategy + #1340 comparison report (#1340)`
- After commit lands: run the diagnostic (two strategies + compare), commit the output files separately as `chore(diagnostics): #1340 validation report on a1/sounds-letters-and-hello`
- Comment on #1340 with: commit SHAs, comparison verdict (PASS/FAIL), ≥8/10 count

## Reading before coding

1. `gh issue view 1340` — full spec
2. Existing `scripts/wiki/diagnostics/retrieval_playback.py` — 500 LOC, current single-strategy implementation
3. `tests/wiki/test_retrieval_playback.py` — existing tests must survive
4. `scripts/wiki/sources_db.py::search_sources` — the multi-corpus entry point you're calling in modern_dense mode

## File scope (allow-list)

Modify:
- `scripts/wiki/diagnostics/retrieval_playback.py` — add --strategy flag + --compare mode + output-file routing
- `tests/wiki/test_retrieval_playback.py` — extend with modern_dense and compare-mode tests

New (generated by running the diagnostic, commit them):
- `wiki/.reviews/diagnostics/a1-sounds-letters-playback.json` (legacy, overwrites existing)
- `wiki/.reviews/diagnostics/a1-sounds-letters-playback.md` (legacy, overwrites existing)
- `wiki/.reviews/diagnostics/a1-sounds-letters-playback.modern.json` (new)
- `wiki/.reviews/diagnostics/a1-sounds-letters-playback.modern.md` (new)
- `wiki/.reviews/diagnostics/a1-sounds-letters-comparison.md` (new)

Conditional:
- `docs/architecture/adr/adr-006-compile-layer-retrieval.md` — only if PASS (record validation)

**Do NOT modify**:
- `scripts/wiki/sources_db.py`, `scripts/wiki/dense_rerank.py`, `scripts/wiki/enrichment.py` (stage-c code)
- `data/embeddings/manifest.db` or `.npy` shards
- `data/sources.db`
- `curriculum/`, `plans/`, `orchestration/`

## Acceptance criteria (summary of #1340 ACs)

- AC1: `--strategy` flag with legacy_chunk + modern_dense values
- AC2: `a1-sounds-letters-comparison.md` with 10-concept × 2-strategy table, PASS/FAIL verdict
- AC3: if PASS → close #1340 with comment
- AC4: if FAIL → file embedder-bakeoff follow-up, do NOT close
- AC5: ruff + pytest green, comparison output deterministic given the same DB snapshot

## Done-when

- Two commits landed (code + diagnostic output)
- Comparison report committed with real ≥8/10 verdict OR <8/10 with bakeoff issue filed
- #1340 closed (PASS) or commented with failing concepts + bakeoff issue filed (FAIL)
- All existing tests green; new tests cover --strategy + --compare modes
