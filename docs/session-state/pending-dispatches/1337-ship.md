# Task: #1337 — actually ship the schema that was nominally closed

**Status context**: #1337 was marked closed on the assumption `scripts/wiki/extract_sections.py` (925 LOC, written 2026-04-19) constituted completion. In reality the schema was NEVER migrated into production `data/sources.db`, and the extractor was never wired into `scripts/wiki/build_sources_db.py`. Verified:
- `PRAGMA table_info(textbooks)` → NO `parent_section_id` column
- `textbook_sections` table → does not exist
- No call-site for `extract_sections.py` in `build_sources_db.py` or anywhere else in the build pipeline

This is blocking #1348 stage (c) — `sources_db.py::_search_sections_fts5` references `s.parent_section_id` which crashes in production.

Full spec: `gh issue view 1337`. ACs AC1–AC6 already defined there — match them.

## Scope — exactly this

1. **Audit existing `extract_sections.py`** — confirm the schema-creation SQL and extraction algorithm match #1337 AC1/AC2/AC3. Fix any drift, minimal change only.
2. **Run the extractor against production `data/sources.db`** — inside a SQLite transaction so mid-way failure doesn't leave partial schema. Produce `data/corpus_audit/section_extraction_report.md` per AC4.
3. **Gate on unassigned rate**: if >20% of chunks end up without `parent_section_id`, STOP, report the sample, do NOT commit schema changes. Per AC4 this is a BLOCKER.
4. **Wire extract_sections into `build_sources_db.py`** — end-of-build hook so future DB rebuilds include the parent-section pass automatically. Idempotent (safe to re-run).
5. **Rollback script** — `scripts/wiki/rollback_sections.py` that drops `textbook_sections` table + `parent_section_id` column (AC6 safety requirement; may already exist from the 2026-04-19 work — verify).
6. **Regression tests** — `tests/wiki/test_extract_sections.py` per AC5. In-memory SQLite fixtures, idempotency test, backfill test, edge-case fixtures. If tests already exist, review them against AC5 and add anything missing.
7. **Ruff + pytest green** on touched files.

## Ground rules

- Branch: `main`. No worktrees.
- `.venv/bin/python`, `.venv/bin/ruff`, `.venv/bin/pytest` — never bare.
- `git add` allow-list only — files named in the file scope below. No `-A`, no `-u`.
- Commit: `feat(sources): ship #1337 schema + extraction for textbook parent sections`
- Comment on #1337 with: commit SHA, extraction report summary (total chunks, assignment rate, sections created, unassigned count), path to `data/corpus_audit/section_extraction_report.md`.
- Reopen #1337 if it was re-closed; leave open after this commit until user re-verifies in the morning.

## Reading before coding

1. `gh issue view 1337` — full spec
2. `scripts/wiki/extract_sections.py` — existing extraction logic, 925 LOC
3. `scripts/wiki/build_sources_db.py` — where the integration hook goes (end of main build function)
4. Stage (c) commit `9e86a33a0`, specifically `scripts/wiki/sources_db.py:196,214` — the consumers of `parent_section_id` that are currently crashing
5. `data/sources.db` — production database, must survive migration

## File scope (allow-list)

Modify:
- `scripts/wiki/extract_sections.py` (if audit finds drift from ACs)
- `scripts/wiki/build_sources_db.py` (integration hook)
- `tests/wiki/test_extract_sections.py` (new or extended)

New (if not present):
- `scripts/wiki/rollback_sections.py` (per AC6)
- `data/corpus_audit/section_extraction_report.md` (generated output; commit it)

Modifies production DB:
- `data/sources.db` (schema migration + backfill; NOT committed, it's gitignored)

**Do NOT modify**:
- Any `text` / `title` / `section_title` column value on existing rows (schema-only additions; backfill populates NEW column, doesn't mutate existing text)
- `scripts/wiki/sources_db.py` (stage-c consumer; out of scope — it's already expecting the column, will just start working once schema lands)
- `curriculum/**`, `plans/**`, `orchestration/**`
- Any other `*.db` files

## Acceptance criteria (summary of #1337 ACs with dispatch-specific notes)

- **AC1**: `textbook_sections` table exists in production `data/sources.db` with the schema from #1337 AC1
- **AC2**: `textbooks.parent_section_id` column added with REFERENCES to textbook_sections(section_id); NULL allowed
- **AC3**: Extraction algorithm correctly groups by `(source_file, section_title)` with normalization of leading section markers (§, chapter numbers, Roman numerals)
- **AC4**: `data/corpus_audit/section_extraction_report.md` written with assignment stats; <20% unassigned rate
- **AC5**: Tests green on synthetic fixtures (including idempotency + backfill + edge cases)
- **AC6**: Ruff clean; rollback script present; migration safe (transactional); no DROP TABLE on production

## Out of scope

- Literary sections (#1341)
- Search / retrieval code changes (#1348 stage-c handles consumption)
- Running #1340 re-validation (separate dispatch after this lands)
- Fixing #1339 grade filter bug (separate dispatch after this lands)

## Done-when

- Single commit on `main` with the title above
- `pytest tests/wiki/test_extract_sections.py -v` green
- `ruff check` clean on touched files
- Production `data/sources.db` has the schema (verify: `PRAGMA table_info(textbooks)` shows `parent_section_id`; `SELECT COUNT(*) FROM textbook_sections` > 0)
- `data/corpus_audit/section_extraction_report.md` committed with real stats
- #1337 commented with commit SHA + report summary
- `pytest tests/wiki/test_multi_corpus_retrieval.py tests/wiki/test_cold_encode.py -v` still green (stage-c tests must survive the schema change)
