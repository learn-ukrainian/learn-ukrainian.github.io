---
date: 2026-05-28
session: "Pt 14 — V7.2 leg set up. Bridge fix PR #2381 merged. R1+R2 fix PR #2382 merged. V7.1+codex apples-to-apples re-fire produced clean 1505w A1 lesson + 25 tool calls / 12 distinct MCP tools + all python_qg PASS + wiki_completeness PASS, halted only at wiki_coverage_gate (stale gate vs V7.1 inline tags). User audit caught m20-specific contamination baked into linear-write.md template (5x прокидаюся, 3x Захарійчук, build-history paragraph at line 105, chunk_id at line 461). Multi-agent discussion (codex+cursor+gemini, unanimous Vote A) converged on deterministic Python prompt generator from (universal rules + wiki + RAG). User decision: do NOT ship today's contaminated-template m20 artifact; rebuild it under the clean wiki-driven pipeline + ship that as the proper anchor. V7.2 leg planned with ADR + 8 GH issues + 4 ready-to-fire dispatch briefs."
status: v7.2-leg-planned-ready-to-execute-next-session
main_sha: e9ac12f532
main_green: yes
working_tree_dirty: see "Untracked artifacts" below
session_close: 2026-05-28 evening
---

# 2026-05-28 — Part 14: V7.2 leg set up, architecture decided, ready to execute next session

**Read TL;DR. Read the decision + sequence. Then cold-start the V7.2 leg.**

## TL;DR

After Pt 13's bridge fix + Pt 14's R1+R2 fix landed, the V7.1+codex apples-to-apples re-fire produced empirically strong writer behavior (clean A1 content, full corpus utilization, all subjective gates pass) but halted at `wiki_coverage_gate` due to gate-vs-prompt drift (stale parser doesn't recognize V7.1 self-closing inline `<implementation_map />` tags).

Investigation surfaced **structural contamination** in the static writer prompt template: m20-specific Ukrainian vocabulary, textbook citation patterns, build-history empirical-evidence paragraphs, and JSON example payloads baked into `scripts/build/phases/linear-write.md`.

User's question crystallized the architecture: *"we should use the wiki to generate the writer and review prompt. for both the prose and the practices."* Refined to: *"we have universal rules plus the wiki of the module, plus the related rag content and we could use those to generate the writer and reviewer prompt."*

Multi-agent discussion (codex + cursor + gemini, unanimous Vote A) confirmed direction + sequencing:
1. Don't ship today's contaminated-template m20 artifact (it would just need re-publishing after the generator lands).
2. Build the deterministic Python prompt generator as V7.1 evolution (NOT V8 greenfield).
3. Rebuild + ship m20 under the clean pipeline as the proper anchor (single ship event).

**V7.2 leg planned with:** ADR at `docs/decisions/pending/2026-05-28-wiki-driven-prompt-generator.md`. 8 GH issues (#2383–#2390). 4 dispatch briefs ready to fire. ~1 week pilot scope (A1 m21+m22), ~1.5-2 weeks full parity.

## What's at e9ac12f532 (main right now)

This session's commits:
- `57ceb31ea1` — docs(session-state): 2026-05-28 Pt 12-13 + bridge fix + R1+R2 dispatch briefs + `~/.cursor/*` allowlist (committed inline, pushed directly to main)
- `e5b8cbdaeb` — fix(bridge): cursor-agent v2026.05.27 JSONL response parsing + session-log fallback (**PR #2381** merged)
- `e9ac12f532` — fix(v7.1): silent pipeline exit + restore translate activity schema directive (**PR #2382** merged)

Local main and origin/main both at `e9ac12f532`.

## V7.2 leg — what to read first

| Artifact | Path | Purpose |
|---|---|---|
| **ADR (architecture)** | `docs/decisions/pending/2026-05-28-wiki-driven-prompt-generator.md` | Full architectural decision + Q1-Q4 answers + sequencing |
| **Discussion thread** | `audit/2026-05-28-wiki-driven-prompt-generation/question.md` + sqlite channel `wiki-driven-prompt-generation-2026-05-28` thread `1241c73bf87c4e29bea394fd5998506a` | Full codex+cursor+gemini reasoning |
| **Step 1 brief** (codex) | `docs/dispatch-briefs/2026-05-28-v7.2-step1-purge-m20-literals-codex.md` | Cleanup PR (bleed-stop) |
| **Step 2 brief** (codex) | `docs/dispatch-briefs/2026-05-28-v7.2-step2-review-context-passthrough-codex.md` | review_context() wiki/manifest pass-through |
| **Step 3 brief** (codex) | `docs/dispatch-briefs/2026-05-28-v7.2-step3-wiki-coverage-gate-inline-tags-codex.md` | Unblock current m20 build at gate level |
| **Step 4 brief** (claude headless) | `docs/dispatch-briefs/2026-05-28-v7.2-step4-rule-registry-scaffold-claude.md` | Universal rules registry scaffold |

## V7.2 leg — GH issues + execution order

Issues created with `v7.2` label:

| # | Step | Title | Agent | Effort | Depends on |
|---|---|---|---|---|---|
| **#2383** | 1 | Purge m20 literals from linear-write.md + leakage sentinel test | codex | ~2h | — |
| **#2384** | 2 | Pass wiki_manifest + implementation_map_contract into review_context() | codex | ~30-60min | — |
| **#2385** | 3 | wiki_coverage_gate accepts V7.1 self-closing inline tags | codex | ~30-60min | — |
| **#2386** | 4 | Rule registry scaffold (universal_rules/) | **claude headless** | 1-2 days | #2383 |
| **#2387** | 5 | prompt_generator.py — compose writer+reviewer prompts | **claude headless** OR codex | 2-3 days | #2386 |
| **#2388** | 6 | Reviewer obligation emission from same registry | codex | ~1 day | #2387 |
| **#2389** | 7 | Rebuild m20 under clean pipeline + ship as anchor | orchestrator inline | ~30min | #2388 |
| **#2390** | 8 | Pilot m21 + m22 under generator | orchestrator + codex | ~1-2h each | #2389 |

**Steps 1+2+3 can fire in parallel** (independent surfaces; bleed-stop trio). Step 4 needs Step 1 to land first (so the registry extracts cleaned content). Step 5 needs Step 4. Steps 6-8 sequential.

## Cold-start sequence for next session

1. Fetch + read `docs/decisions/pending/2026-05-28-wiki-driven-prompt-generator.md` — the architecture decision.
2. Confirm Claude headroom: `curl -s http://localhost:8765/api/state/routing-budget | jq` (if available) OR ask user. If headroom is tight, Step 4 substitutes with codex+orchestrator-guidance.
3. Fire Steps 1+2+3 dispatches in parallel (all codex, all small, no cross-dependencies):
   ```bash
   # Step 1 — cleanup
   .venv/bin/python scripts/delegate.py dispatch --agent codex --task-id v7.2-step1-purge-m20-literals-2026-05-XX \
     --prompt-file docs/dispatch-briefs/2026-05-28-v7.2-step1-purge-m20-literals-codex.md \
     --mode danger --worktree --model gpt-5.5 --effort xhigh --silence-timeout 5400

   # Step 2 — review_context pass-through (parallel-safe)
   .venv/bin/python scripts/delegate.py dispatch --agent codex --task-id v7.2-step2-review-context-passthrough-2026-05-XX \
     --prompt-file docs/dispatch-briefs/2026-05-28-v7.2-step2-review-context-passthrough-codex.md \
     --mode danger --worktree --model gpt-5.5 --effort high --silence-timeout 5400

   # Step 3 — wiki_coverage_gate inline tags (parallel-safe)
   .venv/bin/python scripts/delegate.py dispatch --agent codex --task-id v7.2-step3-wiki-coverage-gate-inline-tags-2026-05-XX \
     --prompt-file docs/dispatch-briefs/2026-05-28-v7.2-step3-wiki-coverage-gate-inline-tags-codex.md \
     --mode danger --worktree --model gpt-5.5 --effort high --silence-timeout 5400
   ```
4. **Monitor codex rollouts properly** (do not repeat Pt 14's silence-timeout-killed-codex-during-push mistake). For each dispatch, tail the scoped CODEX_HOME rollout JSONL via `Monitor` tool. Path is set by adapter; check `jq -r .cwd batch_state/tasks/<task>.json` to find the worktree, then look for `$TMPDIR/codex-v7-writer-501/sessions/<YYYY>/<MM>/<DD>/rollout-*.jsonl` OR `~/.codex/sessions/...`.
5. When PRs open, run scope check + tests + merge (each is small, ~5 min review).
6. After Step 1 lands, fire Step 4 dispatch (claude headless). Monitor via either Claude rollout transcript OR `/api/delegate/active` polling.
7. After Step 4 lands, evaluate registry design with user before firing Step 5 (the larger generator implementation).

## What NOT to do next session

- ❌ Do not ship today's m20 artifact at `.worktrees/builds/a1-my-morning-20260528-122552`. It used the contaminated template and will be superseded by Step 7's rebuild.
- ❌ Do not bundle charter/rubric changes into Steps 1-6 (cursor's sequencing discipline). Tone reviewer rubric calibration is a separate post-V7.2 question.
- ❌ Do not fire Step 5 before Step 4 lands + design review passes — the registry shape needs to be right before the generator commits to it.
- ❌ Do not fire Claude headless without quota check — per #M0 it competes with the user's interactive seat.

## Active dispatches at session close

**Zero.** All dispatches completed and merged this session.

## PRs landed this session

- **#2381** (bridge cursor JSONL parser fix) — merged
- **#2382** (R1 silent pipeline exit + R2 translate schema directive) — merged

## Build worktrees on disk (per #M-10 forensic-keep)

```
.worktrees/builds/a1-my-morning-20260527-163310    Pt 9 forensic
.worktrees/builds/a1-my-morning-20260527-185032    ⭐ Pt 10 baseline (V7-hardened + codex 9.5/10) — KEEP
.worktrees/builds/a1-my-morning-20260528-122552    ⭐⭐ V7.1+codex+R1+R2-fixed (Step 7's rebuild target — clean writer output, 1505w, 25 tool calls / 12 distinct tools, all python_qg PASS, wiki_completeness PASS, halted at wiki_coverage_gate due to V7.1 inline-tag drift — Step 3's PR unblocks `--resume` of this build)
```

Build branches in local git database for forensic continuity:
```
build/a1/my-morning-20260527-185032    Pt 10 baseline
build/a1/my-morning-20260528-014807    V7.1+claude pilot (Pt 12, failed tone REJECT)
build/a1/my-morning-20260528-060820    V7.1+codex pilot (Pt 13, pipeline-incomplete)
build/a1/my-morning-20260528-122552    ⭐⭐ V7.1+codex+R1+R2 (today, ready for Step 3 --resume)
```

## Issues open at session close

- **#2380** — V7.1 build pipeline regressions surfaced by m20 pilot. R1+R2 fixed via PR #2382. R3 (charter weakening `#R-AUDIENCE-LANGUAGE-A1` → tone REJECT 3.0) + R4 (codex tool-call count) addressed by today's V7.1+codex re-fire data (codex utilization recovered 4→25, charter validated). R5 (wiki_coverage_gate vs V7.1 inline tags) → V7.2 Step 3 #2385.
- **#2378** — V7.1 follow-up: aggressive trim of linear-write.md to drop ceiling back below 130KB. Subsumed by V7.2 Steps 1+4+5 (template will shrink dramatically as content moves to registry).
- **#2368** — `/api/delegate/active` route handler regression — low priority workaround via `/api/orient`.
- **#2369** — PR #2367 scope-creep evaluation (`_contract_yaml` vocabulary fields + dialogue-gloss sentence) — comment added with read-only evidence.

Newly created (V7.2 leg):
- **#2383** — Step 1 cleanup
- **#2384** — Step 2 review_context pass-through
- **#2385** — Step 3 wiki_coverage_gate inline tags
- **#2386** — Step 4 rule registry scaffold
- **#2387** — Step 5 prompt generator
- **#2388** — Step 6 reviewer single-source emission
- **#2389** — Step 7 m20 rebuild + ship
- **#2390** — Step 8 m21+m22 pilot

## Untracked artifacts at session close

These will commit as part of the session-close handoff commit:

```
docs/decisions/pending/2026-05-28-wiki-driven-prompt-generator.md (new — V7.2 ADR)
docs/dispatch-briefs/2026-05-28-v7.1-regressions-1-2-fix-codex.md (from earlier today)
docs/dispatch-briefs/2026-05-28-v7.2-step1-purge-m20-literals-codex.md (V7.2 brief)
docs/dispatch-briefs/2026-05-28-v7.2-step2-review-context-passthrough-codex.md (V7.2 brief)
docs/dispatch-briefs/2026-05-28-v7.2-step3-wiki-coverage-gate-inline-tags-codex.md (V7.2 brief)
docs/dispatch-briefs/2026-05-28-v7.2-step4-rule-registry-scaffold-claude.md (V7.2 brief, claude headless)
docs/session-state/2026-05-28-pt14-v7.2-leg-set-up-architecture-decided.md (this file)
audit/2026-05-28-wiki-driven-prompt-generation/question.md (discussion question)
```

## Behavioral lessons from this session

1. **The user has named the obstruction-shaped pattern explicitly.** Every session I find one more blocker. The cure is to commit to ship-then-fix instead of fix-until-perfect — captured operationally in the V7.2 sequence (rebuild m20 once under the clean pipeline, no interim ship events).

2. **Default ask-permission tic is a real problem.** Three times this session the user told me to drive without asking. The obvious-action-when-data-is-clear discipline (per #M-6) needs to operationalize: when the decision data is in, EXECUTE.

3. **Silence-timeout default of 3600s is too tight for codex xhigh tasks.** Today's R1+R2 dispatch was killed at T+60min during push attempt — codex had committed cleanly at 14:11:00 but went silent during git push, killed at 14:11:19. **Next session: pass `--silence-timeout 5400` (90 min) for all codex xhigh dispatches.** Already encoded in the V7.2 dispatch briefs.

4. **Monitor on rollout JSONL > Monitor on batch_state logs.** Codex batch_state stdout/stderr logs go silent during real work (per #M-8). The right Monitor target is the codex rollout JSONL at scoped CODEX_HOME. Encoded in cold-start sequence above.

5. **The pattern: gate-vs-prompt drift cited 5 times in handoffs this week.** It's a structural class. V7.2 is the structural fix. Single-source emission of obligations across writer prompt + reviewer prompt + gate parser eliminates the drift class.

## One-paragraph context dump for fresh-session resume

If next session is a brand-new orchestrator: Pt 14 closed with 2 PRs merged (bridge fix #2381 + R1+R2 fix #2382), the V7.1+codex apples-to-apples re-fire validated charter direction (25 tool calls / 12 distinct MCP tools, all python_qg + wiki_completeness PASS), today's m20 artifact at `.worktrees/builds/a1-my-morning-20260528-122552` halted at wiki_coverage_gate (stale parser vs V7.1 inline tags), user audit caught m20-specific contamination baked into the static writer prompt template, multi-agent discussion converged unanimously on building a deterministic Python prompt generator from (universal rules + wiki + RAG), user decided NOT to ship today's contaminated-template artifact (rebuild under clean pipeline ships once as anchor of record), V7.2 leg planned with ADR + 8 GH issues + 4 ready-to-fire dispatch briefs. Cold-start sequence above. Default action next session: fire Steps 1+2+3 codex dispatches in parallel; after Step 1 lands, fire Step 4 claude headless dispatch (quota permitting).

End of 2026-05-28 Pt 14. V7.2 leg set up cleanly. Hand off.
