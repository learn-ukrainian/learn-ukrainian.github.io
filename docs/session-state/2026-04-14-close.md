# Session Handoff — 2026-04-14 close (budget-constrained)

## Context

Claude weekly usage hit 90% on Tuesday. Rest of week should run without Claude turns.
**Codex has 10x capacity through 2026-05-17.** Gemini has headroom.
Next Claude availability: Monday after weekly reset.

## What shipped this session (main branch)

| SHA | What |
|---|---|
| `ad5030806` | landing real-time status priority docs |
| `10e97f6dc` | prior handoff |
| `839ebe9e2`, `ee47e80ab`, `1738bb205` | channel-bridge attempt-count fixes |
| `fb3d44258` | **#1251 fix**: ACTIVITY_ORDER matcher accepts descriptive writer IDs against bare-type contract entries |

## Real plateau data from a1 M18

First full run on #1247 contract-first pipeline with #1251 fix applied:

```
Write phase:    4 rounds, passed (1366/1200 words)
Review phase:   R1 8.2 → R2 8.3 (+0.1) — PLATEAU
Dimension floor: Linguistic accuracy 8/10
Raw scores:     [8, 8, 8, 9, 9, 8, 6, 9, 9]
Result:         halted, needs_human_review
```

This confirms the plateau hypothesis and fixes the scope of #1252.

## Open work with no Claude dependency

### #1252 — Plateau-aware heal loop + auto-plan-patch (DISPATCHED)

Codex running in background (task `blomtxvig`, first dispatch `bt4uoj9d6` failed on bad CLI args, re-dispatched with corrected `--prompt-file` + `--hard-timeout`). Full spec in GH #1252.
Three parts:
- A: plateau detection replaces 2-round cap
- B: auto-plan-patch on plateau via Gemini
- C: terminal escalation only if B fails

**Acceptance criteria (user-verifiable):**
1. 5 of the 19 currently-failing a1 modules auto-ship at ≥9.0.
2. No regression on 36 already-passing a1 modules.

**User-runnable validation** when Codex finishes:
```bash
for N in <5 failing a1 numbers>; do
  .venv/bin/python scripts/build/v6_build.py a1 $N --resume \
    --writer gemini-tools --reviewer codex-tools
done
```

Monitor Codex progress:
```bash
ab channel tail reviews --thread issue-1252 -n 20
# or
tail -f /private/tmp/claude-501/.../tasks/bt4uoj9d6.output
```

### #1249 — Contract-first validation on 6 A1 modules

Partially tested this session via M18. Extend to 5 more modules once #1252 lands.

### #1251 — CLOSED today

ACTIVITY_ORDER comparator fixed. Bare-type contract entries match descriptive writer IDs via prefix.

## Known non-blocking issues (file as separate tickets when budget allows)

1. **VESUM false positive on morpheme stubs.** `хот-` as a pedagogical root reference fails VESUM check. Add morpheme-marker recognition (trailing `-`).
2. **ACTIVITY_ORDER message UX.** Currently shows bare-type "expected" list against full-ID "found" list. Confusing. Should show position-by-position mismatch.

## Uncommitted on disk (user's earlier weather rebuild, NOT mine)

```
M  .coverage
M  curriculum/l2-uk-en/a1/audit/sentence_hashes.json
M  curriculum/l2-uk-en/a1/orchestration/my-day/v6-review-prompt.md
M  curriculum/l2-uk-en/a1/orchestration/weather/*
M  curriculum/l2-uk-en/a1/review/weather-review.md
M  curriculum/l2-uk-en/a1/weather.md
M  starlight/src/content/docs/a1/weather.mdx
?? curriculum/l2-uk-en/a1/orchestration/weather/dispatch/*meta.json
?? curriculum/l2-uk-en/a1/orchestration/weather/review-structured-r2.yaml
?? curriculum/l2-uk-en/a1/orchestration/weather/review-structured-r3.yaml
?? curriculum/l2-uk-en/a1/orchestration/weather/state.json
?? curriculum/l2-uk-en/a1/review/weather-review-r2.md
?? curriculum/l2-uk-en/a1/review/weather-review-r3.md
```

User decision pending: commit, stash, or revert.

## What happens next without Claude

1. Codex implements #1252 in background. Commits to main.
2. User pulls, runs 5-module validation (bash one-liner above).
3. Result determines go/no-go on scaling:
   - ≥3/5 auto-ship at 9.0 → architecture works, scale to all 19 a1 failures, then a2, then b1
   - <3/5 → file follow-up with specific failure modes, reconvene Monday
4. Monday reset: Claude is back. If architecture validated, Claude's role shifts to adversarial review of content only — not pipeline work.

## Critical reminder

The architecture was the bottleneck, not content competence. A1 M01 already hit 9.7 on the OLD pipeline. Eighty percent of the problem was "no deterministic contract binding writer and reviewer." That layer shipped this session (#1247). #1252 is the last structural piece.

Do not conclude failure before the Codex test completes.
