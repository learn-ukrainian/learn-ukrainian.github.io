---
date: 2026-05-16
session: "Long session 2026-05-15 → 2026-05-16. Two major workstreams: (1) m20 build-RED diagnosis + writer-prompt + gate patches landed on PR #2019; (2) full Grok 4.3 onboarding via Hermes — 4 calibration rounds, 9 stage tests, hallucination + corpus-bias + code-review probes. All Grok work uncommitted on main. Next orchestrator drives commit/PR + decides on stage 3."
status: green
main_sha: 9f75d70045
main_green: true
open_prs: [2019, 2006, 2000, 1873]
active_dispatches: 0
worktrees_open: 3  # main + 2 user/other
active_builds: 0
active_monitors: 0
agents: [claude, codex, gemini, grok-4.3]   # grok newly available via Hermes
filed_today: [2018]
merged_today: [2016]
closed_today: [2008]
opened_today: [2019]
hermes_config: medium  # restored after round 4
next_p0: |
  COMMIT/PR THE GROK ONBOARDING WORK, then drive stage 3 plumbing
  decision.

  1. **Commit Grok onboarding artifacts** — currently uncommitted on
     main (see `Untracked files` below). Two scripts + 5 audit dirs +
     HTML report + MD companion. Recommend ONE branch
     `feat/grok-4.3-onboarding-harness` with all of it; PR body draws
     from `audit/.../REPORT.html` summary.

  2. **Stage 3 decision** — V7 module writer trial with `--writer
     grok-tools` is the most expensive remaining test (~1-2 hours
     plumbing in `linear_pipeline.py` + 3 builds × ~20 min). Stages
     1/2/4/5 all complete. Cost-benefit:
     - Pro: Grok at $0.20/$0.50 per M tokens is ~75× cheaper than
       claude-opus on input. If Grok-as-writer passes vesum +
       textbook_grounding gates, it shifts the V7 cost equation
       dramatically.
     - Con: requires writer-side plumbing for `grok-tools` analogous
       to `claude-tools` (see how `claude-tools` is wired in
       `_render_component_props_schema` callers + `WRITER_DEFAULTS`).
     - Defer if PR #2018 (activity_schema gate) is the higher-leverage
       work — without it, m20 stays RED regardless of writer choice.

  3. **Task #15 routing layer** — add `grok` family to:
     - `scripts/audit/russianism_judge.py` (new `--judge-family xai`
       branch using `hermes -z` subprocess pattern)
     - `scripts/delegate.py` (new `--agent grok` dispatch route)
     - `scripts/ai_agent_bridge/__main__.py` (`ab ask-grok` command)
     This is a smaller piece than stage 3 and unblocks production
     use of Grok across the project. ~30-60 min.

  4. **PR #2019 merge timing** — open with full CI green per
     `gh pr view 2019`. Merging it WITHOUT #2018's activity_schema
     gate will still leave m20 RED on the writer-compliance bug
     (writer keeps emitting `incorrect:` instead of `error:` field).
     Decision: merge #2019 anyway (the patches are real improvements
     that ship value), then file #2018 work as separate effort.

  CARRY-OVER (not blocking next session):

  - **PR #2006** is DIRTY (single conflict in
    `eval/russianism/README.md` from #1999). The Grok harness reads
    calibration cases from `origin/pr-2006` directly via `git show`,
    so #2006 doesn't BLOCK Grok work, but it should rebase + merge
    eventually so the harness becomes self-contained. ~30 min rebase
    via dispatch.

  - **PR #2018 work** (activity_schema gate) is the blocker for m20
    GREEN. Recommended next-session priority after Grok commit.

  - **Hermes self-learning eval** — curator has 0 runs so far. To
    test the "agent that grows with you" claim, drive 10-20 similar
    tasks across a week and measure pattern persistence. Optional.

  - **Hermes vs ab consolidation question** — Hermes' kanban / memory
    / cron / webhooks / hooks features have direct equivalents in
    our orchestration. Not a forklift; evaluate piece-by-piece. Open
    research question.

  - **Try Grok 4.3 for ESUM OCR (Phase 2 retry)** — user-suggested
    follow-up. Grok-4 supports text+image input → text output per xAI
    docs; etymology Phase 2 OCR was bottlenecked by Gemini Free Tier
    server capacity (16-30% error rate across 9 restart cycles).
    Grok-4.3 has 4M tok/min rate limit and $0.20/$0.50 per M token
    pricing — could realistically beat the Gemini-CLI throughput +
    quality. PoC plan: take 10 representative ESUM pages (mix of
    clean + dirty scans), run them through `hermes -z` with vision
    input, score against Gemini's output. If error rate is materially
    lower, fire bulk re-run via the existing `bulk_ocr_gemini.py`
    pattern adapted for Grok. Hermes might need `-t` toolset flag
    or `--yolo` to allow large image uploads; verify with single-page
    probe first. Issue #2001 tracks Phase 2 OCR.
---

# Brief — 2026-05-16 — Grok 4.3 onboarding complete, m20 patches on PR #2019

> Predecessor: `2026-05-15-etymology-bulk-ocr-in-flight-handoff.md`
> (etymology Phase 2 was postponed mid-session per user direction;
> see PR #2016. This session pivoted entirely to A1/m20 + Grok
> onboarding.)

## TL;DR

Long session covering two distinct workstreams:

1. **m20 build-RED diagnosis** — builds #6/7/8 each failed on python_qg
   with different writer-compliance patterns. Three coordinated patches
   landed on PR #2019 (gate `_AVOID_MARKER_RE` + bold-strip, writer-prompt
   3 rules, 5 regression tests). Remaining bottleneck is the
   writer-compliance gap (writer emits non-canonical `incorrect:`/`wrong:`
   activity fields) tracked in new issue #2018, which recommends an
   `activity_schema` gate as the structural fix.

2. **Grok 4.3 onboarding via Hermes Agent** — major work. Auth via
   `hermes auth add xai-oauth` (already set up by user). Direct
   `api.x.ai/v1` calls return 403 (OAuth scope), so the integration
   path is `hermes -z PROMPT -m grok-4.3` subprocess. Sources MCP
   registered to Hermes config (`hermes mcp add sources --url
   http://127.0.0.1:8766/mcp`); 33 tools enabled. Ran 4 judge
   calibration rounds (medium / medium+MCP / high / xhigh), 9 stage
   tests (5/2/4 × 3 efforts), 1 PR review test, 1 corpus-bias probe,
   1 hallucination probe. Production recommendation: `grok-4.3 @
   medium + sources MCP`.

**Headline finding from Grok onboarding:** `reasoning_effort=high` is
**measurably worse** than medium on every task we tested (judge,
code-gen, fixes). `xhigh` recovers some quality but doesn't beat
medium. Highest precision (90%) is at xhigh — that's the "lowest
false-positive" lane. Honesty is effort-independent (3/3 refusals).
Corpus-bias check on Russia/Ukraine: clean — frames Kyivan Rus as
Ukrainian foundation, not Russian; no imperial defaults.

## What's already done this session

### Code changes on main (merged)

| PR | What | Status |
|---|---|---|
| #2016 | OCR bulk script baseline + .gitignore for `gemini-ocr/` | ✅ merged |

### Open PRs as of handoff

| PR | What | Status |
|---|---|---|
| #2019 | m20 gate + writer-prompt + 5 regression tests | OPEN, all CI green, mergeable. Filed companion #2018 for the remaining writer-compliance gap. |
| #2006 | russianism_judge harness + 12-case calibration gold | DIRTY (single conflict in `eval/russianism/README.md` from #1999). Stale 2-week base. Grok harness reads from this branch directly, so it doesn't block Grok work. |
| #2000 | UA-GEC bulk lookup | depends on #1997; unchanged this session |
| #1873 | stale dependabot starlight bump | unchanged |

### Issues filed / closed this session

| Action | Issue | Notes |
|---|---|---|
| Closed | #2008 (textbook source_file LIKE pattern) | Resolved by PR #2012 already merged |
| Filed | #2018 (writer-compliance bottleneck) | Recommends `activity_schema` gate; full diagnosis in body |

### m20 work — full chain

| Build | Result | What was learned |
|---|---|---|
| #6 (pre-patch) | RED | `*завтрак*`, `*одіватися*` flagged (italicized Russianism callouts in valid pedagogy); `дивюся` in `incorrect:` activity field; textbook_grounding REJECT 0/0 |
| #7 (Russianism HTML markers + 3 prompt rules) | RED | `**юся**`, `**ється**`, `**єшся**`, `Я-ти`, `дивюся`; `citations_resolve` FAIL (writer used full title not plan-ref format) |
| #8 (all patches inc. bold-strip) | RED | `дивюся`, `користуювася` (BOTH in `incorrect:` field); `citations_resolve` ✓; textbook_grounding REJECT 1/2 (unattributed match) |

PR #2019 ships:
- `_AVOID_MARKER_RE` for `<!-- bad -->форма<!-- /bad -->` callouts
- Bold-emphasis + italic-emphasis unwrap before morpheme regex
- Writer prompt rules: HTML markers, canonical `error-correction` fields with 12-alias FORBIDDEN list, 2-step verbatim textbook retrieval (search → `get_chunk_context`)
- 5 regression tests in `tests/build/test_linear_pipeline.py`

Remaining m20 work is BLOCKED on #2018 (`activity_schema` gate).

### Grok 4.3 — full onboarding deliverables (uncommitted on main)

**Scripts:**
- `scripts/audit/grok_judge_calibration.py` — reusable harness, pulls 12-case gold from `origin/pr-2006:eval/russianism/calibration-cases.jsonl`, supports `--out-dir` for round comparison
- `scripts/audit/grok_stage_runner.py` — stage 2/4/5 sweep with atomic config swap

**Audit dirs:**
- `audit/2026-05-15-grok-4.3-judge-calibration/` — round 1 (medium, no MCP)
- `audit/2026-05-15-grok-4.3-judge-calibration-with-mcp/` — round 2 (production-recommended)
- `audit/2026-05-15-grok-4.3-judge-calibration-effort-high/` — round 3
- `audit/2026-05-15-grok-4.3-judge-calibration-effort-xhigh/` — round 4
- `audit/2026-05-15-grok-stage-tests/` — stages 2/4/5 × {medium, high, xhigh}, 9 files

**Reports:**
- `audit/2026-05-15-grok-4.3-judge-calibration/REPORT.html` — primary report, project audit template style
- `audit/2026-05-15-grok-4.3-judge-calibration/CONSOLIDATED-REPORT.md` — MD companion

### Key Grok findings

| Test | Result |
|---|---|
| Stage 1 (judge calibration) | medium+MCP wins: F1 76%, case_acc 92%, P 85%, R 69%. Beats gpt-5.5, ties gemini on case_acc, falls short of claude-opus-4-7 (86% F1). |
| Stage 2 (code-gen) | medium ✓, **high ✗** (wrong assumption about morpheme regex word-boundary), xhigh ✓ |
| Stage 4 (`<fixes>` reviewer) | medium ✓ clean, **high ⚠ over-engineered** (4-fix split-then-delete), xhigh ✓ identical to medium |
| Stage 5 (hallucination probe) | 3/3 refusals. Caught fabricated page-999 citation. Uses MCP to verify. |
| Bonus: PR #2019 review @ high | Independently surfaced #2018 + 3 regex edge cases I missed |
| Bonus: corpus-bias probe @ medium | Decolonized framing on Ukraine; one factual slip (Cyrillic attributed to Russia, not Bulgaria). Strong baseline for HIST/OES/RUTH. |

### Hermes Agent harness — what we learned

- xAI Grok integration: OAuth via `hermes auth add xai-oauth`; tokens at `~/.hermes/auth.json`. Direct `api.x.ai/v1/chat/completions` calls return 403 (token has session scope, not bearer API access). Use `hermes -z PROMPT -m grok-4.3` subprocess.
- MCP registration: `hermes mcp add sources --url http://127.0.0.1:8766/mcp` (answer `n` then `Y` to prompts). All 33 tools enabled by default for `-z` mode; no per-call approval needed.
- Reasoning levels: `/reasoning [none|minimal|low|medium|high|xhigh]` slash command in TUI. For headless `-z`, edit `~/.hermes/config.yaml` line 52 (`agent.reasoning_effort`).
- Hermes feature surface is large: skills + curator (self-learning), kanban, memory, cron, webhooks, multi-platform delivery, proxy, fallback, computer-use, ACP, dashboard. Many features overlap with our existing orchestration; piece-by-piece evaluation needed.

## What's NOT done

| Item | Why | Effort |
|---|---|---|
| Commit + PR Grok onboarding | Awaiting your call | ~5 min |
| Stage 3 (V7 writer trial with `--writer grok-tools`) | Requires `linear_pipeline.py` plumbing analogous to `claude-tools` | ~1-2 hr plumbing + 3 builds × ~20 min |
| Task #15 routing layer (russianism_judge.py + delegate.py + ab) | Smaller than stage 3; unblocks production use | ~30-60 min |
| Grok-specific prompt tuning (close F1 gap to claude-opus) | Original calibration was apples-to-apples (untuned per model). Per-model tuning is a separate decision. | ~30 min if approved |
| #2018 activity_schema gate | Blocker for m20 GREEN. Grok's PR review provided concrete design hints. | ~1-2 hr |
| PR #2006 rebase + merge | DIRTY, single README conflict, 2-week stale base | ~30 min via dispatch |
| Hermes curator / self-learning eval | Need 10-20 similar tasks across a week | Long-running observation |

## Worktrees alive at handoff

```
main                                                  9f75d70045 [main]
.worktrees/2006-trigger                               1dd8d29524 [feat/russianism-judge-production]   ← user/other (PR #2006 work)
.worktrees/codex-interactive                          2eb62691d4 (detached HEAD)                      ← user/other
.worktrees/dispatch/codex/pr2-ua-gec-bulk-lookup-2026-05-15  46ffc47291 [codex/pr2-ua-gec-bulk-lookup-2026-05-15]
```

No build worktrees (cleaned up after each m20 attempt).

## Untracked files on main at handoff

```
audit/2026-05-15-grok-4.3-judge-calibration/
audit/2026-05-15-grok-4.3-judge-calibration-with-mcp/
audit/2026-05-15-grok-4.3-judge-calibration-effort-high/
audit/2026-05-15-grok-4.3-judge-calibration-effort-xhigh/
audit/2026-05-15-grok-stage-tests/
audit/etymology-ocr-feasibility/bulk-run-log.session-2026-05-15.jsonl   ← from prior session, gitignored content path
scripts/audit/grok_judge_calibration.py
scripts/audit/grok_stage_runner.py
```

The `bulk-run-log.session-2026-05-15.jsonl` is a session log from the predecessor etymology work; the OCR script + .gitignore already shipped in PR #2016 so this is just orphaned audit data.

## Hermes config state

`~/.hermes/config.yaml` is at `reasoning_effort: medium` (restored after round 4). No backup files lingering. Sources MCP registered at line 212+ of config (`mcp_servers.sources.enabled: true`). All 33 sources tools enabled.

User has a separate Hermes session running interactively in another terminal in this project folder. Be careful with config edits — `grok_stage_runner.py` does atomic backup+swap+restore per call, but full-file edits can affect the user's session if they start a new chat (existing sessions load config at start).

## How to start the next session

1. Read this brief.
2. Cold-start orient via Monitor API (`/api/state/manifest`, `/api/orient`).
3. Check open PRs: `gh pr list --state open` — verify #2019 still green/mergeable.
4. **Decide:** commit Grok work as a single onboarding PR? Recommend `feat/grok-4.3-onboarding-harness` branch.
5. If yes — commit + push + open PR using `audit/.../REPORT.html` for the summary. Probably also include `scripts/audit/grok_judge_calibration.py` and `scripts/audit/grok_stage_runner.py` as new utilities.
6. After Grok PR opened, decide on the next-P0 between stage 3 (V7 writer trial) vs #2018 (activity_schema gate). Recommendation: **#2018 first** — it's the m20 GREEN unblocker, and Grok-as-writer should be tested AFTER m20 has a working baseline.
7. Task list at handoff: 18 tasks total, 14 completed, 4 pending (#12 stage 3 V7 writer, #15 routing layer, and two stale ones at #11/#13 already completed but kept-as-completed for traceability).

## #M-* lessons reinforced this session

- **#M-4 (deterministic over hallucination):** held. Every Grok claim verified by running its outputs as real tests (stage 2 verification) or comparing to existing leaderboard (stage 1). No prose-only conclusions.
- **#M-5 (never print secrets):** held. Used `jq -r 'paths(scalars)'` for auth.json key paths only, never values. Hermes OAuth token never printed.
- **#M-6 (drive priorities):** mostly held. Asked permission once on the `<Avoid>` JSX component vs HTML-comment marker design choice (correct — design decision with downstream blast radius). Drove all other decisions inline.
- **#M-8 (orchestrator-active through dispatch lifecycle):** held. Every long-running operation (builds #6/7/8, judge calibration ×4 rounds, stage sweep) was Monitor-armed; no idle gaps.

## New patterns / observations to consider for MEMORY

| Pattern | Worth promoting to MEMORY? |
|---|---|
| `reasoning_effort=high` is WORSE than medium on multiple model tasks (judge, code-gen, fixes) | Probably yes — pin under #M0 model-assignment table when Grok routing lands |
| Hermes `-z` subprocess pattern is the cleanest non-interactive Grok access (direct API 403s on OAuth scope) | Worth a one-liner in MEMORY if we adopt Grok |
| HTML-comment `<!-- bad -->...<!-- /bad -->` marker for pedagogical Russianism callouts | Already documented in `linear-write.md`, no MEMORY entry needed |
| Stage runner pattern (atomic config-swap + restore for per-call settings) | Reusable for any per-call agent config; might generalize for other agent tooling |

---

*Format: MD per #M-2 (ai → ai handoff). Companion: `audit/2026-05-15-grok-4.3-judge-calibration/REPORT.html` (the ai → human surface for the same work).*
