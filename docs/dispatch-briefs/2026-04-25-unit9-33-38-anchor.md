# Unit 9 — Canonical anchor for 33-letters-vs-38-sounds explanation (#1550)

You are Codex. Your job is to add a Canonical Anchor entry to
`scripts/build/phases/v6-write.md` so the writer cannot invent a wrong
causal explanation for the 33-letters / 38-sounds inventory mismatch.

## Why this exists

Phase B v3 of a1/1 (gpt-5.5 writer, post-Unit-7) failed Factual at 5.0
because the writer wrote (verbatim from `evidence` field of
`curriculum/l2-uk-en/a1/review/sounds-letters-and-hello-review-factual-r1.yaml`):

> Why the mismatch? Three reasons... **Third, дж/дз together spell
> single sounds.**

That is **wrong reasoning**. Two letters spelling one sound (digraphs
like дж/дз) would REDUCE the sound count below the letter count, not
increase it. The reviewer (Claude) caught it cleanly:

> *"Причина 'дж and дз together spell single sounds' є правдивою як
> окремий орфографічний факт, але хибною як пояснення того, чому 38
> звуків більше за 33 літери. За контрактом треба назвати тверді/м'які
> пари приголосних."*

The plan (`curriculum/l2-uk-en/plans/a1/sounds-letters-and-hello.yaml`)
DOES contain both correct explanations — but in separate sections:
- Section 2 (Голосні): "Я, Ю, Є, Ї в певних позиціях позначають два
  звуки"
- Section 3 (Приголосні): "32 приголосних звуки з 22 приголосних літер.
  Деякі приголосні утворюють пари: тверді і м'які."

The plan does NOT explicitly tie those two facts together as the JOINT
explanation for the 33-vs-38 mismatch in the Підсумок. The writer has
to synthesize. gpt-5.5 invented a third reason instead. We need a
Canonical Anchor that nails the correct reasoning at writer-prompt level
so future builds (and any phonetics module that touches this fact)
cannot drift.

## Worktree instructions (mandatory)

```bash
git fetch origin main
git worktree add -b codex/1550-unit9-33-38-anchor \
    .worktrees/dispatch/codex/codex-1550-unit9-33-38-anchor origin/main
cd .worktrees/dispatch/codex/codex-1550-unit9-33-38-anchor
git log --oneline HEAD..origin/main   # MUST be empty before proceed
```

## File-level work (numbered, non-optional)

### Step 1. Bump version comment

`scripts/build/phases/v6-write.md` line 1 — bump 2.5.0 → 2.5.1, today's
date, summary line: "phonetics canonical anchor: 33 letters / 38 sounds
— hard/soft pairs + Я/Ю/Є/Ї are the joint explanation; ban дж/дз as a
third reason (#1550 U9)".

### Step 2. Add a `### Phonetics canonical anchor` subsection

Insert immediately AFTER the existing `### Phonetics discipline: звук vs
літера` section and BEFORE the next `###` heading. The current
`### Phonetics discipline` section ends with a paragraph about applying
the звук/літера distinction in every relevant sentence.

Use this exact body:

```markdown
### Phonetics canonical anchor: 33 letters vs 38 sounds

When a module teaches the 33-letters / 38-sounds inventory mismatch
(any module focused on phonetics, alphabet, or sounds-and-letters), the
explanation has TWO parts and ONLY two parts:

1. **Yotated vowel letters spell two sounds in some positions.** Я, Ю,
   Є, Ї each represent [й]+vowel at the start of a word, after another
   vowel, or after an apostrophe / soft sign. Ї is always [й]+[і]; Я,
   Ю, Є also map to a single vowel sound + softness of the preceding
   consonant elsewhere.

2. **Many consonants come in hard / soft pairs that share one letter.**
   The 32 consonant sounds map to 22 consonant letters because letters
   like Л, Н, С, Т, Д, З, Ц, Р, Л, Н encode both a hard and a soft
   consonant phoneme; the soft variant surfaces before я/ю/є/ї/і or a
   soft sign. The Захарійчук Grade 1 textbook tags hard with `[–]` and
   soft with `[=]`.

These two together explain why 38 sounds > 33 letters. Together they
add ~8 sound-positions on top of the 33-letter base.

WRONG — never write any of these as the explanation for 38 > 33:
- "**дж** and **дз** together spell single sounds." (TRUE as a separate
  digraph fact, FALSE as an explanation for 38 > 33 — two letters
  mapping to one sound would REDUCE the count, not raise it.)
- "Soft sign **Ь** adds a sound." (FALSE — Ь spells no sound; it marks
  softness of the preceding consonant.)
- "The apostrophe **'** adds a sound." (FALSE — it is an orthographic
  marker, not a phoneme.)

If you teach дж/дз or the soft sign in the same module, present them as
SEPARATE orthographic facts (two letters → one digraph sound; Ь = no
sound, only softens the previous consonant), NOT as reasons for 38 > 33.

The summary / Підсумок of any module that names the 38-vs-33 number
must give BOTH parts (yotated doubling + hard/soft pairs). Listing only
the yotated vowels is incomplete and the Factual reviewer will flag it.
```

### Step 3. Add a regression test

Create `tests/test_v6_write_prompt_phonetics_anchor.py` with a single
`@pytest.mark.unit` test that asserts the following substrings appear in
`scripts/build/phases/v6-write.md`:

- `"Phonetics canonical anchor: 33 letters vs 38 sounds"`
- `"Yotated vowel letters spell two sounds in some positions"`
- `"Many consonants come in hard / soft pairs that share one letter"`
- `"never write any of these as the explanation for 38 > 33"`

Match the convention from `tests/test_v6_write_prompt_naturalness.py`
(relative `Path` from cwd, missing-substring assertion list).

### Step 4. Verify

```bash
.venv/bin/ruff check scripts/build/phases/ tests/
.venv/bin/pytest tests/test_v6_write_prompt_phonetics_anchor.py -q
```

Both must pass.

### Step 5. Commit + push + PR (EXPLICIT)

```bash
git add scripts/build/phases/v6-write.md \
        tests/test_v6_write_prompt_phonetics_anchor.py
git commit -m "feat(write): phonetics anchor — 33 letters vs 38 sounds joint explanation (#1550 U9)"
git push -u origin codex/1550-unit9-33-38-anchor
gh pr create \
  --title "feat(write): 33-letters-vs-38-sounds canonical anchor (#1550 U9)" \
  --body "Closes the Factual 5.0 root cause from Phase B v3 of a1/1. Adds an explicit canonical anchor under '### Phonetics discipline' that ties the two correct reasons (yotated vowel doubling + hard/soft pairs) and bans дж/дз / Ь / apostrophe as wrong third reasons. Brief: docs/dispatch-briefs/2026-04-25-unit9-33-38-anchor.md. Refs #1550 U9."
```

Do NOT enable auto-merge.

## Acceptance criteria

- `scripts/build/phases/v6-write.md` v2.5.1 with the new
  `### Phonetics canonical anchor: 33 letters vs 38 sounds` subsection
  placed AFTER `### Phonetics discipline: звук vs літера` and BEFORE
  the next `###` heading
- The four required substrings present
- New regression test exists and passes
- ruff clean
- PR opened, no auto-merge
- Branch fresh on `origin/main`
