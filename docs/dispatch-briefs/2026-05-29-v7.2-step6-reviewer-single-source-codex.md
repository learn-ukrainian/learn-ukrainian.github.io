# Dispatch — V7.2 Step 6: reviewer single-source emission (codex gpt-5.5)

**Agent:** codex `gpt-5.5`, `--effort high`, `--mode danger`, `--worktree`. ~1 day.
**Issue:** #2388 — `[V7.2 Step 6] Reviewer obligation emission from same registry — close single-source loop`
**ADR:** `docs/decisions/pending/2026-05-28-wiki-driven-prompt-generator.md` (§ Roadmap → Step 6)
**Design doc:** `docs/best-practices/universal-rules-registry.md` (§ Roadmap; § Slots — `shared.contract`)
**Builds on (MERGED):** Step 5 `12735cfabb` (#2398). **Do NOT re-implement Step 5.**

## #M-4 preamble — verifiable claims (quote raw tool output, never "I checked")
Every claim below is tool-backed; capture the command + cwd + raw final line:
- "tests pass" → `.venv/bin/python -m pytest tests/build/ tests/audit/ -q` final summary line raw.
- "lint clean" → `.venv/bin/ruff check <files>` final line raw.
- "checklist byte-identical" → the new acceptance test's final line raw.
- "gate parses 1:1" → the new acceptance test's final line raw.
- "commit landed" / "PR opened" → `git log -1 --oneline` + `gh pr view --json url` raw.

## Goal (one predicate)
With the Step 5 generator landed, make the **reviewer-side rendering** and the **`wiki_coverage_gate` parser** consume the **SAME** Obligation Checklist object the writer prompt emits — closing the single-source loop that m20's `wiki_coverage_gate` failure exposed. Keep it behind the existing `--use-generator` opt-in (default OFF = byte-identical to `main`, same discipline as Step 5).

## Required surface (real call sites — verify before editing)
The single renderer already exists and is shared by writer + reviewer (Step 5):
- `scripts/build/linear_pipeline.py:1080` — `_render_wiki_coverage_required_items(wiki_manifest)` — imports `_extract_required_items` from the gate (L1090-1091), calls it at L1117. This is the ONE renderer.
- `scripts/build/prompt_generator.py` — `build_obligation_checklist()` delegates to that renderer (single source).
- `writer_context` (L~2935) exposes `OBLIGATION_CHECKLIST` + legacy `WIKI_COVERAGE_REQUIRED_ITEMS`; `review_context` (Step 5) exposes the SAME `OBLIGATION_CHECKLIST`.
- The gate: `scripts/audit/wiki_coverage_gate.py` — `_extract_required_items`, `_normalize_required_claim`, `check_wiki_coverage_paths`; invoked via `linear_pipeline.run_wiki_coverage_gate` (L3656).

**Step 6 work:**
1. Make `run_wiki_coverage_gate` / `check_wiki_coverage_paths` validate the built artifact against the **same** rendered Obligation Checklist the prompts received — not a separately re-derived list. The checklist object (or its `_extract_required_items` output) must flow from one computation into prompt-emission AND gate-parsing, so the gate checks the exact obligations the writer was handed. No second extraction path.
2. If reviewer-side deterministic checks are emitted, source them from the registry (`shared.contract` rules) via the Step 5 generator — not hardcoded in the gate.
3. Behind `use_generator` where it changes prompt/gate behavior; flag OFF stays byte-identical to `main`.

## Acceptance tests (from #2388 — both REQUIRED)
- Assert the Obligation Checklist text is **byte-identical** between the rendered `writer_prompt.md` and `reviewer_prompt.md` for a1/my-morning (m20) with `--use-generator` ON.
- Assert `wiki_coverage_gate` parses the obligations **1:1** against the registry/renderer's emission (same count, same normalized claims; no drift, no orphans).
- Plus: flag-OFF regression (gate behavior byte-identical to `main`).

## Out of scope (do NOT do)
- m20 rebuild — Step 7 (#2389).
- Trimming legacy `linear-write.md` — #2378.
- Re-touching the Step 5 generator composition (it's merged + parity-tested).

## Numbered execution steps (DISPATCH-BRIEF CHECKLIST)
1. `git worktree add` is handled by `--worktree`; confirm cwd is the worktree, base `origin/main` at/after `12735cfabb`.
2. Implement the single-source gate consumption + reviewer emission (above).
3. Tests: `.venv/bin/python -m pytest tests/build/ tests/audit/ -q` — green (note: 2 pre-existing data-dependent failures in `tests/build/` are env-only; confirm by stashing if seen).
4. `.venv/bin/ruff check` on changed files — clean.
5. Commit, conventional message citing #2388 + ADR.
6. `git push -u origin <branch>`.
7. `gh pr create` — cite #2388, the Step 5 SHA `12735cfabb`, and how the loop is now closed.
8. **NO auto-merge** — orchestrator reviews.
