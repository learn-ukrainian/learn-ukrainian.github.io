# Dispatch: fix requirements-lock.txt resolver conflicts (#2261 torchvision CPU-wheel + #2732 inscriptis/lxml)

Two independent but lock-file-adjacent dep problems. Fix BOTH in ONE coherent `requirements-lock.txt`
update so two agents don't race the same file. Read the issues first: `gh issue view 2261`,
`gh issue view 2732`.

## Scope
1. **#2261 — torchvision CPU-wheel ABI mismatch.** CI installs `torch==2.11.0+cpu` /
   `torchvision==0.26.0+cpu` from `https://download.pytorch.org/whl/cpu`, then the lockfile install
   replaces torchvision with the PyPI `0.27.0` wheel → `RuntimeError: operator torchvision::nms does not
   exist` → cascades to `Could not import module 'PreTrainedModel'`. Failing:
   `tests/rag/test_benchmark_harness.py::test_reranker_dry_run_skips_model_loading` and
   `tests/wiki/test_mlx_flagembedding_parity.py` (import collection). **Fix:** pin `torchvision` in the lock
   to the version that matches the explicit CPU install (`0.26.0`), OR bump torch to a matching pair — pick
   the MINIMAL change that makes those two test paths import cleanly under the CI install order.
2. **#2732 part 1 — inscriptis↔lxml.** `inscriptis==2.7.0` requires `lxml<6.0.0` but we pin `lxml==6.1.0`,
   so a clean-resolver install of the current lock is ALREADY broken. **Fix:** bump `inscriptis` to a
   release allowing `lxml>=6`, OR pin `lxml<6.0.0` — but FIRST grep what actually needs `lxml==6.1.0`
   (`grep -rn lxml requirements*.txt pyproject.toml`) and choose the option that breaks nothing.
3. **#2732 part 2 — `.dagger/uv.lock` idna 3.15.** ONLY attempt if `.dagger/sdk` is checked out / the
   dagger dev env is available. If not, leave it: add a one-line note + `# TODO(#2732)` and do NOT block
   the PR on it.

## Numbered steps
1. `cd /Users/krisztiankoos/projects/learn-ukrainian && git fetch origin` (you are in a `--worktree` from origin/main).
2. Implement scope 1–2 (and 3 only if dagger SDK present). Mirror existing lock formatting exactly.
3. Verify a clean resolve: `cd /Users/krisztiankoos/projects/learn-ukrainian && .venv/bin/pip install --dry-run -r requirements-lock.txt` → no conflict.
4. Run the named tests: `cd /Users/krisztiankoos/projects/learn-ukrainian && .venv/bin/python -m pytest tests/rag/test_benchmark_harness.py tests/wiki/test_mlx_flagembedding_parity.py -q` → paste summary.
5. `cd /Users/krisztiankoos/projects/learn-ukrainian && .venv/bin/ruff check .` → paste final line.
6. Commit `fix(deps): pin torchvision to CPU wheel + resolve inscriptis/lxml lock conflict (#2261, #2732)`.
7. `git push -u origin <branch>`; `gh pr create` referencing #2261 + #2732. NO auto-merge.

## Evidence (#M-4 — command + cwd + raw output per claim)
- clean resolve: the `pip install --dry-run` tail; tests: the pytest summary line; lint: ruff final line;
  pushed: `git log -1 --oneline`; PR: `gh pr view --json url`. Unproven "did X" = fabrication.
