# Dispatch — V7.2 Step 2: Pass wiki_manifest + implementation_map_contract into review_context()

**Status:** ready to dispatch (next session, parallel-safe with Step 1)
**Agent:** codex (`gpt-5.5`, `high`)
**Mode:** `danger` (worktree required)
**Task ID:** `v7.2-step2-review-context-passthrough-2026-05-XX`
**Issue:** #2384
**ADR:** `docs/decisions/pending/2026-05-28-wiki-driven-prompt-generator.md`

## Goal

Eliminate the reviewer drift surface that codex r2 flagged in the 2026-05-28 multi-agent discussion: today's `review_context()` at `scripts/build/linear_pipeline.py:3687` passes plan/content/dim to the reviewer but does NOT pass `wiki_manifest` or `implementation_map_contract`. The reviewer is structurally blind to what the writer was asked to do.

## Required change

In `scripts/build/linear_pipeline.py`:

1. Locate `review_context()` function (around line 3687 per ADR).
2. Add `wiki_manifest` and `implementation_map_contract` to the context dict it returns (or to the keyword arguments it forwards into `render_phase_prompt()`).
3. Update `scripts/build/phases/linear-review-dim.md` template to consume the new slots — render them in the reviewer prompt so the reviewer sees:
   - The same wiki obligation set the writer saw
   - The same implementation_map_contract the writer was asked to satisfy
4. Update any reviewer-side rendering helpers that build the reviewer prompt to forward the new slots.

This is a **structural pass-through**, NOT a rubric change. The reviewer still scores on the same 9 dimensions. We're just making sure it has access to what it should have had all along.

## Verifiable claims (#M-4)

| Claim | Tool that proves it |
|---|---|
| "review_context now passes wiki_manifest + implementation_map_contract" | `git diff origin/main..HEAD -- scripts/build/linear_pipeline.py` raw diff showing the new context keys |
| "Reviewer prompt template renders the new slots" | `grep -n 'WIKI_MANIFEST\|IMPLEMENTATION_MAP_CONTRACT' scripts/build/phases/linear-review-dim.md` raw matches |
| "Existing reviewer tests pass" | `.venv/bin/python -m pytest tests/build/ -k review -q --no-header` final summary raw |
| "End-to-end: rendered reviewer prompt for m20 contains wiki obligations + impl-map contract" | New unit test asserts both slots appear in the rendered reviewer prompt for `a1/my-morning` |
| "PR opened" | `gh pr view <N> --json url` raw URL line |

## Dispatch steps

1. `git worktree add` via `delegate.py --worktree`.
2. Edit `scripts/build/linear_pipeline.py::review_context()` to include `wiki_manifest` + `implementation_map_contract`.
3. Edit `scripts/build/phases/linear-review-dim.md` to render the new slots in the appropriate section (likely near the existing wiki-coverage rubric block).
4. Add a test asserting the rendered reviewer prompt contains both new slots for a known module.
5. Full pytest: `.venv/bin/python -m pytest tests/ -q --no-header` — quote final line.
6. Ruff: `.venv/bin/ruff check scripts tests` — quote final line.
7. Commit `fix(v7.2): pass wiki_manifest + implementation_map_contract into review_context` + `X-Agent:` trailer.
8. Push + open PR citing #2384 + the ADR.
9. NO auto-merge.

## Out of scope

- Do NOT change the reviewer's rubric dimensions or scoring weights.
- Do NOT touch `wiki_coverage_gate.py` (Step 3).
- Do NOT extract universal rules to a registry (Step 4).

## Expected wall-clock

30-60 min. Pass `--silence-timeout 5400`.

## Done criteria

- All blocking CI green.
- Scope: `scripts/build/linear_pipeline.py` + `scripts/build/phases/linear-review-dim.md` + new test file. Nothing else.
