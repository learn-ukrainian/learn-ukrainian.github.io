# BIO Bilash Gate Review — #2337/#2336 — 2026-06-29

**Scope:** `oleksandr-bilash` only, after #4005 base-prep fixes.

## Verdict

| Gate | Verdict | Notes |
| --- | --- | --- |
| #4005 base packet | PASS WITH WARNINGS | Dossier, plan, wiki packet exist; AGY metadata/source blockers fixed. |
| #2337 crosslink gate | PASS WITH WARNINGS | Dossier §7 paths pass local xref lint. Plan `connects_to` uses existing BIO identifier convention. |
| #2336 decolonization/source gate | PASS WITH WARNINGS | Framing keeps Soviet co-optation visible and avoids invented martyrdom; source/image TODOs remain explicit. |

## Evidence

- `curriculum.yaml` BIO parses: 387 modules, 0 duplicate slugs.
- `curriculum/l2-uk-en/plans/bio/oleksandr-bilash.yaml` parses; `aliases` are clean indexable strings; all four `objectives` parse as strings.
- `scripts/audit/lint_bio_dossier_xref.py --paths docs/research/bio/oleksandr-bilash.md` passes.
- `npx markdownlint-cli2` over changed BIO docs/audits/templates passes.
- `git diff --check` passes.

## AGY/Gemini Findings

- Valid blocker fixed: plan `aliases` no longer index parenthetical instructions or forbidden Russian-imperial forms.
- Valid blocker fixed: Pavlychko interview reference now uses the exact `Високий Замок` URL.
- False positive challenged: `connects_to` values such as `bio-137-dmytro-pavlychko` are BIO identifiers, not filenames. Existing BIO plans use this convention, and the referenced figures resolve to sequences 137, 39, and 149 in `curriculum.yaml`.
- Warning carried: CSAMM and `Високий Замок` can return `403` to automated fetches; this is a link-check warning, not proof the sources are dead.

## Carry Into #4004 Pilot

- Do not quote the three §4 primary-voice snippets until exact wording is verified against Bilash published collections or a stronger source.
- Do not assert Bilash composed a setting of «Любіть Україну» unless T1/T2 evidence is found.
- Do not use the Градизьк monument image until photo licence and Ukrainian freedom-of-panorama status are resolved; text-only fallback remains acceptable.
- Keep the KGB episode as Pavlychko recollection / censorship-and-surveillance pressure. Do not inflate it into arrest, exile, imprisonment, or dissident martyrdom.

## Claude Gate Attempt

`scripts/delegate.py dispatch --agent claude --task-id bio-2337-2336-bilash-gates` failed with `response_chars: 0` and no stdout/stderr review output. This is a runtime/provider failure, not a substantive review.
