[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/infra-6870-grok-rotation-cleanup-r2
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Task: resolve issue #6870 — grok-4.6 rotation cleanup residuals (items 1, 2, 4 + proposal for 3)

ROUTING_CARD_V1
- tier: heap/mechanical
- model_x_harness: cursor:auto (native Cursor CLI)
- why_this_tier: small pre-scoped cleanup bundle from a CF review; Cursor Auto is the served first pick for mechanical this month
- advisor_packet: residuals were banked from the PR #6868 CF review (grok-4.6 APPROVE) — pre-reviewed scope, no new design
- owned_paths: grok routing/launcher surfaces + their tests (per issue #6870)
- acceptance_cmd: .venv/bin/pytest (affected tests) + ruff clean + CI green on PR
- alternatives_considered: kimi (busy on #6888; overkill for mechanical), grok (same-family conflict of interest on its own lane's routing cleanup)
- parallel_free_seats: deepseek (rough), glm (local-only)

## Evidence rules (#M-4)
Every claim tool-backed with quoted raw output (e.g. the refusal behavior before/after, test runs).

## Steps
1. Read GitHub issue #6870 in full. Implement items 1, 2, and 4 exactly as banked there.
2. Item 3 (project-local harness declaration): do NOT unilaterally rewrite the project config — implement the "align or explicitly exempt with a comment" choice that best matches how the fleet seat list is actually consumed, and justify the choice in the PR body. If genuinely ambiguous, exempt-with-comment and say why.
3. Do NOT touch the 4.6/4.7 two-tier split — it is operator-gated and out of scope.
4. Work ONLY in your dispatch worktree. Add/adjust tests for each behavior change (the item-4 test must pin the literal retired model name).
5. Run affected pytest + ruff. Conventional commit, push, open a PR referencing #6870.
6. Do NOT arm auto-merge and do NOT merge. The driver handles review + merge.