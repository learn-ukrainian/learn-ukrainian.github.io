[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/6687-ci-fix
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Task: fix PR #6687 CI — DeepSeek OpenCode must not break ask-hermes tests

## CAPACITY_CARD
pick=cursor cool
## ROUTING_CARD_V1
tier=practical model_x_harness=cursor-agent
why=Cursor free; Kimi already authored #6687; do not pile more on Kimi
owned_paths: tests/ai_agent_bridge/test_hermes_review_isolation.py, scripts/ai_agent_bridge/_hermes.py, scripts/agent_runtime/registry.py (only if ask-hermes must keep Hermes adapter)

## Failure (CI shard 3, tool-backed)
```
tests/ai_agent_bridge/test_hermes_review_isolation.py
AgentUnavailableError: 'deepseek' Popen failed: FileNotFoundError: opencode
SystemExit: ask-hermes: Hermes runtime failed
```

Cause: #6687 switched registry `deepseek` to `DeepSeekAdapter` (opencode). `ask-hermes` still routes through agent `deepseek`, so CI (no opencode binary) dies.

## Do
1. Fetch and start FROM `origin/kimi/routing-defaults-2026-08-13` (PR #6687), not from stale main.
2. Smallest fix that preserves operator GO (dispatch `--agent deepseek` = OpenCode `deepseek-direct/flash`):
   - `ask-hermes` must keep using `HermesDeepSeekAdapter` (do not spawn `opencode`).
   - Hermes isolation tests must pass without an `opencode` binary.
3. Push to `kimi/routing-defaults-2026-08-13` (`--force-with-lease` only if you rebased that branch) so #6687 updates. Do not open a second competing PR unless push is refused.
4. Quote local pytest on the two hermes isolation tests.
5. X-Agent: cursor/6687-ci-fix. No merge.

Do not revert the OpenCode DeepSeek dispatch default.