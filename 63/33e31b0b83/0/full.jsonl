{"metadata":{"actual_model_known":"false","entrypoint":"delegate","harness":"cursor-headless","requested_model":"auto","runner_agent":"cursor"},"repo_path":"/Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/infra-6935-delta-instance-bind","session_id":"fleet-de2b7c4554714cb6ac97910534317527","timestamp":"2026-08-17T10:12:34.184125Z","type":"session"}
{"text":"[delegate worktree]\nRun all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/infra-6935-delta-instance-bind\nDo not switch branches in the main checkout.\nIf you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.\n\n[shared project interpreter]\nNever create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.\nSparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).\n\n[optional delivery signal]\nCommits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {\"outcome\":\"no_change\",\"reason\":\"why no changes are required\"}`. This line is optional — its absence never fails the dispatch.\n\n# Task: apply the FAIL delta on PR #6936 (your branch cursor/infra-6935-liveness-pin)\n\nROUTING_CARD_V1\n- tier: heap/mechanical\n- model_x_harness: cursor:auto (authoring lane — review FAIL on its own PR)\n- why_this_tier: prescribed minimum fix from the grok review seat\n- advisor_packet: grok FAIL verdict on PR #6936 (driver comment)\n- owned_paths: scripts/agent_runtime/adapters/** (grok adapter), tests/test_grok_build_adapter.py\n- acceptance_cmd: .venv/bin/pytest tests/test_grok_build_adapter.py + ruff clean\n- alternatives_considered: n/a — author-lane delta\n- parallel_free_seats: n/a\n\n## The blocking finding (grok seat, verbatim substance)\nYour instance-level `_liveness_session_id` fallback bind lets a SHARED adapter serving two plans hand plan B (unpinned) the session id pinned for plan A — cross-plan liveness contamination that silently keeps a later plan \"live\" off a peer. Pin-on-metadata and snapshot-on-metadata are the right sources of truth; the instance BIND undoes them for the shared-adapter poller the fleet actually uses.\n\n## Minimum fix (prescribed)\n1. `_bound_liveness_session_id` reads ONLY `plan.metadata` (when it is a dict). Instance SNAPSHOT fallback may stay; instance BIND must go.\n2. Add the same-adapter two-plan guard: plan A pinned, plan B unpinned → B must resolve to sessions_root / its own post-snapshot child, NEVER A's id. Mutation-check it (restore the instance bind → test fails).\n3. Optional if trivial: a pinned-dir-deleted-mid-run test documenting the false-dead direction is acceptable behavior.\n\n## Steps\nWork on the EXISTING branch cursor/infra-6935-liveness-pin in your dispatch worktree; apply; run acceptance + ruff; push; comment on PR #6936 with mutation evidence. Do NOT arm auto-merge and do NOT merge.\n","timestamp":"2026-08-17T10:12:34.184125Z","type":"user"}
