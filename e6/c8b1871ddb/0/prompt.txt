[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/grok/infra-drive-epic-no-idle
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

Delta on your PR #6974 (worktree .worktrees/dispatch/grok/infra-drive-epic-no-idle, branch grok/infra-drive-epic-no-idle, head 0d862a4d89): the operator-directed fleet discussion (recorded on issue #6976 — read it: gh issue view 6976) specified items your section must also carry. Unit: ONE commit amending your new §2c in agents_extensions/shared/skills/drive-epic/SKILL.md: (1) definitions — free lane, ready item, compatible/independent, settle event, grace period, epic done; (2) explicit precedence order: correctness/quality → safety/resource bounds → dependency/critical-path → utilization; (3) guardrail-authorized idle is NOT a utilization failure — a settle-hold must carry one of the structured disposition codes (dependency_blocked | review_wip_cap | ci_capacity | disk_capacity | human_decision | no_ready_work); (4) pipeline-depth limit + unit N regains priority the moment review feedback returns; (5) queue rule: an unfinished epic needs a current ready-work forecast — an empty ready queue requires an explicit disposition, not silence; (6) anti-gaming: no placeholder agents, artificial task splitting, premature PRs, or speculative work without an integration path; (7) one line pointing to #6976 for the mechanical enforcement so prose and tooling stay linked. Keep it TIGHT. Acceptance: scripts/lint/lint_agent_skills.py clean (quote), diff quote of the section. Conventional commit docs(drive-epic): disposition codes, precedence, anti-gaming per fleet discussion (#6976); commit in the worktree — driver finalizes push if your harness cannot. Evidence preamble binds.