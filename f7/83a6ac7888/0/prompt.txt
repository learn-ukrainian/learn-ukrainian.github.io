[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/kimi/infra-6888-agy-watchdog
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Task: resolve issue #6888 — agy dispatch stall watchdog

ROUTING_CARD_V1
- tier: practical (upper)
- model_x_harness: kimi:k3-256k (native Kimi Code)
- why_this_tier: runtime/adapter debugging with real failure forensics — strongest available builder while codex is capped; needs judgment beyond mechanical
- advisor_packet: n/a (tier=practical)
- owned_paths: scripts/delegate.py, scripts/agent_runtime/** (agy adapter), related tests
- acceptance_cmd: .venv/bin/pytest (runtime/delegate tests) + ruff clean + CI green on PR
- alternatives_considered: cursor:auto (taken for #6890; this needs deeper debugging), grok-4.6 (taken for #6852; breadth), agy itself (conflict of interest — it is the stalling lane)
- parallel_free_seats: deepseek (rough), glm (local-only, lightly used)

## Evidence rules (#M-4)
Every claim tool-backed: quote raw command output for any duration, timeout value, task-state field, or behavior claim. The two burn task JSONs named in the issue are your primary evidence — quote them.

## Steps
1. Read GitHub issue #6888 in full, including the two 2026-08-16 burn task records it names under batch_state/tasks/. Root-cause first: determine WHERE the stall sits (adapter exit contract vs silence detection) before choosing the fix.
2. Work ONLY in your dispatch worktree. Never touch the primary checkout.
3. Design + implement the fix at the layer your root-cause analysis points to (the issue sketches candidate directions — tighter agy silence default and/or an end-of-work exit contract — treat these as hypotheses, not the spec). Sibling-check: do other lane adapters share the same stall shape?
4. Guard test required, and mutation-check it: revert your fix locally → the test must fail; restore. State this in the PR body with output.
5. Run affected pytest + ruff. Conventional commit, push, open a PR referencing #6888 with root-cause narrative + evidence.
6. Do NOT arm auto-merge and do NOT merge. The driver handles review + merge.