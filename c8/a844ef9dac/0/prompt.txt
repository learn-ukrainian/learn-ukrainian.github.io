[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/kimi/infra-6812-bridge-module-identity
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Dispatch brief — infra-6812-sweep-completion (red CI on PR #6831)

Your PR #6831's new guard test WORKS — and it caught that your sweep was incomplete: CI
shard 2/4 fails `test_bare_bridge_identity_not_loaded` because **35 top-level
`tests/*.py` files still import the bare identity** (your sweep covered
`tests/ai_agent_bridge/` + siblings, but not these). Evidence:
`grep -rln '^import ai_agent_bridge\|^from ai_agent_bridge' --include='*.py' tests/` → 35 files
(e.g. tests/test_codex_report.py, tests/test_ab_reconcile.py, tests/test_worktree_brief.py …).

## Steps (in your existing worktree .worktrees/dispatch/kimi/infra-6812-bridge-module-identity)
1. Standardize ALL remaining bare importers under tests/ on `scripts.ai_agent_bridge`.
2. Re-run the grep above — quote the EMPTY output.
3. `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python -m pytest tests/ai_agent_bridge/test_module_identity.py -q`
   green, plus a spot-run of 3 of the newly-swept files; quote outputs.
4. Commit (trailer `X-Agent: kimi`), push same branch. Auto-merge is armed — it merges
   when CI greens. Reply on PR #6831 via REST with the evidence.

ROUTING_CARD_V1
task_id: infra-6812-sweep-completion
tier: heap
model_x_harness: kimi/k3-256k (authoring lane, CI fix on own PR)
advisor_packet: fable (this brief; defect evidence included)
owned_paths: tests/
acceptance_cmd: .venv/bin/python -m pytest tests/ai_agent_bridge/test_module_identity.py -q
alternatives_considered:
  - none — authoring-lane rule binds
parallel_free_seats: n/a