[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/grok/work-control-plane-public-p1
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

You are the Grok implementation owner for public PR #6842 in the existing dispatch worktree.

Exact head: `038cf687b33ff3adf00cc931cd9a5c26e6273ca1`.

Your fix8 residual correctly states that delegate task pagination still happens before Work filters to the canonical public repository. This does not fully satisfy the prior finding’s requirement to filter before pagination/counting. Complete the root-cause fix now.

Extend the internal read-only delegate list boundary in `scripts/api/delegate_router.py` so `_delegate_task_rows`, `list_delegate_tasks`, and `active_delegate_tasks` can accept an optional exact repository predicate and filter task state before totals, sorting/slicing/pagination, and public summary construction. Repository attribution must use only the same authoritative `repository` / `repository_id` contract: exact agreement if both exist, omit missing/blank/ambiguous/foreign. Do not infer from paths, cwd, worktree, branch, or task id. Do not expose a free-form repository selector on the public HTTP route unless it is itself a closed safe contract; Work only needs the internal call.

Then have `fetch_delegate_active` and `fetch_delegate_tasks` pass the already-admitted canonical public singleton into the production loaders, so private/unclassified volume cannot starve public rows or affect totals/truncation. Keep defense-in-depth filtering in Work. Update injectable loader signatures and tests as needed.

Add regressions with >500 newer foreign/unclassified tasks plus older public tasks proving the internal repository-scoped list returns the complete bounded public set and correct public total; verify active and all-task paths. Also prove default existing delegate API behavior remains compatible for other Monitor consumers and no new private metadata is emitted.

Keep the rest of fix8 intact, including digest `89fb9c1e...` unless this change does not touch schema. Run delegate API tests, complete Work/API/privacy/dashboard, fleet observer/dashboard, Monitor contracts, Ruff, `git diff --check`, and changed-doc checks. Commit with trailer `X-Agent: grok/work-control-plane-public-p1-fix9`, push if permitted, do not merge or change PR state. Return commit SHA, exact verification, and residual risk.