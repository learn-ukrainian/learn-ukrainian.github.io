[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/kimi/6876-kimi-diminutive
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# 6876 Kimi — diminutive strip-and-annotate (34)

CAPACITY_CARD: pick=kimi status=warm 5h headroom avoid=codex source=codexbar
#6876 stays OPEN. Do not use Kimi read-only (harness refuses). This is implement.

34 diminutives/augmentatives in the 1311 (`білочка`, `вовчище`, …). Dictionaries index the root.

## Outcome
In reenrich translation path only:
- Detect common diminutive/augmentative suffixes.
- If the **base noun** exists in the same manifest and has sourced `translation.en`, set adverb… no: set diminutive EN as base EN + `(diminutive)` / `(augmentative)` — only when base EN exists.
- VESUM-check both forms if the sources MCP / vesum path is available in-repo. Do not invent Ukrainian lemmas.
- Tests for one diminutive + one augmentative + no-fill when base lacks EN.

Worktree origin/main. Disjoint from AGY adverb work: prefer a small helper module `scripts/lexicon/derived_en_fallback.py` if that avoids colliding with AGY on `reenrich_thin_manifest_entries.py`. If you must touch the same file, stop and comment #6876 instead of overlapping.

Trailer `X-Agent: kimi/6876-diminutive`. Do not close #6876. Do not pointer-flip.
pytest + ruff on touched files.