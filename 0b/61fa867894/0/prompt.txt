[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/agy/6876-agy-goroh
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# #6876 — Горох EN fallback after slovnyk.me ukreng (no DeepL, no e2u)

## CAPACITY_CARD
pick=agy status=idle avoid=codex(deficit),cursor(in_flight),claude(CF 6978) source=delegate/active

## ROUTING_CARD_V1
- tier: practical
- model_x_harness: agy / gemini-3.7-flash-high
- why: lexicon wire of an existing UA source (`goroh_translate` in `scripts/rag/source_query.py`)
- owned_paths: scripts/lexicon/reenrich_thin_manifest_entries.py, scripts/lexicon/enrich_manifest.py
- acceptance_cmd: pytest on the new/changed tests; a fixture word with no ukreng still gets EN from goroh; e2u_reverse is NOT called
- alternatives_considered: DeepL (operator rejected; prefer UA sources), e2u_reverse (unsafe: вода→bread)
- parallel_free_seats: none reserved

## Why
Operator: prefer slovnyk.me or other **Ukrainian** resources, not DeepL.
Live probe 2026-08-17 on 10 unresolved leftovers after job `6876-slovnyk-ukreng-2078`:
- slovnyk `ukreng` empty for 9/10 (`актовий` has only the collocation «актовий зал»)
- slovnyk `vts` has UA definitions for all 10
- goroh.pp.ua EN hit 4/10: албанці, анімізм, базиліка, артикул

`goroh_translate()` already exists. It is not wired into reenrich/enrich `_translation`.

## Do
- After dmklinger / kaikki / balla / slovnyk ukreng, call `goroh_translate` for missing EN.
- Attribute via existing `source_attribution.py` goroh.pp.ua mapping. Never put it in `translation.en` as if it were Balla.
- Tests with fixtures / mocked HTML. No live HTTP in CI.
- Do **not** call `e2u_reverse` / `e2u_translate`. Do **not** add DeepL.
- Do not close #6876. Do not merge. Do not start #4696.

PR: `Refs #6876`. Trailer `X-Agent: agy/6876-goroh`.