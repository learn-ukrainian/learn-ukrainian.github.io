[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/grok/work-control-plane-public-p1
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

You are the Grok implementation owner for public PR #6842 in the existing dispatch worktree.

Exact head: `5e19bdba7f1f164af7ff011b4c13ae32af8c9fa5`.

The final independent review found a remaining privacy-boundary bypass:

- `fetch_open_issues()` and `fetch_open_prs()` in `scripts/work/sources_public.py` accept an arbitrary repository id and pass it directly to `gh`; direct invocation can target a foreign/private repository despite the closed top-level identity.

Fix the root cause by enforcing canonical public-repository admission inside both collector entry points before any runner or GitHub call. Then audit every repository-taking or repository-derived public source/normalization entry point in `scripts/work/` and the Work API for the same sibling bypass. Harden all such entry points in this same pass where needed, without broadening scope.

Add adversarial tests proving a foreign repository id:
- raises/fails closed before the runner is invoked;
- cannot affect issues, PRs, reviews, normalization, cache keys, filters, or emitted projection;
- cannot exploit alternate casing, suffix, whitespace, or missing values if those forms reach a boundary.

Keep P1 public-only, read-only, bounded, mutation-free; do not add private adapter/browser aggregation. Run the complete focused Work/API/privacy/dashboard suite, Monitor contracts if touched, Ruff on changed Python, and `git diff --check origin/main...HEAD`. Commit with trailer `X-Agent: grok/work-control-plane-public-p1-fix6`, push the existing branch, do not merge or change PR state. Return commit SHA, changed paths, exact verification results, audited entry points, and residual risk.