[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/atlas-6506-fix
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Fix CHANGES_REQUESTED + red Contracts CI on YOUR PR #6506 (LINT-003)

You authored PR #6506 (task atlas-6437-lint003). Grok verdict on the PR requires:
1. **Guard `apply_sense_honesty_tags`**: must NOT overwrite dictionary-backed `source` in
   SENSE_SOURCE_SOURCED when tagging `ai_minimum`. Add the unit test that FAILS on current
   behavior first, then fix.
2. **Docs honesty**: helpers are not wired into enrich emit paths — either wire a real call
   site or state plainly in the docs/PR that wiring lands in PR3.
3. **Red Contracts CI**: same class as PR #6501 — the fingerprint sidecar
   (`site/src/data/lexicon-manifest.fingerprint.json`) must be regenerated to cover your
   changed enrich scripts (see 6501's fix commit 7aa12c84bb for the pattern).
4. **PR-body correction**: test count says 21, reviewer measured 19 — correct the body to the
   measured number (claims must match tools).
Non-blocking items from the verdict (lexemes scan key, senseId propagation, empty-learner_en
test) go to the PR3 residual list on issue #6437, not this PR.
pytest (affected) + ruff. Push SAME branch. NO auto-merge. Report: commit SHA + item→commit map
+ green local Contracts freshness check.