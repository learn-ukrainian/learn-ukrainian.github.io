[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/release-hardening-7242
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

Fix issue #7242 (read 'gh issue view 7242' if gh works in your worktree; the spec is verbatim CF findings from PR #7241, summarized here). Harden the #7236 branch-holder release predicate in scripts/delegate.py — three findings, priority order: (1) REQUIRED: a bound task state file that EXISTS but is unparseable (JSONDecodeError -> _read_state None) currently reads as absent and can degrade toward releasable; make exists-but-unparseable a REFUSAL reason (uncertainty never reads as releasable — P0-reaper posture). (2) REQUIRED: _record_worktree_prep_failure writes unconditionally and can clobber a concurrent live record for the same task-id (guard->write race); add a pre-write re-check (refuse to overwrite a record whose status is running/spawning) — a per-task-id lock is acceptable if cheap, but the minimal atomic-enough re-check is fine. (3) DOCUMENT ONLY: the live-cwd TOCTOU is an accepted residual — extend the existing dirty-TOCTOU comment to name it; no code. Tests: one per new refusal path (unparseable-state refusal; clobber-refusal with a running record), mutation-checked (break each check -> its test fails; restore; quote output). Acceptance (quote raw output; primary interpreter /Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/pytest, from YOUR worktree root): tests/test_delegate.py tests/test_delegate_readonly_guard.py tests/test_delegate_runtime_tmp_sweep.py tests/test_delegate_ownership.py tests/test_delegate_primary_integrity.py tests/test_delegate_check_budget.py tests/test_delegate_timeouts.py tests/test_subprocess_timeout_guard.py ALL green (412+ passed); ruff clean. ONE commit (fix(dispatch): harden branch-holder release — unparseable-state refusal + refusal-record clobber guard (#7242), X-Agent trailer). NO push, NO auto-merge, NO amend after reporting. Report HEAD SHA + evidence + ready PR body (RETURN text; the driver posts).