[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/grok/infra-6949-launcher-epic
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Task: resolve issue #6949 — launchers must derive the infra epic id from the registry (stop minting epic:4707)

ROUTING_CARD_V1
- tier: practical
- model_x_harness: grok:grok-4.6 (native Grok CLI)
- why_this_tier: bounded launcher/taxonomy alignment with a derived-not-literal design goal; grok free and cool
- advisor_packet: issue #6949 (from the #6945 live-impact check) + taxonomy program #5880 (step-8 adjacency — do NOT do step 8's renames, only the epic-id derivation)
- owned_paths: scripts/lib/handoff_identity.sh (or wherever the mint lives), scripts/config/fleet_taxonomy.yaml consumers, related launcher scripts + tests
- acceptance_cmd: .venv/bin/pytest (launcher/taxonomy tests) + ruff/shell lint + a quoted probe showing a fresh capsule now carries registered dual-write + non-empty handoff paths for the infra stream + CI green on PR
- alternatives_considered: cursor (already carried the 6945 arc; spread), kimi (re-check heat)
- parallel_free_seats: kimi, deepseek

## Evidence rules (#M-4)
Quote the current mint site(s), the derived value after the fix, and the capsule probe before/after. Mutation-check the guard.

## Steps
1. Read issue #6949. Find every literal `epic:4707` mint on the launch path (handoff_identity.sh, taxonomy configs, launcher scripts) — the sweep is part of the task.
2. Fix by DERIVATION: the launcher resolves the infra epic id from the issue-stream registry (infra-harness anchor) so the next epic succession requires zero launcher changes. Keep scope to the epic-id mint — no step-8 renames, no lease-shape changes.
3. Guard test: derived-not-literal (a registry change must flow through; a literal reintroduction must fail).
4. Probe: build a capsule for a fresh infra stream id and quote registered dual-write mode + non-empty handoff paths.
5. Work ONLY in your dispatch worktree. Push if your harness allows; otherwise commit and say so (driver finalizes). PR (Fixes #6949).
6. Do NOT arm auto-merge and do NOT merge.