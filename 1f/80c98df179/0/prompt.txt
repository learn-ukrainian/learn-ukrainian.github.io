[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/infra-6803-readonly-guard-entire
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Dispatch brief — infra-6803-readonly-guard-entire

**Issue:** #6803 — read the full issue AND its comments first (`gh issue view 6803 --comments`).
The retry comment matters: the guard fires inside dispatch worktrees too, not only at repo root.

## Evidence preamble (#M-4, binding)
Every claim in your report and PR must be tool-backed: state the claim, the deterministic
command that proves it, and quote the raw output. No unverified assertions.

## Shippable unit (one PR)
Fix the read-only dispatch guard so harness-generated `.entire/` telemetry does not fail a
read-only dispatch — at repo root AND inside dispatch worktrees — while genuine
task-authored mutations still fail the guard.

You own the design: choose the correct layer for the fix (guard classification vs telemetry
placement vs something better you find) and defend the choice in the PR description with
the trade-off you rejected.

## Acceptance criteria (verify BEFORE opening the PR)
1. Regression tests: (a) a read-only dispatch whose only tree delta is `.entire/**`
   settles clean; (b) a genuine mutation (e.g. a tracked-file edit) still fails the guard.
   Cover both the repo-root and worktree layouts named in the issue.
2. Mutation-check the new tests (#M-16): revert your fix locally → the tests must fail;
   quote that run.
3. `.venv/bin/pytest` green on affected test files; `.venv/bin/ruff check` green on changed files.
4. PR body quotes the guard's before/after behavior (raw output).

## Out of scope
- Any change to Entire capture behavior beyond how its telemetry is placed/classified.
- No changes to dispatch mode semantics (read-only must still reject write-shaped prompts).

## Process (numbered, binding)
1. Work only inside your dispatch worktree (already created for you).
2. Implement + tests.
3. Run pytest (affected files) + ruff; quote outputs.
4. Conventional commit(s) with trailer `X-Agent: cursor`.
5. Push; open PR titled `fix(delegate): ... (#6803)` referencing #6803.
6. Do NOT enable auto-merge; do NOT merge. Review + merge stay with the orchestrator.

---
ROUTING_CARD_V1
task_id: infra-6803-readonly-guard-entire
tier: practical
model_x_harness: cursor/auto (Ultra month first-pick, operator 2026-08-13)
why_this_tier: bounded guard-classification fix with clear acceptance; mechanical-with-judgment fits Cursor Auto
advisor_packet: fable (this brief)
owned_paths: scripts/ (dispatch guard + its tests)
acceptance_cmd: .venv/bin/pytest <affected test files> && .venv/bin/ruff check <changed files>
alternatives_considered:
  - kimi/k3-256k — free — taken by #6812 (disjoint work)
  - deepseek/v4-flash — transport down on this host (hermes binary missing, #6805)
parallel_free_seats: kimi, agy, codex free per /api/orient headroom (all true, 0 in-flight)