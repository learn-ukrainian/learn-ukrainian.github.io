[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/5737-agy-worker-briefs
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Task: #5737 Route gemini-flash/agy as worker with complete task briefs

Stream: orphan (link to #4707 in PR body). Closes #5737.

## Problem
Fast cheap AGY/gemini-flash lane was left to self-decompose → many micro-PRs → CI queue starvation. Structural fan-out fixed elsewhere; this is the behavioural/process half.

## Acceptance (from issue — all required)
1. `agents_extensions/shared/rules/model-assignment.md` states explicitly that agy/gemini-flash receives **complete task briefs** and does **not** self-decompose into serial micro-PRs.
2. `docs/best-practices/agent-activity-matrix.md` reflects the same worker-vs-orchestrator boundary for agy.
3. Dispatch-brief guidance requires a brief to name **one shippable unit** and its **acceptance criteria**, not an open-ended objective. Update the canonical place(s) agents already read (e.g. workflow.md dispatch section, SCRIPTS.md, or dispatch-brief template docs — pick real existing homes, do not invent parallel policy).
4. Note the merge-cadence soft check intent: no lane should routinely land >5 PRs/day against one feature area (document as operator/orchestrator expectation; no new flaky automated enforcer unless one already exists and is trivial to extend).

## Owned paths
- `agents_extensions/shared/rules/model-assignment.md`
- `docs/best-practices/agent-activity-matrix.md`
- Existing dispatch-brief / workflow guidance docs only as needed for AC #3

## Out of scope
- Changing model catalog pins
- CI workflow job count
- Implementation code beyond docs/rules

## Validation
- markdownlint clean on touched files if project lint applies
- Open PR with `Closes #5737`, X-Agent trailer, mention stream #4707

## Research classification
- role: implementer
- task-family: process / routing policy
- track: infra-harness