[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/agy/work-6849-projection-health
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Fix issue #6849 — Work public-monitor falsely `unavailable`; issue health floods UNKNOWN; fleet_reviews AttributeError

You are an implementation agent in a dispatch worktree. Read GitHub issue #6849 in full,
INCLUDING the triage comment (root-cause diagnosis is recorded there), before writing code.

```
ROUTING_CARD_V1
tier: practical
model_x_harness: gemini-3.7-flash-high × agy
why_this_tier: bounded backend bug fix; expected behavior fully specified by reporter on the
  issue; root cause already diagnosed and posted on the issue by the infra driver
advisor_packet: n/a (tier=practical)
owned_paths: scripts/work/**, scripts/api/fleet_router.py, tests/**
acceptance_cmd: see "Acceptance" below (curl probes + pytest)
alternatives_considered: kimi-K3 (max-effort-only — cost overkill for a bounded fix),
  codex (lane capped until ~2026-08-20 reset)
parallel_free_seats: second agy task carries #6850 (frontend, disjoint owned paths)
```

## Evidence discipline (#M-4)
Every claim in your PR description and final report must be tool-backed: name the
deterministic command and quote its raw output. No asserted-but-unprobed "works now".

## Scope (all three symptoms of #6849 — see the issue's Expected section)
1. On a healthy Monitor, the `fleet_reviews` section of `GET /api/work/v1/projection`
   must be `ok` — no `fleet_reviews_error:AttributeError`. Fix the cause at the right
   layer, not by swallowing the exception.
2. When the core GitHub enumerations (issues, prs) succeed, the `public-monitor` source
   status must NOT read `unavailable` because optional/class-4 sections (streams,
   fleet_reviews) failed. Use a degraded-class status while keeping every omission
   visible in the per-section metadata. Do not hide failures; re-classify them honestly.
3. A missing streams cache (fresh Monitor start) must not leave the board useless:
   real PR health signals must not be buried under a 100-row UNKNOWN pile. The
   reporter accepts either restoring streams availability at Monitor start or keeping
   issue health honest without pretending the source is down — choose the root-cause
   layer and justify the choice in the PR description.

Non-goals: no changes to the private adapter, no redaction-contract changes, no
attention-default UX change (that is #6850, running in a parallel task — do not touch
`dashboards/work.html`).

## Steps
1. Work only in your dispatch worktree (already created for you).
2. Read issue #6849 + its triage comment. Re-probe the live symptoms yourself:
   `curl -s http://127.0.0.1:8765/api/work/v1/projection` (quote section statuses).
3. Implement the fix. Every changed function gets a test; guard tests must be
   mutation-checked (#M-16: revert the fix → test fails; re-apply; state this in the PR).
4. Run the affected pytest set locally and quote the pass/fail counts.
5. `.venv/bin/ruff check` on every changed Python file — must be clean.
6. Conventional commit (e.g. `fix(work): ...`), reference `#6849`.
7. Push the branch; open a PR titled for #6849 with the evidence in the body.
8. Do NOT enable auto-merge and do NOT merge — the infra driver owns review + merge.

## Acceptance
- `curl -s http://127.0.0.1:8765/api/work/v1/projection` → `fleet_reviews` section `ok`;
  source `public-monitor` not `unavailable` while issues+prs sections are `ok`.
- Simulated missing-streams state (test-level) → the two behaviors in Scope item 3 hold.
- Affected pytest set green; ruff clean.