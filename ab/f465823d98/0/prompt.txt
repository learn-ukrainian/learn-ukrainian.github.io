[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/kimi/6876-kimi-venv
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# 6876 Kimi — runner venv has yaml/jsonschema

CAPACITY_CARD: pick=kimi status=warm rem=33% weekly / 84% 5h will_last=true avoid=codex source=codexbar
ROUTING_CARD_V1: tier=practical model_x_harness=kimi k3-256k why=everyday coding; last atlas-runner job died ModuleNotFoundError: yaml then systemd success; operator use Kimi

Campaign #6876. Branch from origin/main. Worktree required. Trailer `X-Agent: kimi/6876-venv`.
Do not merge. Do not flip pointers. Do not edit atlas_job.py (AGY owns free-disk). Do not edit atlas_jobs_router.py (GLM is reviewing).

## Outcome
`launch_reenrich_class_b.sh` (and remote wrapper if needed) fail closed unless the **workdir/repo venv** can `import yaml` and `import jsonschema` before systemd-run. Prefer the project `.venv` on the runner (`$REPO/.venv/bin/python` or `$CODE_ROOT/.venv`). If missing, print a one-line install recipe and exit 1 — do not apt-get as root, do not succeed without the imports.

Add a test (script-level or pytest) that the launcher checks this. Conventional commit + push + PR linked to #6876.

`.venv/bin/python -m pytest` on the new/changed tests + ruff on touched files.

Owned paths only:
- scripts/lexicon/runner/launch_reenrich_class_b.sh
- scripts/lexicon/runner/launch_reenrich_class_b_remote.sh
- tests/test_launch_reenrich*.py (or a new small test)