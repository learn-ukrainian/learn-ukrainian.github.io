[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/6458-ci-ruff-fix
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Cursor Auto — fix CI on PR #6458 (mid-definition truncation)

## Why you (Cursor Auto)
Operator 2026-08-08: **Cursor free Auto is heavily underused** while Codex is near cap.
CodexBar (live): Cursor primary ~13% used (behind), **tertiary/Auto ~2.6% used (farBehind, ~20% reserve)**; Codex secondary **95% near_cap**. Prefer Cursor Auto for this mechanical CI fix.

## Goal
Make **PR #6458** (`fix(atlas): stop mid-definition truncation of dictionary cards (#6437)`) **CI green**, then ready for cross-family review + merge.

Branch: `grok/6437-no-mid-definition-truncate`
Issue: #6437 / program #6460

## Root cause (tool-backed from CI job 93047855198)
The "Python (pytest)" jobs fail on the **post-pytest ruff step**, not on test assertions:

```
ruff check scripts/
SIM103  scripts/lexicon/enrich_manifest.py:4636  return condition directly
SIM108  scripts/lexicon/enrich_manifest.py:5504  use ternary for if/else start assignment
Found 2 errors.
```

## Required work
1. Work **only** in the assigned worktree on branch `grok/6437-no-mid-definition-truncate` (layout A). Do **not** edit primary checkout.
2. Fix the two ruff findings in `scripts/lexicon/enrich_manifest.py` without changing behavior.
3. Re-run locally:
   - `.venv/bin/python -m ruff check scripts/lexicon/enrich_manifest.py`
   - Targeted tests related to definition/truncation if present; at minimum ruff clean.
4. Commit with `X-Agent: cursor/6458-ci-ruff-fix` trailer.
5. Push to the same branch (updates PR #6458). Do **not** merge; do **not** self-review as formal CF.
6. Report: SHA, files changed, ruff output, PR URL.

## Constraints
- Do not touch `.python-version`, `.yamllint`, `.markdownlint.json`.
- Do not regenerate status/audit artifacts.
- Do not invent partial-done bars.
- Disk ~91% — no bulk re-enrich, no large temps.
- Formal CF path is direct ask-* only (review-pr retired).

## Success
CI Gate green on #6458 (or only remaining failures are unrelated flakes you document with log evidence).