[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/agy/atlas-3797-cloze-phase2
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# atlas-3797-cloze-phase2

Drive **Phase 2** of #3797 (reviewed cloze). Phase 0 generator and Phase 1 Tatoeba yield path already merged (#3859, #5498, #5183).

## ROUTING_CARD_V1
- tier: practical
- model_x_harness: agy / gemini-3.7-flash-high (language-lane)
- why_this_tier: Ukrainian cloze review; not architecture
- advisor_packet: n/a
- owned_paths: lexicon-practice cloze allowlist / issue #3797
- acceptance_cmd: named candidate count + first reviewed batch OR a tool-proved blocker posted on #3797
- alternatives_considered: claude (on 673 leftover), cursor (not language), codex (deficit)

## CAPACITY_CARD
pick=agy status=cool rem≈98% will_last=true avoid=codex source=routing-budget

## Work
1. Read #3797 body + comments. Quote what Phase 1 actually shipped (PR SHAs). Do not re-do Phase 0/1.
2. Phase 2 is **review at scale**: Russianism/calque/CEFR/case/naturalness → attest approved into allowlisted `lexicon-practice` cloze sources.
3. Start a **bounded** first review batch (not the whole 188k). Propose size with a denominator. VESUM/`sources` every UA claim.
4. If the generator output is missing on disk, say so with paths — do not invent sentences.
5. PR only for attested approved items + tests. Do not auto-merge. Do not close #3797 until Phase 2 ACs are met.

## Non-goals
- Phase 3 UI attribution
- Audio / #4696
- Lowering cloze quality bars