[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/kimi/infra-6805-reviewer-transport-residual
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Dispatch brief — infra-6805-hermes-removal (red CI on PR #6837 + operator policy change)

Two things, same branch (kimi/infra-6805-reviewer-transport-residual):

## 1. Red CI — admission probe fires on replay paths (real ordering bug)
CI run 31910182381: `tests/ai_agent_bridge/test_ask_contract.py` — 3 replay-semantics
tests (`test_terminal_failure_replays_without_invoking_provider_again`,
`test_terminalization_conflict_preserves_completed_provider_response`,
`test_claim_race_to_terminal_replays_without_invoking_provider`) die with
`ACP participant 'agy': agy binary not found on PATH` from your new
`probe_participant_reachability` at admission. Those paths REPLAY a terminal response
and never invoke the provider — a missing binary must not fail them. Fix the ordering:
probe reachability only when a real provider invocation would occur (after the
terminal-replay short-circuit), and keep the tests hermetic from host PATH state.
Mutation-check (#M-16): revert the ordering → the 3 tests fail again; quote it.

## 2. OPERATOR POLICY CHANGE (verbatim order 2026-08-16): "i removed hermes, so you
have to route gemma to opecode and not hermes"
- Gemma already rides opencode in this branch (AcpxGemmaShadowAdapter) — VERIFY no
  hermes reference is load-bearing anywhere in the gemma path and state the proof.
- Hermes is REMOVED from this host permanently, not awaiting reinstall. Update what
  this PR shipped accordingly: the hermes-seat remediation text and the runbook
  section must say the DeepSeek seat routes via opencode
  (`opencode run --model deepseek-direct/deepseek-v4-flash`) as the STANDING path —
  not "reinstall Hermes per runbook". Keep the admission-time refusal shape for any
  route that still names a hermes binary, but its message points to the opencode path
  as the route, not a reinstall.
- Do NOT change routing policy beyond this (which seats review what stays untouched).

## Acceptance (verify BEFORE pushing)
1. The 3 replay tests + your 386-test set green locally with NO agy/hermes binary
   assumptions (quote).
2. Grep-proof: no remaining "reinstall" hermes instruction in code/runbook; gemma path
   hermes-free (quote commands + output).
3. ruff + markdownlint green on changed files.
4. Push same branch; auto-merge is armed. Reply on PR #6837 via REST with evidence,
   trailer `X-Agent: kimi`.

ROUTING_CARD_V1
task_id: infra-6805-hermes-removal
tier: practical
model_x_harness: kimi/k3-256k (authoring lane, CI fix + policy delta on own PR)
advisor_packet: fable (this brief; operator order quoted verbatim)
owned_paths: scripts/agent_runtime/, scripts/ai_agent_bridge/, docs/runbooks/, tests/
acceptance_cmd: .venv/bin/python -m pytest tests/ai_agent_bridge/test_ask_contract.py tests/agent_runtime/test_acpx_adapter.py -q
alternatives_considered:
  - none — authoring-lane rule binds
parallel_free_seats: claude free