{"metadata":{"actual_model_known":"false","entrypoint":"delegate","harness":"cursor-headless","requested_model":"auto","runner_agent":"cursor"},"repo_path":"/Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/grok-launcher-unstick","session_id":"fleet-e443a8c534a044069244825ba9d3ec8f","timestamp":"2026-08-13T04:34:20.550052Z","type":"session"}
{"text":"[delegate worktree]\nRun all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/grok-launcher-unstick\nDo not switch branches in the main checkout.\nIf you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.\n\n[shared project interpreter]\nNever create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.\nSparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).\n\n[optional delivery signal]\nCommits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {\"outcome\":\"no_change\",\"reason\":\"why no changes are required\"}`. This line is optional — its absence never fails the dispatch.\n\n# Task: stop start-grok.sh / start-grok-driver.sh from pinning model/effort\n\n## CAPACITY_CARD\npick=cursor status=cool will_last=true avoid=codex(deficit) source=codexbar\n\n## ROUTING_CARD_V1\n- tier: practical\n- model_x_harness: cursor Auto\n- why: mechanical launcher argv; Cursor reserve; Codex deficit\n- owned_paths:\n  - scripts/lib/launcher_core.sh\n  - scripts/launchers/grok.sh\n  - tests/test_start_grok_launcher.py\n  - tests/test_launcher_contract.py (only if it asserts grok default model)\n- acceptance_cmd: LAUNCHER_DRY_RUN=1 ./start-grok.sh  → argv is `grok` with NO `--model` and NO `--effort`; same for start-grok-driver.sh --epic infra (after lease dry-run). Explicit `--model grok-4.5` still injects. Uncertified `--model grok-unknown` still exits 4.\n\n## Operator rule\nGrok interactive and driver must **not stick** model or effort. Same contract as Claude: omit `--model` / `--effort` unless the caller passed `--model` / `--effort` or set `LAUNCHER_MODEL` / `LAUNCHER_EFFORT`. Empty means the Grok TUI keeps the last session selection (today that can be 4.6; pinning grok-4.5 is the defect).\n\n## Current defect (tool-backed)\n- `start-grok.sh` / `start-grok-driver.sh` are thin wrappers (`launcher_main grok interactive|driver`).\n- `launcher_defaults` grok branch: `LC_MODEL=\"${LAUNCHER_MODEL:-grok-4.5}\"` — always sticks.\n- `scripts/launchers/grok.sh` `launcher_adapter_exec`: always `cmd=(grok --model \"$LC_MODEL\")`.\n- Effort is already empty by default (`LC_EFFORT=\"${LAUNCHER_EFFORT:-}\"`) and grok adapter does not pass `--effort` / `--reasoning-effort`. Keep it that way: only inject if LC_EFFORT is non-empty (and then use grok CLI's `--reasoning-effort` / `--effort` alias). Do not invent a default effort.\n\n## Required changes\n1. `launcher_defaults` grok: `LC_MODEL=\"${LAUNCHER_MODEL:-}\"` (mirror claude comment).\n2. `launcher_adapter_exec` in `scripts/launchers/grok.sh`: start `cmd=(grok)`; append `--model \"$LC_MODEL\"` only when non-empty; append `--reasoning-effort \"$LC_EFFORT\"` only when non-empty.\n3. `launcher_validate_driver_certification`: treat empty grok model like empty claude model (skip pin check). If a model IS provided, keep the existing certified allowlist (`grok:grok-4.5` and add `grok:grok-4.6` if that ID is already used in-repo — do not invent extra SKUs; if 4.6 is not yet in the certification case, omit it and let an explicit unknown 4.6 fail 4 until catalog catches up, OR accept grok-4.6 if tests/catalog already name it). Check `scripts/config/model_catalog.yaml` before adding a cert ID.\n4. Help text in `launcher_usage`: Grok omit keeps last TUI/session model, same as Claude.\n5. Tests in `tests/test_start_grok_launcher.py`:\n   - dry-run `start-grok.sh` must contain `would exec grok` and must NOT contain `--model grok-4.5`.\n   - dry-run with `--model grok-4.5` still contains `--model grok-4.5`.\n   - dry-run with `--effort high` injects `--reasoning-effort high` (or `--effort high` if that is what the adapter uses).\n   - dry-run without --effort must not inject effort flags.\n   - driver `--epic infra` still claims lease / canary / bind; no stuck model in argv.\n   - uncertified `--model grok-unknown` still exit 4.\n6. Do not change Codex/Gemini/Kimi/GLM defaults. Do not flip plane. Do not claim leases.\n\n## Constraints\nLayout A worktree only. `.venv/bin/python`. ruff on Python tests. Conventional commit + `X-Agent: cursor/grok-launcher-unstick`. Open PR, no merge.\n\n## Proof in PR body\nQuote `LAUNCHER_DRY_RUN=1 ./start-grok.sh` argv and `LAUNCHER_DRY_RUN=1 ./start-grok-driver.sh --epic infra` argv.\n","timestamp":"2026-08-13T04:34:20.550052Z","type":"user"}
