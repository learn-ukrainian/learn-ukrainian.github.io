[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/agy/ui-codex-lockdown-7202
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Task: #7202 — close the UI-bridge lockdown gap left by #7197 (one small PR)

Read `gh issue view 7202 -R learn-ukrainian/learn-ukrainian.github.io`. PR #7197 (your lane) made
`CodexAdapter.build_invocation` emit `--disable apps` unconditionally so dispatched Codex workers cannot
reach write-capable GitHub app connectors. The cross-family review found one path outside the adapter:
`scripts/ai_agent_bridge/_ui_codex.py:139` builds a raw `["codex", "exec", "resume", "--json", thread_id, "-"]`
subprocess (exposed via `_cli.py:1168`). That lane is not locked down.

## Outcome
No `codex` argv is built anywhere under `scripts/` without the connector lockdown. Prefer routing the UI
bridge spawn through the adapter; if that is disproportionate for a resume-only bridge, emit the same
unconditional `--disable apps` at that site — say which you chose and why in the PR body.

## Also fold (review nits from #7197, same files)
- `tests/test_agent_runtime.py::test_codex_adapter_disables_apps_connector_in_review_isolation` still
  passes with the lockdown reverted because the isolation tool-config already lists `apps`; make it
  prove the unconditional line (or replace it with an assertion that does).
- `_tool_config_flags` docstring (`scripts/agent_runtime/adapters/codex.py:353-362`) should say `apps`
  is filtered there because it is emitted unconditionally. Keep this edit minimal — #7197 is still
  unmerged and touches the same file; do not restructure anything near its hunks (~lines 330-340, ~411).

## Acceptance (quote raw output)
1. `grep -rn '"codex"' scripts/ --include='*.py' | grep -v tests` — every argv builder outside the adapter
   either routes through it or carries the flag; quote the grep and disposition per hit.
2. Test for the UI bridge path asserting `--disable apps` is in the FINAL argv; mutation-check: revert the
   site fix → test fails; quote both runs.
3. `.venv/bin/pytest -q tests/test_agent_runtime.py` + the bridge's test file(s) green; `.venv/bin/ruff check`
   clean on changed files.
4. One conventional commit (`fix(bridge): apply codex connector lockdown to UI resume spawn (#7202)`,
   X-Agent trailer, `Refs #7181`). NO push, NO auto-merge, NO amend after reporting. Report HEAD SHA +
   evidence + ready PR body.