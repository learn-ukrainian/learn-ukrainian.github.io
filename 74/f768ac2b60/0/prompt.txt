[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/agy/sources-7040-volume-order
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Dispatch brief — #7040 CF delta: search_esum volume bind order

CAPACITY_CARD: pick=agy status=cool avoid=codex,cursor,gemini(CLI retired) source=routing-budget
ROUTING_CARD_V1:
- tier: practical
- model_x_harness: agy / gemini-3.7-flash-high
- why_this_tier: author's PR; mechanical SQL bind order
- owned_paths: scripts/wiki/sources_db.py; tests/test_sources_db.py
- alternatives_considered: kimi (CF author — cannot implement own CR)

Initiator: grok-infra. AGENT_NO_MERGE=1.

## Work
PR #7040 branch `agy/sources-7028-rank` head 09272caa1f9ef918ab18062a03a11d04f9111637.
Kimi CF CHANGES_REQUESTED: `search_esum` binds `volume` in the wrong placeholder order.
With `volume=2`, meta/headword path returns 0 rows (stressed `кни́га` found without volume, dropped with volume=2).

Fix param order in `scripts/wiki/sources_db.py` ~2427-2444. Add a test `search_esum(..., volume=...)` that fails on the old order. Do not touch `.mcp/servers/sources/server.py` (#7038 owns contracts).

Push to the existing PR branch. Trailer `X-Agent: agy/sources-7040-volume-order`. Do not merge.

## Verify
```bash
.venv/bin/ruff check scripts/wiki/sources_db.py
.venv/bin/python -m pytest -q tests/test_sources_db.py -k 'esum or volume'
```
Quote the volume-filter test node id.