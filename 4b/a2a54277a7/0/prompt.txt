[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/kimi/reservation-supersede-fix
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Fix P1 review finding on your PR #6421

Sealed cross-family verdict: CHANGES_REQUESTED (claude-sonnet-5, review_8de6811f32d74e52bba1211ec19b029a).
One P1 finding, category "regression", at `scripts/fleet_comms/routing_reservations.py:354-368`:
**"Active-reservation join no longer runs for substitution requests."**

Detail (reviewer, verified quote): before your change, the statement
`if latest is not None and str(latest["status"]) in _ACTIVE_STATUSES: return self._reservation_from_row(latest)`
ran unconditionally AFTER the if/elif — including after `_validate_substitution_tx` in the
substitution branch. Your refactor moved it inside `elif latest is not None:`, which is only
reached when `substitution_data` is falsy — so a truthy `substitution_data` with an ACTIVE
latest reservation now falls through to `selector(...)` and creates a brand-new reservation
instead of joining the existing active one.

Reviewer's smallest fix: restore the active-join as a statement after the if/elif block (or
re-check active status inside the substitution branch too), AND add a test covering
substitution_data + active latest → returns the existing reservation.

## Steps
1. Apply the fix (keep your terminal-supersede behavior intact — its test must stay green).
2. Add the substitution+active-join test.
3. Mutation-check: remove the restored join → your new test must fail; restore it.
4. Full run: the tests/fleet_comms files you touched + ruff on changed files.
5. X-Agent trailer, push --force-with-lease, PR comment quoting the green output and the
   mutation-check proof. No auto-merge.