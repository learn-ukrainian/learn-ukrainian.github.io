# Dependencies
node_modules/

# OS files
.DS_Store
Thumbs.db

# Environment files
.env
.env.local
.env*.local

# Secrets / credentials (defence in depth — gitleaks is the real gate)
credentials.json
credentials.*.json
*_credentials.json
*.pem
*.key
id_rsa
id_ed25519
secrets/
.secrets/
*.secret
api_keys.*
service-account*.json

# AI assistant configs
#.gemini/
.claude/
.codex/


# Python
__pycache__/
*.py[cod]
*.pyo
.Python
*.so

# Build/Output

dist/
/build/
*.tsbuildinfo
output/json/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Logs
logs/
*.log
npm-debug.log*

# Database
*.db
*.sqlite
*.sqlite3

# Temporary files
tmp/
temp/
*.tmp
*.pid
.venv/
.agent/
# Temporary/sample images in docs (textbook screenshots, vision test outputs)
docs/temp_*.png
docs/bolshakova_*.png

gha-creds-*.json
.pytest_cache/

# ULP mapping intermediate files (build artifacts)
docs/resources/ukrainianlessons/resource_module_scores.json
docs/resources/ukrainianlessons/resource_module_scores_v*.json

# Dobra Forma local copy
docs/references/dobra-forma/opentext.ku.edu/

# Private references (copyrighted materials for personal verification only)
docs/references/private/

# Temporary audit and pipeline files (root folder)
*_audit_results*.txt
audit_report*.txt
pipeline_*.txt
*_mapping.txt
missing_*.txt
missing_*.json
test_*.txt
enrichment_*.json
recovery_*.json
issue_*.json
*_report.json

# Temporary markdown files (root folder)
temp*.md
/tmp*.md
/content.md
/content_*.md
/review.md
/review_*.md
/testbed.md
/CODEBASE_QUALITY_REVIEW.md
/claudes_response.md
*_BRIEF.md
*_PROMPT.md
issue_*_body.md

# Temporary text/html/patch dumps at the repo root
/content.txt
/tmp*.txt
/test.txt
/response.html
/*.patch
/P

# Damage report snapshots from scripts/audit/damage_report.py — generated
# per-run, timestamped, and can be regenerated any time. Do not version.
docs/damage-report-*.md
docs/damage-report-*.csv

# Stray top-level working files from ad-hoc agent dispatches
/response.md
/fix-markers.yml

# Root-level scripts and data (all scripts go in scripts/)
/*.py
/*.js
!start-claude.sh
/*.yaml
!docker-compose.qdrant.yaml
!.pre-commit-config.yaml
/*.csv
/*.json
!package.json
!package-lock.json
!tsconfig.json
!.mcp.json
!.markdownlint.json
!pyrightconfig.json
!dagger.json

# Codex interactive worktree (start-codex.sh)
.worktrees/
!.prettierrc
!jest.config.js
!eslint.config.mjs

# Batch runner state (generated at runtime)
batch_state/

# Orchestration build artifacts — TRACKED for pipeline analytics
# Lock files and raw LLM dumps are excluded; everything else is valuable data
# (prompts, reviews, audit logs, state, completion reports)
**/orchestration/**/state.json.lock
**/orchestration/**/*-raw-output.md

# Archive of old content (pre-rebuild snapshots)
_archive/

# MCP broker runtime files
.mcp/servers/message-broker/pids/
.mcp/servers/message-broker/*.db-shm
.mcp/servers/message-broker/*.db-wal

# Test artifacts
test_git_mtime/

# Downloaded textbook PDFs (561 MB+, not for version control)
data/textbooks/

# RAG pipeline artifacts (generated at runtime)
data/qdrant/
data/qdrant_db/
data/textbook_images/
data/textbook_chunks/
data/literary_texts/*.bin
data/literary_texts/*.npy
data/vesum/
data/vesum.db
data/backups/
data/esu/urls.jsonl
data/esu/chunks.jsonl
data/zno/
data/embeddings/
embed-venv/

# Starlight
starlight/dist
starlight/node_modules
starlight/.astro
/testbed/*
!/testbed/reference/
data/sum11/
data/frazeolohichnyi/
data/ubertext-freq/
data/balla-en-uk/
data/dmklinger-uk-en/
data/ukrajinet/
data/wiktionary/
data/puls/
data/literary_texts/*.jsonl
data/external_articles/
data/antonenko-davydovych/
data/ua-gec/

# ESUM re-OCR feasibility (issue #2001) — raw JP2 scans pulled from
# Archive.org and intermediate per-engine OCR outputs. The committed
# deliverable is audit/etymology-ocr-feasibility/REPORT.md only.
data/raw/esum/jp2-staging/
data/raw/esum/gemini-ocr/
audit/etymology-ocr-feasibility/raw-outputs/
audit/etymology-ocr-feasibility/bulk-run-log.jsonl

# Grinchenko 1907 raw OCR landing — out-of-band per-page transcription
# probes (no current ingestion pipeline; canonical Grinchenko ingest reads
# from data/grinchenko/json_raw/). Pages may be deposited here while the
# pipeline is designed.
data/raw/grinchenko-1907/

# OCR temporary PNG staging directory (bulk_ocr_gemini.py uses this as
# scratch; cleared between runs; never committed).
gemini_ocr_images/

# Unidentified-source OCR scratch landing — when an agent or run drops
# `transcription.md` at repo root without a known dictionary destination,
# move it here pending triage. See follow-up issue tracking source-identification.
data/raw/orphan-ocr/
/transcription.md

.build.lock

# Corpus gap audit working files — only the human-reviewed deliverables land in git
data/corpus_audit/*
!data/corpus_audit/coverage_map.json
!data/corpus_audit/gap_categories.md
!data/corpus_audit/ingestion_roadmap.md
!data/corpus_audit/draft_tickets/
!data/corpus_audit/draft_tickets/*.md


.gemini/

# SQLite WAL/SHM sidecars — same exclusion as the .db files themselves.
data/*.db-shm
data/*.db-wal
# Test-only SQLite WAL/SHM sidecars (e.g. test_wal.db-* from pytest fixtures)
test_*.db-shm
test_*.db-wal

# Session-state handoffs that authors marked KEEP_UNTRACKED_UNTIL_PUBLIC_OK
# (e.g. eval results pending validation) live under a `private/` subdir,
# matching the existing `docs/references/private/` convention.
docs/session-state/private/

# the native reviewer's teaching material (large, local-only, IP-sensitive)
data/native-reviewer-lessons/

# Dispatch briefs — historical prompts for codex/claude/gemini delegations.
# Each brief is consumed once, producing a PR; keeping them in the repo just
# pollutes the working tree across sessions. See #1518 session for the
# rationale.
.worktree-briefs/

# Database backups (timestamped). The `*.db` blanket above does NOT match
# `*.db.bak-YYYYMMDD-HHMMSS`. These are local recovery artifacts only —
# always 100MB+, well over GitHub's hard limit, never belong in version control.
data/*.db.bak-*
data/*.db.bak

# Stray top-level diff/patch artifacts left over from ad-hoc work.
# `patch.diff` shows up when someone exports a quick fix to disk and forgets
# to clean up. Never belongs in version control.
/*.diff
/*.patch

.envrc

# graphify outputs — knowledge graph artifacts written by `/graphify <path>` or
# `graphify extract`. Includes graph.json (queryable), graph.html (viz),
# GRAPH_REPORT.md, Obsidian vault, wiki/, cache/ (SHA256 extraction cache). All
# re-derivable from source via `graphify update <path>` (AST-only, free).
# Single full graph can be 50MB+; vault has 100s of files. Never in VCS.
graphify-out/
**/graphify-out/

# V7 build telemetry — intermediate artifacts emitted per module by
# scripts/build/v7_build.py. These are large, machine-regenerated on every
# build, and forbidden in PRs by the AGENTS.md pre-submit checklist
# ("generated artifacts"). Scoped to the curriculum tree so unrelated files
# elsewhere with the same names aren't affected. Added 2026-05-13 after a
# stash containing the dirty post-build tree was dropped and ~30min of
# validated build output was lost — recurrence prevention.
curriculum/l2-uk-en/*/*/python_qg.json
curriculum/l2-uk-en/*/*/wiki_manifest.json
curriculum/l2-uk-en/*/*/writer_output.raw.md
curriculum/l2-uk-en/*/*/writer_tool_calls.json
curriculum/l2-uk-en/*/*/knowledge_packet.md

# Etymology data manifest is NOT gitignored — it's the build-time data source
# for the Astro dynamic route at starlight/src/pages/etymology/[slug].astro,
# consumed via getStaticPaths(). github.io's build runner has no access to
# data/sources.db (1.5 GB, not in repo), so the manifest must be checked in
# so the deploy pipeline can build pages from it. ~27 MB JSON, infrequent
# updates (ESUM volumes are a stable published reference work).
#
# Regenerate when ESUM data changes:
#   .venv/bin/python scripts/etymology/build_data_manifest.py
data/eval-archive/

# clawpatch tool output (config + findings + locks + patches — regenerable)
.clawpatch/

# antigravity-cli (agy) per-project state — symlinks into
# ~/.gemini/config/projects/, owned by the agy binary, regenerable. Kubedojo
# owns the agy migration; the dir is a local-only side-effect of agy running
# in this repo.
.antigravitycli/

# Raw ESUM OCR outputs (per-volume .txt files; jp2-staging/ + gemini-ocr/ already covered)
data/raw/esum/*.txt
