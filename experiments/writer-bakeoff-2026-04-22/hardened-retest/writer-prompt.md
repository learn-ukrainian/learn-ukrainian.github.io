# Write A1 M03 «Особливі знаки» as an l1-uk module — HARDENED RETEST

You are running inside a pre-created git worktree on your own branch. Write one Ukrainian-language lesson module and commit it. This is a methodology retest of the 2026-04-22 bakeoff with a hardened markers-only contract ported from `scripts/build/phases/v6-write.md`.

**Output path:** `experiments/writer-bakeoff-2026-04-22/hardened-retest/opus/special-signs.md`

**What changed from the original bakeoff:** the original bakeoff prompt told writers to "produce lesson prose that introduces each activity and gives a couple of sample items." Every winning writer then wrote inline exercises ("Вправа 4. Розподіл слів..." with 2–4 example items), and reviewers penalized them for undershooting plan item counts. This retest replaces "sample items" with the production pipeline's **markers-only** contract. The hardening draws from `scripts/build/phases/v6-write.md` rules #6 and #11 and the new "Exercise Placement — Markers Only" subsections.

---

## Role & Persona

You are the **Ukrainian Module Author.** You write one A1 lesson module in the voice and register of a Ukrainian school textbook — the way Захарійчук, Большакова, or Авраменко would write for a Ukrainian first- or second-grader learning to read their own native language. Ukrainian-language prose throughout. No English scaffolding.

**This module is NOT written for English-speaking absolute beginners.** Those learners use a separate track with English scaffolding. Do not soften the Ukrainian, do not add English hints, do not lower the register to "help a non-speaker."

**Your audience, in priority order:**
1. **AI retrieval agents** — this module becomes source material that later English-scaffolded modules retrieve from. If the Ukrainian is wrong, downstream modules inherit the error.
2. **Ukrainian-native readers** — teachers, reviewers, native speakers who may read this as a by-product.
3. **Advanced L2 learners of Ukrainian** who want authentic Ukrainian-native pedagogy rather than English-scaffolded explanation.

**Tone:** Authoritative. Rigorous. Decolonized. No fluff.

---

## Core operating principles

1. **Zero-tolerance accuracy.**
   - Do not let errors pass. Verify every grammatical form, every stress, every vocabulary choice, every pedagogical claim before you commit it.
   - If you are unsure of a word, stress, or nuance — admit it. Mark it `<!-- VERIFY: {word/claim} -->` and state what is unclear. `<!-- VERIFY -->` is NOT a failure signal; it is the correct honest move, and reviewers score its presence positively on the honesty axis.
   - Never invent. Silent invention is the worst failure mode.
   - **State rules honestly. Cite or hedge — never invent.** A grammar rule is "strict" ONLY when the plan YAML or a named Ukrainian authority (Правопис 2019, Антоненко-Давидович, VESUM, Захарійчук / Большакова / Авраменко textbooks) says so explicitly. Default language when a rule has exceptions or sociolinguistic variation: *common*, *typical*, *usually*, *most often*. When your pre-training and the plan/wiki disagree, the plan/wiki wins — and if its claim surprises you, flag it inline with `<!-- VERIFY -->`.

2. **Linguistic purity.**
   - Use **Cyrillic only.** No Latin transliteration of Ukrainian words. IPA is allowed only in explicit phonetic sections (e.g., Г [ɦ]).
   - Strictly avoid **Russianisms.** Strictly avoid **Surzhyk** (шо, тіки, січас). Detect every **calque**.
   - Apply **stress marks** (ˊ) ONLY inside the vocabulary section (`## Словник`). Never in teaching prose.

3. **Source authority. Verify, don't invent.**
   - Consult in this order: **VESUM** (does this form exist?) → **Правопис 2019** (spelling) → **Горох / Словник.UA** (stress) → **Антоненко-Давидович** (is this natural or a calque?) → **Грінченко** (etymology).
   - Every factual claim traces to a specific citation already in the plan's `references` block. Do not invent citations. Do not attribute claims to textbooks you have not seen.
   - If references disagree, state the disagreement. Do not paper over it.

4. **Decolonized pedagogy.**
   - Ukrainian is not a dialect of Russian. Never use Russian as the baseline Ukrainian is described against.
   - Frame unique Ukrainian features (Ґ, trilled Р, м'який знак rules) on their own terms.
   - Cite Ukrainian textbooks (Захарійчук, Большакова, Авраменко, Заболотний, Вашуленко) and Ukrainian authorities (Антоненко-Давидович). Not Russian linguistics tradition.

---

## Exercise Placement — Markers Only (THE HARDENED CONTRACT)

**Do NOT write exercises directly.** Activity items are generated by a separate downstream step from the plan's `activity_hints`. Your job is to (a) teach the concept with engaging prose and (b) place a marker showing WHERE each exercise belongs.

### Marker format

After the teaching prose that sets up a given exercise, insert one line:

```
<!-- INJECT_ACTIVITY: {activity_id} -->
```

Use a short, stable id derived from the plan's `activity_hints[].type` + `section`. For this fixture, the ids are:

- `odd-one-out-soft-sign` — plan `activity_hints[0]` (odd-one-out, «М'який знак»)
- `fill-in-apostrophe-or-soft-sign` — plan `activity_hints[1]` (fill-in, «Апостроф»)
- `error-correction-missing-apostrophe` — plan `activity_hints[2]` (error-correction, «Апостроф»)
- `group-sort-ь-apostrophe-nothing` — plan `activity_hints[3]` (group-sort, «Апостроф»)
- `match-up-voiced-voiceless-pairs` — plan `activity_hints[4]` (match-up, «Дзвінкі і глухі»)
- `true-false-voicing-rules` — plan `activity_hints[5]` (true-false, «Дзвінкі і глухі»)
- `quiz-g-vs-ghanoki` — plan `activity_hints[6]` (quiz, «Вимова українських звуків»)

Place each marker immediately after the prose paragraph that teaches the concept that exercise will test. Do NOT cluster markers; spread them across the relevant sections.

### WRONG — do not write these inline

The failure mode is not "DSL blocks" — it is prose that describes-an-exercise with a short item list. Writers rationalize these as "just examples." They are exercise authoring, and they are rejected.

WRONG (numbered inline items — the canonical failure from the previous bakeoff):
> **Вправа 4. Розподіл слів на три колонки.** Вісімнадцять слів потрібно розподілити за трьома колонками: з м'яким знаком, з апострофом і без жодного знака. Наприклад: «сіль» → Ь; «м'яч» → апостроф; «риба» → без знака.

WRONG (fill-in slots written inline):
> **Вправа 2. Вставити Ь або апостроф.** Наприклад: «сім_я» → «сім'я»; «ден_» → «день»; «п_ять» → «п'ять».

WRONG (prompt disguised as a reading drill — activity items scattered into prose):
> Для тренування: _удзик, _ора, _анок, _олова. Обери правильну літеру для кожного слова.

WRONG (reading-aloud prompt masquerading as prose when the plan has it as a fill-in activity):
> Прочитай уголос: сім'я, п'ять, буряк, дев'ять.

RIGHT — all four of those belong as a single injection marker right after the teaching section that sets them up:
> [...teaching prose about апостроф after губні + р...]
>
> `<!-- INJECT_ACTIVITY: fill-in-apostrophe-or-soft-sign -->`

### STOP test — before you finish a section

Read it back. If either of these is true, you are authoring an exercise inline and you must delete it:

1. Your prose contains a numbered list of two or more exercise items (`«сіль» → Ь; «м'яч» → апостроф; ...`, `_арно, _удзик, _ора, _рунт`, `сім_я, ден_, п_ять`), whether as a bulleted list, inline semicolon-separated list, or the phrase «наприклад: ...» followed by more than one item-with-answer.
2. Your prose contains the phrase «Вправа N.», «Exercise N.», or any task-instruction verb («розподіли», «з'єднай», «обери», «встав», «прочитай уголос», «познач») followed by a task description and the items to work on.

If either is true: delete the items AND the instruction. Replace with `<!-- INJECT_ACTIVITY: {id} -->`.

### Authority — `activity_hints[N].items` belongs to the NEXT step

When the plan shows `activity_hints[group-sort].items: 18` or `items: 6`, those counts are a signal to the downstream ACTIVITIES step. They are NOT a request for you to demonstrate the items. Your deliverable is (a) the teaching prose and (b) a correctly-placed marker. Item generation is explicitly the responsibility of the next pipeline step.

The `## Практика` section, if you include one, should contain **only injection markers**, optionally bracketed by one or two short Ukrainian sentences that transition into the practice block. No items, no instructions, no numbered exercise descriptions.

---

## Input context

**Plan (your contract — immutable, authoritative):**
`/Users/krisztiankoos/projects/learn-ukrainian/.worktrees/codex-l1uk-plan-special-signs/curriculum/l1-uk/plans/a1/special-signs.yaml`

Read this first. It specifies objectives, content_outline sections (with per-section word targets and teaching points), vocabulary_hints, activity_hints, grammar points, register, and textbook references. Your module must cover every plan requirement **except** the activity items themselves — those belong to the downstream step.

**Wiki (retrieval context — use selectively):**
`/Users/krisztiankoos/projects/learn-ukrainian/wiki/pedagogy/a1/special-signs.md`

The wiki contains rich pedagogical framing, minimal pairs, textbook exercise examples, and decolonization context. USE IT FOR: pedagogical framing ideas, minimal pairs to teach with, textbook exercise patterns, decolonization principles. IGNORE IT WHEN: it addresses "English-speaking learners" or "L2 учні" (this module is UK-native). If the wiki and the plan disagree on any specific (mnemonic, consonant count, rule formulation), the plan wins.

**Handling disagreements between retrieval contexts:**
- Plan > wiki > your own prior knowledge.
- If retrieval contains multiple valid variants (e.g., «Де Ти З'їСи Ці ЛиНи» vs «ДЗіДЗьо, Де Ти З'їСи Ці ЛиНи»), use the one cited in the plan.

---

## Output contract

Write your module to: `experiments/writer-bakeoff-2026-04-22/hardened-retest/opus/special-signs.md`

Structure:

- `# Title` (Ukrainian, matching the plan's `title`: Особливі знаки)
- `## Цілі модуля` (objectives, bullet list, Ukrainian)
- One `##` section per `content_outline` entry in the plan, in the same order. Section title matches the plan. Target words per section ≈ plan's `words` field, within ±10%.
- `## Словник` — vocabulary section with stress marks, using **only words from the plan's `vocabulary_hints`**. Each entry: word (stressed) — short Ukrainian note — one natural example sentence in Ukrainian.
- `## Практика` — **markers only.** One `<!-- INJECT_ACTIVITY: {id} -->` per `activity_hints` entry from the plan, placed in an order that matches the teaching flow. Optionally, one short Ukrainian transition sentence before the markers. No items, no instructions, no numbered exercises.
- `## Підсумок` — short summary reflecting the plan's final `Підсумок` section. May include a short self-check question list as prose (e.g., «Що робить Ь?») — this is a summary, not an exercise.

**Alternative marker placement:** if it reads more naturally, you may place each marker INLINE after the relevant teaching section (e.g., `<!-- INJECT_ACTIVITY: odd-one-out-soft-sign -->` right after the soft-sign teaching section) and drop the `## Практика` section entirely. In that case, the module ends with `## Підсумок` after all seven markers have been placed.

**Total word count:** ≥ plan's `word_target` (1200 for A1). Overshoot to ~1400–1600 is expected and acceptable. Undershooting below the target is a failure. **Markers and their surrounding empty prose do NOT count toward the word target.**

**No English scaffolding** in prose. Ukrainian-only teaching prose. No English vocabulary glosses — this is the UK-native track.

---

## What counts as "done"

Your output is done when ALL of these are true:

- Every plan `objective` is addressed in the lesson prose.
- Every word in `vocabulary_hints.required` appears in the `## Словник`, with stress, Ukrainian note, and example.
- Every `activity_hints` entry has exactly ONE matching `<!-- INJECT_ACTIVITY: {id} -->` marker in the module. Seven hints → seven markers. No more, no fewer. No items.
- Word count ≥ plan's `word_target` (1200). Overshoot expected.
- Every `<!-- VERIFY -->` marker is either resolved-and-removed, or surfaced with an explicit explanation for the reviewer.
- Zero Latin-alphabet Ukrainian words in teaching prose.
- Zero Russianisms. Zero Surzhyk. Zero unflagged calques.
- STOP test passes: no numbered inline item lists, no «Вправа N.», no task-instruction verb followed by items.

If any of these is not true — do not stop. Fix it.

---

## Worktree / commit / push

The dispatcher placed you in a pre-created git worktree on your own branch. Work in your current directory, commit, push. Do NOT open a PR. Do NOT merge to main.

Commit message:

```
content(l1-uk/a1): hardened-retest write of special-signs by opus

Produced for the 2026-04-22 writer-harden retest (issue #1370).
Uses the markers-only contract from scripts/build/phases/v6-write.md.
Not for merge to main; retest fixture output only.
```

---

## Time budget

~30–60 min. If you hit a blocker (YAML parse issue reading plan, unclear mnemonic, genuinely ambiguous pedagogical point), commit what you have and surface the blocker in the commit message footer.
