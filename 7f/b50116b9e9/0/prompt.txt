[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/6714-antonym-citation-fix
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Task: fix Claude CHANGES_REQUESTED on PR #6714 (citation accuracy)

**Driver:** grok-atlas · language lane
**X-Agent:** `cursor/6714-antonym-citation-fix`

## CAPACITY_CARD
pick=cursor+gemini-3.6-flash-high status=Cursor Ultra 100% language pin (AGY 5h not yet reset)
avoid=agy-5h-until-reset,habit-codex
source=codexbar+claude-cf

## Frozen outcome
- User-visible: every remaining `wiktionary-explicit-antonym` pair in `data/lexicon/antonym_pairs.yaml` has the other leg actually listed under **Антоніми** on that lemma's uk.wiktionary page (or an in-repo Wiktionary cache / `data/sources.db` dump — prefer in-repo, live only to confirm).
- Every remaining `sum11-lexicographer-pointer` pair is confirmed from in-repo SUM / `mcp__sources__query_sum20` / `data/sources.db`. If SUM is unreachable, **drop** the unverifiable pair rather than keep a claim.
- Non-goals: do not invent replacements. Do not touch heritage, synonym verdicts, or `generate_practice_deck.py` factory lock. Do not merge.
- Held-out: quote dropped-pair count + remaining pair/unique-lemma counts; update `tests/test_generate_practice_deck.py` `len(pairs) == N` to the new live length.
- Stop: if you cannot verify a curator class, delete that class's unverified new pairs. Residual named list in the PR body.
- Completion: push to PR #6714 (`cursor/practice-antonym-attested-grow`). Leaves #4387 #4700 open.

## Claude findings at `755ee563a99747407a72605c88be3b919511222c`
Sampled 9 of 285 `wiktionary-explicit-antonym`; 3 failed live Антоніми:
- `а` ↔ `зет` — empty; idiom "від а до зет", not an antonym
- `атака` ↔ `захист` — page lists контратака, not захист
- `безлюдний` ↔ `заселений` — empty

Also flag `аристократ` ↔ `демократ` (sum11) as linguistically suspicious.

## Work
1. Worktree only. Checkout PR #6714 head.
2. Full-batch verify all 285 wiktionary-explicit **new** pairs (not a sample). Drop mismatches.
3. Full-batch verify all 280 new sum11 pairs from **in-repo** SUM/sources. Drop unverifiable.
4. Recompute counts; fix the test expectation; commit + push.
5. Do **not** add new pairs in this PR.

## #M-4
Quote: dropped N wiktionary, dropped N sum11, remaining pairs/unique, pytest output.