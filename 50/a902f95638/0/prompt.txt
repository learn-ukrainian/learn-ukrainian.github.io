[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/kimi/infra-5375-5374-surface-normalization
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Dispatch brief — infra-5375-5374-surface-normalization (TWO issues, TWO PRs)

**Issues:** #5375 (combining stress marks) and #5374 (apostrophe glyphs) — read both fully.
Both were REOPENED 2026-07-28 after an evidence audit voided a fabricated close; treat them
as unfixed until you prove otherwise in the current code.

## Evidence preamble (#M-4, binding)
Every claim tool-backed: claim + deterministic command + quoted raw output. FIRST verify
whether any part is already fixed on main (git log/grep the resolver) — if a piece is
genuinely done, say so with evidence instead of re-implementing it.

## Shippable units (one PR per issue, same worktree, sequential commits OK)
1. **#5375:** normalize combining stress marks (U+0301) ONLY for vocabulary candidate
   matching/VESUM lookup, preserving displayed evidence bytes. Acceptance in the issue:
   verify against the a1/special-signs vocabulary ledger + regression tests.
2. **#5374:** treat the three Ukrainian apostrophe glyphs (ASCII apostrophe, right single
   quote, modifier-letter apostrophe) as equivalent ONLY for candidate comparison/VESUM
   lookup; preserve original evidence bytes; reject glyphs outside the three.

## Acceptance criteria (each PR, verify BEFORE opening)
1. Regression tests per issue; mutation-check each (#M-16): revert fix → test fails; quote it.
2. A false-MISSING repro from the issue scenario turns into a match, quoted before/after.
3. `.venv/bin/pytest` green on affected files; `.venv/bin/ruff check` green on changed files.
4. No change to displayed/stored evidence bytes — state this explicitly.

## Out of scope
- Any broader Unicode normalization beyond the two issue-scoped classes.
- Content/ledger edits.

## CI gate note
If your change touches ANY file under `scripts/lexicon/`, the blocking "Atlas manifest
freshness" gate requires regenerating the fingerprint sidecar IN THE SAME PR:
`.venv/bin/python -c "from scripts.lexicon.manifest_fingerprint import write_fingerprint; write_fingerprint()"`,
verify `.venv/bin/python scripts/lexicon/check_manifest_freshness.py` exits 0, and commit
`site/src/data/lexicon-manifest.fingerprint.json` alongside your change.

## Process (numbered, binding)
1. Work only inside your dispatch worktree.
2. Implement #5375 → tests → commit → then #5374 → tests → commit (separate PRs preferred;
   if the resolver change is genuinely shared, one PR referencing both issues is acceptable —
   say which you chose and why in the PR body).
3. Conventional commits with trailer `X-Agent: <your-lane>`.
4. Push; open PR(s) referencing the issue(s). Do NOT enable auto-merge; do NOT merge.

---
ROUTING_CARD_V1
task_id: infra-5375-5374-surface-normalization
tier: practical
model_x_harness: (set at fire time from capacity_pick — cursor/auto or kimi/k3-256k)
why_this_tier: bounded matcher fixes with crisp acceptance; sibling pair batched for one worker
advisor_packet: fable (this brief)
owned_paths: scripts/ (vocabulary surface resolution) + its tests
acceptance_cmd: .venv/bin/pytest <affected test files> && .venv/bin/ruff check <changed files>
alternatives_considered: (filled at fire time)
parallel_free_seats: (filled at fire time)