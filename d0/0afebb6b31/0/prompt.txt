[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/cursor/6614-never-pushed-fix
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Fix CF REQUEST_CHANGES on PR #6614 (never-pushed force-delete)

## Context
PR #6614 (branch `cursor/4674-squash-branch-delete`) is open, CI green.
Cross-family AGY review @ head a4d0ad7b **VERDICT: REQUEST_CHANGES**:
- Allowing `git branch -D` on **never-pushed** locals risks permanent data loss (no remote backup).
- Issue #4674 / #M-10a intent is **post-squash cleanup** when remote tip is gone (`gone-upstream`), not unrestricted never-pushed force-delete.

## Required change
1. In `agents_extensions/shared/hooks/guard-branch-switch-in-main.py`: **block** force-delete of `never-pushed` branches (same class as live-upstream+unpushed, or with a clear reason string).
2. Keep allowed: `gone-upstream`, `fully-pushed` (and existing blocks for protected / checked-out / live-unpushed).
3. Update tests in `tests/test_guard_branch_switch_in_main.py`:
   - `test_issue_4674_force_delete_never_pushed_is_allowed` → invert to **blocked** (rename accordingly).
   - Keep gone-upstream / fully-pushed allow tests green.
4. Update docs: `docs/best-practices/git-hygiene.md` and `gitflow.md` — remove never-pushed from sanctioned list; document that never-pushed still needs merge proof or push-then-gone path.
5. Push to the **same branch** `cursor/4674-squash-branch-delete` (update PR #6614). Do not open a second PR.

## Worktree
`scripts/delegate.py dispatch --agent cursor --worktree --task-id 6614-never-pushed-fix --brief ...`
Use subtree layout. X-Agent: cursor/6614-never-pushed-fix

## Success
- PR #6614 head updated; never-pushed blocked; gone-upstream still allowed; pytest for the guard file green.
- Do not merge; leave CF to orchestrator.