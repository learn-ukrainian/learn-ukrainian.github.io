[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/agy/infra-5214-sidecar-fix
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

# Dispatch brief — infra-5214-sidecar-fix (CI fix on PR #6828)

Your PR #6828 (branch agy/infra-5214-grow-lexicon-checkpoint) failed the blocking
"Check DB-free Atlas manifest freshness" gate: PRs touching scripts/lexicon/ must
regenerate the fingerprint sidecar in the same PR.

## Steps (all inside your dispatch worktree)
1. `.venv/bin/python -c "from scripts.lexicon.manifest_fingerprint import write_fingerprint; write_fingerprint()"`
   (use the primary venv's absolute interpreter path)
2. Verify locally and QUOTE the output:
   `.venv/bin/python scripts/lexicon/check_manifest_freshness.py` (must exit 0)
3. Commit ONLY `site/src/data/lexicon-manifest.fingerprint.json` — conventional message
   `chore(lexicon): refresh manifest fingerprint sidecar (#5214)`, trailer `X-Agent: agy`.
4. Push to the same branch. No auto-merge; no merge.

ROUTING_CARD_V1
task_id: infra-5214-sidecar-fix
tier: heap
model_x_harness: agy/gemini-3.7-flash-high (authoring lane, CI fix on own PR)
advisor_packet: fable (this brief)
owned_paths: site/src/data/lexicon-manifest.fingerprint.json
acceptance_cmd: .venv/bin/python scripts/lexicon/check_manifest_freshness.py
alternatives_considered:
  - none — authoring-lane rule binds for fixes on the lane's own PR
parallel_free_seats: n/a (single-command fix)