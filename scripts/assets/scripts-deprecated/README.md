# Deprecated Scripts

These vanilla JavaScript files were used for the HTML output before switching to Docusaurus.

**The active code is now in:** `docusaurus/src/components/*.tsx`

These files are kept for reference only and are NOT used in the current system.
