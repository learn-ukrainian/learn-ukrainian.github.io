#!/usr/bin/env python3
"""Backward-compat stub — real code lives in pipeline.core.

All imports from pipeline_lib are forwarded to pipeline.core.
New code should import from pipeline.core directly.
"""
# ruff: noqa: F401, F403
from pipeline.core import *
from pipeline.core import (
    ACTIVITY_CONFIGS,
    ARCHIVE_DIR,
    ARCHIVE_GIT_REF,
    ARCHIVE_SKIP_TRACKS,
    ARCHIVE_WORD_THRESHOLD,
    IMMERSION_RULES,
    LEVEL_CONSTRAINTS,
    MAX_FIX_ITERATIONS,
    PHASES_DIR,
    PRO_MODEL,
    PRO_TRACKS,
    PROJECT_ROOT,
    SCRIPTS_DIR,
    SEMINAR_TRACKS,
    TMP_DIR,
    TRACK_SKILLS,
    VENV_PYTHON,
    ModuleContext,
    _build_exact_section_titles,
    _build_phase2_expansion_prompt,
    _build_section_budget_table,
    _check_archive_fits_outline,
    _clean_textbook_text,
    _dispatch_prompt,
    _extract_delimited_content,
    _gemini_output_path,
    _get_activities_template,
    _get_checkpoint_guidance,
    _get_checkpoint_review_guidance,
    _get_content_template,
    _get_prompt_tier,
    _get_scoring_output_table,
    _get_scoring_section,
    _get_textbook_grade,
    _get_ukrainian_topic,
    _get_writing_style,
    _get_writing_tone,
    _init_log,
    _is_checkpoint_module,
    _now_iso,
    _parse_section,
    _prefetch_sources_for_phase_B,
    _prefetch_textbook_activity_examples,
    _prefetch_textbook_examples,
    _read_phase_file,
    _validate_activities_yaml,
    bilingualify_section_titles,
    build_placeholders,
    check_prompt_health,
    detect_archived_prose,
    dispatch_gemini,
    dispatch_gemini_raw,
    fill_template,
    get_activity_config,
    get_expansion_method,
    get_h3_word_range,
    get_immersion_rule,
    get_item_minimums_table,
    get_level_constraints,
    get_level_label,
    get_pedagogical_constraints,
    get_structural_rules,
    get_tier_guidance,
    get_track_skill,
    load_state,
    log,
    log_prompt_health,
    mark_phase,
    phase_2_content,
    preflight_v2,
    restore_from_archive,
    run_script,
    run_verify,
    run_verify_prose_only,
    save_gemini_session,
    slug_for_num,
    track_to_level_focus,
    write_completion_report_v2,
    write_review_with_hash,
)
