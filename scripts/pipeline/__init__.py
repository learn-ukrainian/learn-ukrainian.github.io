"""Pipeline package — decomposed from pipeline_lib.py (now pipeline/core.py).

Submodules are imported directly by consumers (e.g., `from pipeline.state import load_state`).
This __init__.py is intentionally minimal to avoid circular imports.
"""
