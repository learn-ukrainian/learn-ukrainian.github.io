[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/grok/work-control-plane-public-p1
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

You are the Grok implementation owner for public PR #6842 in this existing dispatch worktree.

Exact reviewed base: e2d3aeeaadb95f73477f482457b7ce614af71768
Exact reviewed head: 3a7c7f78617c154f1e5e00eb1ba88c4ec3222921
The independent Terra review returned REQUEST_CHANGES with exactly three blockers. Fix their root causes, add focused regressions, run proportionate verification, commit with trailer `X-Agent: grok/work-control-plane-public-p1-fix4`, and push the existing branch. Do not merge or change PR state. Do not expose private data.

Blockers:

1. `scripts/work/schema/__init__.py` / `scripts/api/work_router.py`: repeated allowed multivalue filter values are not canonicalized before forming permanent cache keys. Canonicalize every multivalue filter as a deduplicated, deterministically sorted sequence (or reject duplicates consistently), including the singleton configured `repository_id`. Add regression coverage proving duplicate and non-duplicate query forms share the same canonical cache key / do not create distinct cache entries.

2. `scripts/work/schema/work_projection.v1.json`: `source_id` is advertised and accepted but absent from the closed `filters_applied` object. Add the enum-backed property and endpoint/schema validation coverage for `?source_id=public-monitor`. Preserve the schema contract and update its documented digest/provenance wherever the public repository requires it.

3. `scripts/work/sources_public.py` / `scripts/work/normalize.py`: formal-review rows are collected without an exact public-repository predicate and unlinked rows can emit a supplied non-public repository. Enforce the exact configured public repository at the source query and normalization boundary, remove suffix matching, and test mixed-repository input cannot appear in the public projection.

Constraints:
- Keep P1 public-only, read-only, bounded, privacy-safe, and mutation-free.
- Do not add the private adapter or browser aggregation in this PR.
- Preserve compatibility unless the schema digest necessarily changes.
- Inspect the full diff and avoid unrelated edits.
- Run the focused Work tests, Monitor contract tests if touched, Ruff on changed Python, and `git diff --check origin/main...HEAD`.
- If any blocker statement is inaccurate, prove that with exact code/test evidence before deciding not to change it.

Return a concise report with commit SHA, changed paths, commands/results, and any residual risk.