[delegate worktree]
Run all file edits, tests, and git commands inside this worktree: /Users/krisztiankoos/projects/learn-ukrainian/.worktrees/dispatch/grok/work-control-plane-public-p1
Do not switch branches in the main checkout.
If you need a fresh base, run `git fetch origin main` INSIDE this worktree — `origin` here points at the canonical GitHub remote and fetch works from any linked worktree. NEVER use the primary checkout as a source of freshness (no `git -C <primary> pull/fetch/checkout`); it is the human's interactive home.

[shared project interpreter]
Never create, copy, symlink, activate, or use a `.venv` inside this worktree. Run every project Python command with `/Users/krisztiankoos/projects/learn-ukrainian/.venv/bin/python` (the absolute primary interpreter), never `python`, `.venv/bin/python`, or `python -m venv .venv`. Do not change `PYTHONPATH` merely because the worker cwd is a worktree.
Sparse-checkout is active: these top-level trees are NOT present: curriculum, wiki. If you need them, re-dispatch with --sparse-include <dir> or --full-checkout (do not invent content for missing paths).

[optional delivery signal]
Commits on your dispatch branch are sufficient proof of delivery on their own. If you finish with zero commits (a verified no-op), you MAY end your final response with one machine-readable line as positive proof: `DELIVERABLE: {"outcome":"no_change","reason":"why no changes are required"}`. This line is optional — its absence never fails the dispatch.

You are the Grok implementation owner for public PR #6842 in the existing dispatch worktree.

Exact head: `374d9ac6dd22572221ea233ca5c1647a2955b950`.

The exact-head review found one regression from fix9:

- Unscoped generic `/api/delegate/tasks` and `/api/delegate/active` responses now emit each task’s authoritative `repository`, which can disclose private repository IDs. Repository attribution is needed internally for pre-pagination filtering, but must not be serialized in legacy unscoped responses.

Fix the response-shape boundary so repository/repository_id is used only while reading/filtering raw task state. Preserve the exact legacy public summary shape for unscoped and scoped internal results; Work may receive already-filtered rows without any repository field and rely on the trusted scoped-loader contract plus its own safe wrapper metadata. If Work’s defense-in-depth admission currently requires the emitted field, refactor the internal function contract so admission/filtering is proven before redaction without re-exposing repository identity. Never include repository values in generic delegate API JSON.

Add regressions proving:
- unscoped `/api/delegate/tasks` and `/api/delegate/active` do not contain `repository` or `repository_id` even when raw task state carries a private value;
- repository-scoped internal calls filter before totals/limits but also return rows without repository fields;
- Work still receives/normalizes only public-scoped delegate tasks and cannot be starved by >500 private/unclassified rows;
- existing response keys remain backward compatible.

Run delegate API, complete Work/API/privacy/dashboard, fleet observer/dashboard, Monitor contracts, Ruff, and `git diff --check`. Schema is untouched and digest must remain `89fb9c1eec41baaa00a328d456340111163c1e3ab899cd7baa15e284fff65bde`.

Commit with trailer `X-Agent: grok/work-control-plane-public-p1-fix10`, push if permitted, do not merge or change PR state. Return exact verification and residual risk.